# DONE

Completed tasks, newest first, one line each. The full narratives for entries
before 2026-09-01 are in this file's git history; measurement records live in
`docs/agent-evals.md`.

## 2026-09-24

- [x] Frontier corpus v1 and its first baseline (#318, PR 4) — 17 scenarios in three tiers (`tests/frontier_corpus.py`), each with dev and held-out wordings and deterministic checkpoints over the board, settings, saves and tool results; a new 150-ply fixture (`tests/late_game_150_plies.pgn`, seeded legal play, seed 0; `review_game` over it 1.0 s). The harness now snapshots settings per turn and reads saves back through `GameSession.load`; `test_frontier.py` checks every variant against the parsers on its own setup board and grades every scenario on a do-nothing episode of its shape (85 tests). Baseline 340 samples, 0 breaches, 0 infra: 6 scenarios 10/10 on both splits (gate candidates), two 0/20 (a relative "harder" jumps to `maximum`; a 138-ply rewind is one undo), and dev/held-out gaps of up to ten samples on a single wording. Two checkpoints fixed first (an ambiguous "play that one"; a difficulty scenario that could not say why it scored zero). `docs/frontier-history.jsonl` holds the run; table in `docs/agent-frontier.md`

## 2026-09-23

- [x] Frontier eval harness (#318, PR 3) — the tier for live scenarios built to be hard: measured, never gated. `tests/frontier.py` (scenarios with `dev`/`heldout` variants and named deterministic checkpoints; `measure()` takes a fixed N per scenario, cycles variants, grades whole-task + rubric partial credit, reports passes with a Wilson interval, checkpoint hits and failure modes; provider deaths re-taken and counted, never scored), `tests/test_agent_frontier.py` (opt-in `CHESSAPP_AGENT_FRONTIER=1`; a `frontier_header` with git sha, model, temperature and planner/narrator/offer shas; an item fails only on an invariant breach, a broken scenario or nothing measured), `tests/frontier_corpus.py` (two starter scenarios, tiers 1–2). Every live sample runs the trajectory invariants step by step — `CountingProvider` now keeps each request's messages and offer so #315's offer check reads the real ones — and a `model` step routed anywhere but the planner is a broken scenario. `test_frontier.py` (15 tests, off the GPU, the whole runner over a scripted provider), `test_counting_provider.py` +1. Live smoke on `ade22ed`, 3 samples each, dev: `undo_replace_and_judge` 2/3 (rubric 0.93); `knight_ask_then_change_of_mind` 0/3 on an ambiguous "how do things stand" (the model described, fairly) → wording fixed → 3/3; no breaches, no infra. `docs/agent-frontier.md`. Harness-only: no prompt, model or loop change

- [x] Stockfish dying inside a planner tool (#329, found by the #318 walks) — `evaluate_position`/`get_best_moves` let `EngineTerminatedError` out of dispatch (a bare 500 over a batch whose move had landed), and a settle whose engine died (odd-ply undo, resume, new game as black, `/api/game/undo`, startup) went back to awaiting the player with the engine to move: nothing collected the reply, and the player's next move was played for the engine's colour. Now in layers. `EnginePlayer._run` relaunches a dead Stockfish once, re-applies the strength it had set, and repeats the call (a real SIGKILL recovered in 0.31 s) — the retry is the code's, never Glitch's; a closed engine stays closed. A second death: `dispatch` answers `ok: false, retry: never`, saying whether the call changed the board, so Glitch voices it; a failed settle leaves `player_move_applied` (the reply owed, #284's healing collects it once) and `undo`/`new_game`/`resume_game` report `engine_reply_owed`; `apply_player_move` refuses the engine's side outright; hint/review/difficulty answer 503. `DyingEngine` now dies on every call. Tests: `test_engine.py` +5 (incl. a real kill), `test_engine_failure.py` +8, `test_coordinator.py` +3, two trace tests and one coordinator test that had been playing the engine's side corrected; the three strict xfails became `reproduces:#329` regressions and the walks' engine deaths are unrestricted again (300/300 green; unfixed main 7/100 red). 2573 backend tests. No prompt, model or loop change, no gate

- [x] Trajectory breadth (#318, PR 2) — the walks now speak from the panel and two delegate threads (keyed and unkeyed, plus retries of a keyed exchange), answer confirmations with the button, start one walk in four from the 84-ply fixture, send batches past the per-turn cap, and inject llama-server dying mid-command, Stockfish dying for a step, and a narrator stalled past its (harness-shortened) budget. Six new invariants: an owed reply is settled exactly once; a yes runs a destructive op only from the surface it was asked of, about the board it was asked about, and then runs it once (#281); a retry is the stored answer or 409 and reaches no model (#291); late words are never said or remembered and a stall is bounded (#316); the call and search budgets hold (#288). Each shown to fire: with the check removed or the fix undone, #316 35/100 walks red, #281 7/100, #291 45/100, and each named regression red under its own mutation only. Found a real bug on the way — #329, Stockfish dying inside a planner tool (analysis → 500; an undo's settle → the player moves for the engine's colour) — filed, and pinned as three strict xfails. 48 seeds × 20 steps + 10 named regressions ≈ 35 s in CI; 2553 backend tests. Test-only, no gate

- [x] Composed interaction trajectories in CI (#318, PR 1) — `tests/trajectory.py` walks the shipped app (`build_app` over a `ScriptedProvider` and a deterministic legal-move engine) through seeded sequences of planner batches with several calls per response and up to two rounds, fast-path moves, literal yes/no, board-endpoint moves, undos and new games, and restarts over the same save dir; after every step it checks eight invariants (no 5xx; nothing succeeds after a landed ask; every planner request offers `ask_player` over exactly the menu of the last board it shows; no silent mutation; a coherent state document; no engine reply left owed; the response is the board; a restart restores the game). `tests/test_trajectories.py`: 64 seeds × 20 steps in CI (~20 s), named regressions tagged `reproduces:#314`/`reproduces:#315`/`prevents`, a firing test per invariant, a determinism test, and `CHESSAPP_TRAJ_SEED` replay / `CHESSAPP_TRAJ_SEEDS` soak. Verified against the fixes reverted in a scratch copy: 239/300 walks red without #314's, 176/300 without #315's, each named regression red on its own revert only; 300/300 green on main. Test-only, no gate

- [x] Reproducible serving identity and end-to-end voice latency (#317, PRs 1–3). PR 1 (#325): trace schema 2, with one `calls` entry per model round trip tagged by phase (`planner`, `closer`, `reaction`, `rewrite`, `answer`) and status (`ok`, `truncated`, `bad_args`, `failed`, `late` with `budget_ms`), unknown tokens `null`, totals computed from the list, and `planning` recording elapsed time, deadline and overrun; evalstats now reads the tags, which left no UNKNOWN split on its gate (52 passed, every pass-rate scenario 5/5). PR 2 (#326): a `serving` manifest recording the revision, session and experiment, every client setting, and what the server reports it runs (model file, ftype, build, n_ctx, llama-swap cmd). The server fields are read by one `/props` probe off the request path, only once `/running` says the model is loaded; each call also records its own `system_fingerprint`, `server_ms` and `cached_tokens`; the revision is baked into the image at deploy. PR 3: the browser mints an `interaction_id` when speech ends and sends it on the transcription, the command and the speech request. `speech` records hold timing and sizes only, never audio or words. The browser reports its milestones (`POST /api/telemetry/voice`, validated, 204 whatever happens) with no cross-clock arithmetic, and `scripts/latency_report.py` prints n, failures, censored counts and p50/p95/p99 by manifest, phase and cold/warm/contended condition. New `docs/latency-measurement.md`.

- [x] Closing narration can no longer hold a ready engine reply (#316) — #283 bounded every `Brain.narrate` call, but the brain route's words come from the loop's own closer inside `get_agent_response`, after the planner has played the player's move: a stalled closer held Stockfish's computed reply and the mutation lock (drags, undo, delegate moves) for up to the 300 s read timeout. The bound now lives in `LlamaBrain._close`, around the tool-free speech call alone (`within_budget`, moved with `LateReaction` to `chessapp/deadline.py`), so the thread that can act always returns with the plan's full record and only a words-only thread is ever abandoned; the socket hangs up 5 s later to free the llama-server slot. `_CLOSING_BUDGET_S` 10 s when the reply is owed and the closer is only reacting (trace: 0.8–2.0 s; gate: max 2.8 s), `_CLOSING_CEILING_S` 60 s when nothing is held or the closer thinks (gate max 34.0 s) — never a cut on a thoughtful answer (Ryan, 2026-09-23). A cut closer returns `narration_late` → trace `reaction_late`, and the player hears the fast path's late line (`_late_close_words`: the moves and the reply; the turn-stands line over any other change; `STUCK_REPLY` only when nothing changed). `test_reaction_budget.py` +11 (the issue's repro, a queued undo, late words nowhere incl. the next planner prompt, delegate route, one trace record, fast closer unchanged, question past the budget, fallback lines), `test_llama_brain.py` +10. 2403 backend tests. Gate 52 passed, 14 m 33 s, infra 0, every pass-rate scenario 5/5, `long_capture` 5/5 ×3, costs unmoved; a first cut without the thinking exemption failed `move_and_judgment` and `best_move_then_play` (record in `docs/agent-evals.md`)

- [x] The planner's offer follows the board it is shown (#315) — the loop resolved its tool offer and validation schemas once per command while #282's refresh re-showed the planner the board, so after "undo and move my king pawn" `ask_player(["e3", "e4"])` was refused against the pre-undo enum. The offer is now re-resolved at the refresh boundary and nowhere else, swapped only when it differs, and kept while a reply is owed (an enum narrowed then would be the engine's menu); `ask_player` withdraws and returns across the two-legal-moves line with the board, and its handler refuses with `retry: never` while the engine is to move or the game is over. Trace `offer_refreshes`. `test_llama_brain.py` +10 (the repro, a taken-away move rejected, undo/new_game/resume coherence, both <2 transitions, reply owed, no needless swap, a raising offer), `test_tools.py` +3, `test_trace.py` +1 and one extended. 2382 backend tests. Gate 52 passed, 13 m 13 s, infra 0, every pass-rate scenario 5/5 except `pgn_is_handed_over_not_recited` 4/5 (the known miss), `long_capture` 5/5 ×3; accepted cost ≈ +0.5 s planner latency per mid-command offer swap (A/B in `docs/agent-evals.md`)

- [x] A landed `ask_player` stops the batch at the call (#314) — the planner loop ran every call in a batch and only then checked for a successful ask, so `[ask_player(Nf3, Nh3), make_move(Nf3)]` played Nf3 under a `clarify` handoff. Every call after a landed ask is now answered "not run this turn" (the dispatcher's refusal, as a budget cap answers) and never dispatched, keeping one result per call; calls before the ask stand and are reported as done, and a refused or off-enum ask stops nothing. `test_llama_brain.py` +9 (move/read/setting/destructive after the ask; ask first, middle and last; two asks; refused and off-enum asks), `test_closing_pass.py` +1 (the issue's repro over the real registry and board). 2368 backend tests. Gate 52 passed, 12 m 08 s, infra 0, every pass-rate scenario 5/5 except `pgn_is_handed_over_not_recited` 4/5 (the known miss), `long_capture` 5/5 ×3, costs unmoved

## 2026-09-22

- [x] Delegate threads survive a restart, and a retry never acts twice (astra audit F11, #291, PR 3 of 3 — closes #291) — delegate conversations were process-lifetime, and a conductor that timed out had no way to retry without the pipeline running again. `ConversationStore(path)` rewrites `conversations.json` in the save dir atomically after every change and reloads it at startup (roles validated on load, id counters kept past every id on record; an unreadable file is an empty store and a warning). `POST …/messages` takes an optional `Idempotency-Key` (1–128 chars, stored on the user turn, scoped to the thread): a retry of a finished exchange returns the stored exchange without running anything — after a timeout or a restart alike — one that never finished (502, stale 409, crash) is 409 "did not complete", and a key reused for other words is 422. `resolve_actor` and the doc say plainly that `X-Agent-Actor` is an audit label, not authentication. `docs/persistence-and-identity.md` completed; pointers from `CLAUDE.md` and `BRIEF.md`. `test_agent_api.py` +13. No prompt, model or loop change, no gate. Checked live against the real brain: "play e4 for me" under a key, server killed by port and restarted, the same key returned the identical exchange with the board unmoved and one trace record

- [x] The live game survives a restart; a request can bind to one game (astra audit F11, #291, PR 2 of 3) — the board and the panel transcript lived in process memory, so a restart lost the game in progress, and a delegate could only bind to a board version, which every move bumps. The live game (session, transcript, board version) is checkpointed to `live.json` in the save dir whenever it changes — from the mutation guard's exit and the state broadcast, under the lock, deduplicated on (version, transcript) — and restored by `build_app` (`tools.restore_live_checkpoint`): replayed through the legality gate, a bad file is a fresh board and a warning, the version resumes one past the checkpoint so a number held across the restart is stale, and a checkpoint taken mid-exchange is settled. The pending confirmation is deliberately not persisted. Root-level and nested, so it is never listed as a save nor promoted by the legacy-save migration. `GameSession.game_id` (minted by construction, `new_game` and a resume; kept by moves and a restart) is in the state document and the trace, and every mutating request and delegate message may carry it beside `version` (mismatch: the same 409, `WrongGameError`). Not in the planner's view, so no prompt change and no gate. `test_live_checkpoint.py` +18; checked live against Stockfish on a scratch save dir (killed by port, restarted: same board and `game_id`, version 2 → 3)

- [x] Resume and named overwrites ask first (astra audit F11, #291, PR 1 of 3) — `resume_game` replaced a game in progress with no confirmation and `save_game` silently replaced an existing named save. `resume_game` now goes through the gate whenever a reset would (the player has moved, the game is not over), after the save has proved loadable (a missing or corrupt save arms nothing), and spends the command's destructive budget; `save_game` over an existing name asks whatever the board, except `autosave` and a name the same command already wrote (so "save as checkpoint, undo, save it again" stays one ask), and reports `replaced`. `GATED_TOOLS` names every tool that can arm a question and `CONFIRM_QUESTIONS` has one for each; `DESTRUCTIVE_TOOLS` stays the game-ending three, so a confirmed resume or save never certifies "game over". The gate's refusal text says what the op would cost (`_cost`: "would replace the existing save 'x'" for an overwrite). New `docs/persistence-and-identity.md`. `test_save_resume_gate.py` +19, `test_mcp_server.py` +2, six tests that resumed over a live game now answer the gate. Gate 52 passed, 12 m 47 s, infra 0; new scenarios `resume_mid_game_asks` and `save_over_existing_asks` 5/5; `pgn_is_handed_over_not_recited` 16/20, 15/20 on `main` vs 19/20 here in alternating blocks

- [x] A turn's trace says where its time went and what served it (astra audit F10, #290, PR 2 of 2 — closes #290) — the record carried per-call model latencies and nothing else about time, and nothing about configuration, so a slow turn could not say whether it waited on the lock, a tool, Stockfish or the guard, and two eval baselines could not be tied to a serving setup. `spans_ms` (whole ms, a key only for a phase that ran): `queue` (the mutation-lock wait, measured by `_mutation`, which now opens a per-request `_Spans`), `tool` (handler time via the registry's new `on_tool_done` observer — analysis Stockfish included), `engine` (collecting the reply to the player's move), `guard` (`_honest_words` less its rewrite's model time, already in `model_ms`), `total` (lock request to record). Speech is not a span: TTS is its own request. `serving`: 12-hex sha256 of the planner prompt, narrator prompt and offered tool schemas as they resolve at record time, beside the model name and server URL (`LlamaBrain.serving_identity`, labels from `create_llama_brain`, wired by `build_app`; `None` for injected brains and direct mode; a seam that raises loses the field, never the record). `test_trace_spans.py` +14; checked end to end through `build_app`. 2307 backend tests. No prompt, model or loop change, so no gate

- [x] Every model round trip a turn made reaches its trace cost (astra audit F10, #290, PR 1 of 2) — a reply the answer-reader called `unrelated` fell through to the fast path, resign or brain route, each of which *assigned* its cost over the reader's; a reader that died returned `model_calls=0` and was then skipped; and a round trip that raised out of the brain (the observe beat, the confirmed-op and resign narrations, the rewrite, a late reaction) left no call and no latency. `_ModelCost` is now always summed with `plus`, `_ModelCost.failed(started)` counts a raised call with the time the turn waited on it, `_MoveBeats.cost` carries the observe beat's either way, `LlamaBrain.read_answer` reports a dead provider as one call, and the `finally` writes the cost a dying turn had already spent. Unknown tokens stay unknown: `unmetered_calls` (on `AgentResponse`/`Narration`/`Answer` and the trace) counts calls that raised or returned no usage, so the totals read as a lower bound rather than a measured zero. `test_cost_accounting.py` +11 (the issue's 3/380 repro and its fast-path twin fail on `main`), `test_reaction_budget.py` +1, one `test_llama_brain.py` test that encoded the bug updated. 2293 backend tests. Gate 50 passed, 12 m 24 s, infra 0, every pass-rate scenario 5/5, all 228 samples `completed`

- [x] An input budget trims the oldest conversation, never the state or the ask; the late-game prompt gains its ceiling (astra audit F8, #288, PR 4 of 4 — closes #288) — conversation memory was bounded by turn count, not size, and nothing checked a prompt before it was sent; the narrator, carrying the whole conversation, was "the call a context overrun reaches first". `LlamaBrain.input_budget_tokens` (32k, estimated at 3 characters a token over contents, tool calls and the offered schemas — ~10× the heaviest measured prompt, far under llama-server's 131k): before the planner's opening call and every narrator call an over-budget prompt drops the conversation's oldest user/assistant pairs (`_admit`), never the system prompt, the state block, the brief or the latest exchange (where an unanswered `ask_player` question lives). The loop never trims mid-run (it only appends, so the KV prefix holds); a run whose own results outgrow the budget ends under `budget: input`, and a narrator prompt that still cannot fit is not sent. Trace `input_trimmed`. `late_game_tool_composition` asserts every call under `_LATE_GAME_PROMPT_CEILING` = 4,800 (the PR 2 baseline's 3,838 plus a quarter), the ceiling its docstring said waited for a baseline. `test_llama_brain.py` +5 (a 300-ply board and twenty 8,000-char exchanges trimmed to fit with the latest pair and `legal_moves` kept; a prompt that cannot fit never sent; results that outgrow the budget; the estimate), `test_trace.py` +1. Gate 50 passed, 12 m 29 s, infra 0, every pass-rate scenario ABOVE_FLOOR (`pgn_is_handed_over_not_recited` 4/5, the baseline's known recitation miss), late-game prompts unchanged

- [x] Per-turn tool-work and wall-time budgets; a budget stop speaks from what ran (astra audit F8, #288, PR 3 of 4) — the planning phase was bounded by iterations (4) and corrections (2) only; nothing capped calls per batch, Stockfish-backed calls per turn, or wall time, and every budget stop reached the player as the canned `STUCK_REPLY` with nothing in the trace saying which budget. `LlamaBrain` gains `max_tool_calls` (8), `max_analysis_calls` (3, `_EXPENSIVE_TOOLS` = the analysis reads plus `review_game`) and `planning_deadline_s` (60 s, read off each round trip's own start and checked between trips, never during one), sized from the deployed trace (at most 2 calls and 2 analysis calls in any of 236 turns, planning p99 8.7 s). A call past a cap is answered `"not run this turn"` through the dispatcher's own `refusal` and never dispatched, and the phase ends under the new stop reason `budget`. Ryan's call: any budget stop after real work — `max_iterations` and `correction_limit` included — now closes through the narrator on a `partial` handoff under `_BUDGET_NOTE` ("whatever the record does not show as done was not done"), so the player hears what landed in Glitch's words; a stop with nothing dispatched stays silent and gets the stuck reply. `AgentResponse.budget` / trace `budget` name `iterations | corrections | tool_calls | analysis_calls | wall_time`. Seven `test_llama_brain.py` tests updated to the new contract (the silent case now built from malformed-only calls), +5 budget tests, `test_handoff.py` +4 rows, `test_trace.py` +2. Gate 50 passed, 11 m 39 s, infra 0, every pass-rate scenario 5/5, all 228 samples `completed`

- [x] `review_game` hands the model a summary, not the per-ply table (astra audit F8, #288, PR 2 of 4) — the tool returned every move with classification, cp loss, best alternative and accuracy, one line per ply into the planner's context: 11,946 characters (~3.4k tokens, about a planner prompt's worth again) on the 84-ply eval fixture, where 66 of 84 moves are mistakes or blunders. It now returns `plies`, `accuracy`, `counts` and `critical` — each side's three worst non-`good` moves by cp loss (`analysis.critical_moves`; per side, so a lopsided game still names the player's), worst first, with `move_number`, `san`, `best`, `cp_loss`, `classification` — 927 characters on the same fixture. `ReviewedMove` gains `move_number`; the honesty guard's analysis numbers read `critical`. The full table stays the UI's (`/api/game/review`). No "see the review panel" pointer: the panel only renders post-game. `test_game_review.py` +6. Gate 50 passed, 12 m 12 s, infra 0, every pass-rate scenario 5/5

- [x] `/api/command` text is bounded like the delegate's (astra audit F8, #288, PR 1 of 4) — `CommandRequest.text` had no length bound while the delegate capped a message at `MAX_AGENT_MESSAGE_LENGTH` (8,000 chars), and one command is what both the planner and the narrator carry into their prompts. The panel's route now imports the same constant and refuses a longer command 422 before any turn opens (board and brain untouched); the command box carries a matching `maxLength` (`MAX_COMMAND_LENGTH`). Measured first: the deployed trace's longest utterance is 524 chars. No prompt, schema or loop change, no gate

- [x] The guard's two #289 false positives: placement is not a move, and a draw mentioned is not a draw reported — asked to show the position, the narrator listed it in piece-letter notation ("White: Ke1, Qd1, …") and the move class cut `Ke1` as a move nobody could play; a declined draw offer explained as "too early to call it a draw" was cut by the draw class, whose `(a|the) draw` read every mention as the result (a probe showed most ways of narrating a decline tripped it, and "Draw agreed." — the lie on a decline — did not). `VerifiedFacts.placements` (every non-pawn piece on any board the turn held, built by `api._verified_facts` from `piece_placement()`) lets a bare piece+square token naming a piece on its own square pass; `_DRAW` reads only result shapes ("that's a draw", "ended in a draw", "draw by …", "draw agreed/accepted"). Evidence first: all 6 live guard firings in the deployed trace were on correct replies (all fixed earlier), the current guard re-judges its 230 drafts to only the two real verbosity lies, before and after, and no draw lie exists anywhere in the record. Labeled corpus in `test_honesty.py` written red first, campaign misses added verbatim; `test_api.py` +1. 2257 backend tests. Gate 50 passed, 12 m 41 s, infra 0, `long_capture` 5/5 ×3, costs unmoved; campaign against `main` 17/20 → 20/20 on both `freeform_confirmation_answers[unrelated]` and `offer_draw_routes`

- [x] The planner asks through `ask_player`; a clarification names its moves (astra audit F9, #289, PR 2 of 2) — "two or more fit" used to end the loop with a free-form note the narrator paraphrased away, and on `main` both ambiguity asks came back "which rook and which square?" 20/20, naming none of the moves the player had to choose between. `ask_player` (the app's split registry only; MCP's caller asks its own user) takes `candidates` that `tools.brain_tool_definitions` narrows to an enum of the live `legal_moves` for assembly, the eval harness and the probe alike (withheld with fewer than two legal moves; the handler re-checks against the board). A landed ask ends the planning phase and the handoff is `clarify` with the candidates, which the brief asks the narrator to name and the advice licence covers. Planner prompt: "Two or more fit: do not guess — call `ask_player` with every entry that fits." The ambiguity scenarios now require every candidate named by SAN or square. Measured before shipping: probe screen interleaved per sample (partial candidate lists 6/20 → 0/20 against the tool under the old sentence, every neighbour 20/20), harness confirm in alternating blocks against `main` (`ambiguous_move` 0/20 → 20/20, `ambiguous_knight_then_selection` 0/20 → 20/20, six neighbours 20/20 both). Behaviour changes to know about: the king's-knight ask lists all four knight moves, and a bare "castle" with both castlings legal is now asked rather than played as O-O (the open `castle_both` finding, settled the way the planner's own rule reads). Probe gains a `drop_tool` knob and an `asks` rule. 2221 backend tests. Gate 50 passed, 12 m 56 s, infra 0, `long_capture` 5/5 ×3, costs unmoved; `offer_draw_routes` 4/5 on a draw-class false positive, logged

- [x] The narrator closes from a typed handoff the harness builds (astra audit F9, #289, PR 1 of 2) — the brain route's narrator closed from the raw tool results and the planner's free-form note, told to reply "based only on those results and that note"; a no-tool turn rode on the note alone, nothing said nothing had been done, and a false note ("undid your last move") could come back as "Done, taken back." `handoff.py` sorts a turn's results into done / refused / looked up (`READ_TOOLS`, every registered tool pinned to one side), derives the kind (`completed`, `partial`, `declined`, `reply`) from the results and the stop reason alone, and renders the brief: results with ids, "Done this turn: nothing." when nothing was, the note demoted to "the planner's reading of what the player wants (not a record of what happened)". `narrator_result_view` drops `fen`/`turn`/`legal_moves`/`captures` from every result on both briefs, closing the leak through `undo`/`new_game`/`resume_game` results and the fast path's confirmed-op and resign beats. `LlamaBrain.narrator_facts` (wired from `api.narrator_facts` by assembly and the harness alike, pinned) gives the narrator side-free facts read at handoff plus `reply_owed`, which becomes "the engine has not played its reply yet; the app announces it". Guard: `takeback` and `restart` classes checked against the turn's own `undo`/`new_game`, and `unplayed_reply`, which cuts a SAN only the engine could play on the board the narrator spoke over before its reply existed — on every route, so the fast path's "I played dxc6." guessed right is cut too (two `test_command.py` tests inverted, documented). Measured before shipping: 0/219 deployed drafts fire, labeled corpus in `test_honesty.py`. Narrator `_BASE` rewritten as a speaking contract (no tool language; ask with the options named). `AgentResponse.handoff` and trace `handoff`. 2206 backend tests (`test_handoff.py` +25, `test_closing_pass.py` +8 route-level acceptance tests, `test_honesty.py`, `test_api.py`, `test_llama_brain.py`, `test_eval_harness.py`, `test_trace.py`, `test_personality.py`). Gate 50 passed, 11 m 44 s, infra 0, `long_capture` 5/5 ×3, costs unmoved; the two `[unrelated]` misses are a pre-existing piece-list guard false positive (19/20 on both `main` and this tree, interleaved)

## 2026-09-18

- [x] The honesty guard certifies who won and how (astra audit F7, #287) — the ending class checked one boolean, `facts.ended`, so on any finished game "You win by checkmate", "I win" and "You resigned, so I win" all passed whoever had actually won and however; a documented policy since 2026-09-05 (one boolean, because the ending that *never happened* is the worse lie and a wider class was two more facts to assemble and two more ways to misread trash talk), which the audit asked to close with a typed fact rather than a wider regex. `VerifiedFacts` gains `winner` (`"player"` / `"opponent"` / None, from the player's side like every other fact) and `termination` (`game.py`'s vocabulary), both read off `GameSession.outcome()` by `api._relative_outcome`; a new `outcome` claim class reads them — and **adds no regex surface**: every alternative it matches (`checkmate`, `that's mate`, `(i|you) (win|won|lose|lost)`, `resign*`, `stalemate`) was already the ending or draw class's, and `_outcome_matches` returns true on a live board so the ending class keeps the lie there and one sentence draws one correction. On a finished game a termination word has to be the termination and an owned verb has to point at the winner (Glitch speaks: "I" is the opponent, "you" the player; a draw has no winner so either claim of one is unbacked); a finished game with an unknown termination fails closed, which is the `new_game` case where `ended` is true with no outcome behind it. The correction is the ending as it stands ("The game is over: the player won, by checkmate; you lost." / "…the player resigned, so you won." / "The game ended in a draw, by repetition; nobody won."), so the rewrite path is unchanged and the narrator says it again the right way up. Decided and recorded: the typed narrator brief stays with #289 (these two fields are what it would carry), the 2026-09-10 advice rule stands (a no-tool move is Glitch's opinion), and the board-trail evidence policy (#263) stands (an ending is a scalar). **Measured before it shipped**, on the terms the old note set: the deployed trace's 9 finished-game turns (4 player checkmates on three routes, 5 confirmed resignations) re-judged with the new facts fire 0/9, and `test_honesty.py` holds the labeled corpus — 13 must-fire lines of the wrong winner or termination and 27 must-not-fire lines (those 9 commentaries, true reports in the class's own words, post-mortem hedges, clarification questions, trash talk it has no words for) — with the deferral pinned (`"I win."` on a live board is `("ending",)` alone). Trace gains `outcome` (`{"winner", "termination"}` from the player's side, or None), set by `_trace_turn` at record time like `fen_after`, because the FEN says neither who the player was nor whether anyone resigned and the sweep had to assume white. New live scenario `checkmate_reaction_survives_guard` ("queen takes f7" delivering Scholar's mate on the fast path, the one shape where a wrong winner would reach the player with no app line beside it) prices the misfire the way `advice_capture_survives_guard` does. 2107 backend tests (`test_honesty.py` +52, `test_command.py` +4 through the real pipeline, `test_trace.py` +5, `test_api.py` +5). Gate 49 passed, 1 failed, 11 m 48 s, infra 0: every pass-rate scenario 5/5 ABOVE_FLOOR STABLE, the new scenario 5/5 with no sample guarded, `long_capture` 5/5 ×3, costs unmoved; the red is `judgment_question`'s 30 s latency ceiling on a correct trajectory (a 30.2 s narrator thought, the known tail), 16.2 s re-run alone on the same tree

## 2026-09-17

- [x] The planner samples at 0.3; the knight ask is asked, not played (the knight-ask campaign, #286; astra audit F6) — "move my kings knight" on a fresh board names one piece and two squares, and the planner played one of them instead of asking about a quarter of the time on a good day and 40% on a bad one (`ambiguous_knight_then_selection` 17/20 on 2026-09-05, 13/20 vs 11/20 interleaved on 2026-09-10, 2/5 in that day's gate). The campaign measured before it touched anything, on committed tooling (#299: `backend/scripts/probe_planner.py` makes the shipped planner call directly with arms round-robin per sample, `eval_campaign.sh` runs the harness in alternating blocks between two trees or two environments on one server). **Re-baseline:** unchanged `main` read 30/40 asked on a freshly unloaded llama-server and 29/40 warm on the same session, so the serving-state hypothesis the 2026-09-10 day left (slot-KV prefix reuse on a long-lived server) is not the lever, and the lag-1 agreement sat at the binomial expectation. **Screen** (40 an arm, interleaved): control 34/40; arm A `cache_prompt: false` 30/40 (dead); arm B planner temperature 0.3 **40/40**; arm C's deletion-only trims 23/40 (the planner bullet's `("grab that pawn")` clause — a deletion that made it worse) and 35/40 (`make_move`'s worked example — nothing); dose 1.0 → 34/40, 0.6 → 37/40, 0.3 → 40/40. **Neighbours** at 0.3 vs 1.0, 20 each: both refusals, the rook ask, the STT knight, one-side castling, undo-then-replace's first call (`undo`, `plies` omitted) and the held-out two-bishop ask all 20/20 → 20/20: a parse wants the mode, and cooling it made no wrong parse consistent. Shipped as `llama_brain._PLANNER_TEMPERATURE = 0.3`, applied by `create_llama_brain` (a bare `LlamaBrain(...)` still leaves the provider's default, so the policy has one home); `build_app` defaults to it and `CHESSAPP_PLANNER_TEMPERATURE` overrides it; the narrator keeps the model profile's 1.0. The eval harness resolves its temperature through `app._planner_temperature_from_env` and `test_eval_harness.py` pins that the gate samples the planner exactly as the app does, unset and set; the probe's `control` arm is the shipped planner. Harness confirm (four alternating blocks of five, one server, same tree, env arm): knight 18/20 → 19/20, `undo_twice_and_replace` 16/20 → **20/20** (the `plies` misread #267 left closes with it), everything else 20/20 both, `plain_move` 3 calls both. Gate 49 passed, 12 m 20 s, infra 0, every pass-rate scenario ABOVE_FLOOR STABLE (`save_then_new_game` 4/5, all else 5/5), `long_capture` 5/5 ×3, costs unmoved, `judgment_question` 14.0 s (narrator tail, thinking on). Arm D (a structured `ask_which_move` handoff) was not needed to reach the target and did not ship; arm E (another model) is #298. One finding recorded and not acted on: "castle" with both castlings legal is played as O-O 20/20 at both temperatures (`castle_both`, held out), a design decision, not a miss of this campaign. `docs/knight-ask-campaign.md` holds the full record; `docs/planner-narrator.md` and `docs/agent-evals.md` "Standing results" state the two temperatures. 2041 backend tests

- [x] A slow reaction no longer holds the engine's reply or the mutation lock (astra audit F3, #283) — the observe beat was optional only in the sense that it could be *skipped*: `_play_move` called `brain.narrate` and collected the reply afterwards, so a narrator that merely took its time held an answer Stockfish had already computed, kept the turn in `agent_observing`, and — the whole command runs under `ctx.mutation_lock` — parked every other road onto the board (a drag, an undo, a delegate move) behind it, bounded only by `max_tokens` (which bounds generation, not queueing or a stalled server) and the provider's 300 s read timeout. Two layers, answering different questions. The pipeline owns the turn: every `Brain.narrate` call goes through one `api._narrate` seam under `_REACTION_BUDGET_S` (`_within_budget` runs it on its own thread and raises `LateReaction`), so the three narration sites — the observe beat on the fast path and on a drag, the confirmed destructive op, the resignation — are budgeted by a rule rather than by a fix at the one site that showed the bug. `LateReaction` subclasses `ProviderError` on purpose (the `TurnStateError`/`ValueError` trick): every branch that already treats the words as the one thing a failure may cost handles lateness with no new shape to learn, while the distinct type keeps the record honest — deliberately *not* a `ProviderFailure`, whose vocabulary answers "would asking again work" and which would be a claim about the server rather than the app's judgment about this beat. The late call is simply dropped, the same shape as an abandoned `_PendingReply` and safe for the same reason (the narrator holds a board view snapshotted before the call and answers with words, so a late thread has nothing to land); voice-first, words spoken a beat behind the board are worse than none. The brain owns the socket: `llama_brain._NARRATE_TIMEOUT` (15 s, just above the budget so the two cannot race) rides down a new per-request `ChatProvider.chat(timeout=...)` on `narrate` alone, so the abandoned generation stops holding a llama-server slot the next turn's planner would queue behind — no new `ProviderFailure` either, since a read timeout is already what `UNREACHABLE` names. The budget is **measured, not derived from the token cap**: 58 observe beats in the deployed trace (`fast_path`/`board`, one thinking-off call each) took 0.7–2.1 s, median ~1.5 s, with a single 7.5 s outlier, so 10 s clears every healthy reaction with room for the shared GPU having a bad minute; a llama-swap reload with a warm page cache (~4.5 s) still narrates inside it and only a truly cold load (~100 s) loses one reaction to the app's own line, against a board that froze for 100 s before. The planner loop, its closing narration and the guard's rewrite are deliberately unbounded — the planner is not optional and its results are the record of what ran, and the other two hold no computed reply. Trace gains `reaction_late`: a cut beat's commentary is indistinguishable from verbosity=low, from a dead provider and from a beat that never opened, so without it the one number worth tuning is invisible. 2009 backend tests (`test_reaction_budget.py` +13 on a narrator double that blocks on an event — gameplay proceeds, the lock is free, the late words land nowhere, a reaction inside the budget is spoken exactly as ever; `test_provider.py` +2, `test_llama_brain.py` +3). `docs/turn-coordinator.md` and `docs/planner-narrator.md` updated. Gate 49 passed, infra 0, every pass-rate scenario ABOVE_FLOOR STABLE, `long_capture` 5/5 ×3, costs unmoved, `judgment_question` 9.1 s — and the number that matters for this change, `fast_path_normal`'s narrator call at 1.2 s against a 10 s budget, the same band the trace was sized from

- [x] The planner is told when its own tools move the board (astra audit F2, #282) — the loop was handed the board once, in its opening user message, and from there only appended (the assistant turn and one `role: "tool"` message per call, so the KV prefix holds), so inside one command the planner's second decision was made against the first's `legal_moves`: "undo that and play e4 instead" asked it to submit a move its own list could not contain, while its contract says a move no entry fits is illegal and the answer is to say so. No tool result could close the gap — a mutation reports `fen`/`turn`/`engine_move` and never the menu, and must not, because the same results are what the narrator speaks from (the reason `save_game` answers with a bare `board_version`). So `api.planner_board_refresh(ctx, coordinator)` is asked once per planner iteration through a new `LlamaBrain.board_refresh` seam, and when `board_version` says the board is a different one the planner's own block is appended again as one `user` message labelled "Board state after those tool calls:" (`_board_refresh_message`; a `tool` message would need a `tool_call_id` a batch of N calls has no honest id for, and reusing the last one breaks the one-answer-per-call shape `test_llama_brain.py` pins). Three properties are load-bearing: it is a message and not a result, so `run.tool_results` is untouched and neither the narrator's brief nor `_exchange_key`'s stall rule can see it; it is keyed on the board, because a setting or a save that moved without the position moving is already reported by the tool that moved it; and it is **withheld entirely while a reply is owed** (`PLAYER_MOVE_APPLIED`/`AGENT_OBSERVING`, the pipeline's own predicate), because `make_move` applies the player's move and stops and the board it leaves has the engine to move and the engine's menu — #193 one layer up. Per-iteration, not per-call, and said so: a model emitting `[undo, make_move e4]` in one batch still decides blind. No prompt change, no schema change, no tool-offer change, no `Brain` protocol change; `board_refresh` defaults to None so MCP and `/api/game/hint` are untouched, and `test_eval_harness.py` pins that the gate's harness and `build_app` wire the same one. Trace gains `state_refreshes` (the board versions the planner was re-shown, beside `mutations`). **The first cut failed the gate and the payload is why**: sending the whole opening view cost `undo_twice_and_replace` 4/10 BELOW_FLOOR, every miss one takeback short, 4/20 against unchanged main's 19/20 in interleaved blocks of five on one server — a `history` the bishop move has just left reads to a 12B as the takebacks being done, so a block meant to say what may be played was answering what had been finished. Trimmed to the menu and what qualifies it (`_REFRESH_KEYS`), the same screen read 19/20 against 16/20. A scenario built to catch the bug (`undo_then_capture`, a capture that exists only after the takeback) read 20/20 on unfixed main and was **not** added — the board is the referee, so the planner ignores the stale list and the move lands anyway; #282 is pinned where the evidence is, on the prompt the planner was handed. Live wire checked against llama-server before the GPU spend (a `user` message after a batch of tool messages: `completed`, no provider failure, `state_refreshes: [3]`, and the model played an `e4` its opening list did not contain). `docs/planner-narrator.md` gains the section; 1991 backend tests (`test_llama_brain.py` +11, `test_app.py` +7, `test_api.py` +4, `test_trace.py` +3, `test_eval_harness.py` +1, `ScriptedProvider.rescript`). Gate 49 passed, infra 0, every pass-rate scenario ABOVE_FLOOR STABLE, `long_capture` 5/5 ×3, `judgment_question` 8.1 s, costs unmoved; the refresh costs ~180 prompt tokens on the planner call after a mutation and nothing elsewhere

## 2026-09-16

- [x] A pending confirmation is answered only where it was asked (astra audit F1, #281) — an armed destructive op was bound to the board version alone, so after "I resign" in delegate conversation A a "yes" posted to conversation B, or typed into the web panel, ended the game (nothing has to move between the question and a wrong-thread answer, so `live_pending`'s staleness check could not see it). `PendingOp` carries an `origin` the gate stamps off `ToolContext.origin`, which each surface declares on its way in under the mutation lock: `panel` (free text, the three board buttons and `/api/game/confirm` are one origin on purpose, so a button's question is still answered by a typed yes and the reverse), `delegate:<conversation id>` per thread, `mcp` for the standalone server. `live_pending(origin)` and `confirm_pending(registry, ctx, origin)` read both halves, origin first so a foreign read drops nothing. A foreign answer is a new command: every command from any origin still disarms on its way in, so the words never reach the free-text reader and travel on as an utterance, and the origin that was asked must ask again (the alternative, a foreign op left armed, would make `_gate` refuse a conversation that never asked and let `restamp_pending` re-point the question at a board it was never about); a confirm click with a delegate's question standing is the plain 409 and leaves it alone. `RunCommand` is a `Protocol` (keyword-only `origin`), the MCP wrapper declares `mcp` inside its lock, and `turn_record` gains `origin`. Two eval scenarios that encoded the bug (`destructive_confirm`, `save_then_new_game` answered a delegate's question from the panel) now answer in the same conversation and were run live on the branch: pass, and 5/5 ABOVE_FLOOR STABLE, infra 0. `docs/turn-coordinator.md` armed-op bullet rewritten; one bullet in `docs/mcp-confirmation-surface.md`. `tests/test_confirmation_origin.py` (11), registry, MCP, trace and delegate tests; 1965 backend tests. No prompt, schema or loop change, no gate (#294)
- [x] An engine failure after a committed move is a structured outcome, not a 500, and the turn is traced (astra audit F4, #284) — Stockfish dying on the collect left the player's move committed and broadcast while the request failed with a bare 500 and the interaction wrote no trace. Both collect sites (`_play_move`'s close beat, `_command_turn`'s convergence) catch it: the turn is left open where the coordinator put it (`player_move_applied`, the reply still owed and settled by the next command), the failure travels as `engine_failure` (class and message, the `provider_failure` analogue) on `CommandOutcome` and in the trace, `stop_reason` stays the loop's own so the delegate wire is unchanged, and the player hears the app's own line `ENGINE_LOST_REPLY_OWED` composed where the reply announcement would have stood, never fed back as Glitch's. `/api/command` and the delegate route answer 200 with the committed results; drags in both modes stand (direct mode gains `commentary` on that one turn, and its mid-turn 409 now settles an owed reply first, the healing agent mode always had). The trace is written from a `finally`-owned envelope, so any exception escaping a turn leaves exactly one record with an `error` field. `docs/turn-coordinator.md` known edges updated. `tests/test_engine_failure.py` (13), `test_trace.py` +6, `DyingEngine` in fakes; 1942 backend tests. No prompt or loop change, no gate (#293)
- [x] The panel reads and records its conversation memory under the mutation lock (astra audit F5, #285) — `/api/command` snapshotted memory before `_run_command` took the lock and recorded after it released, so two overlapping panel commands both read before either ran and the second reasoned from a transcript missing the exchange that finished before it started (the board was never at risk; the delegate route and the drag path already serialized read → run → record). The endpoint opens `_mutation` itself and does the three inside it; no second lock, lock order "mutation lock only", 503 and the stale 409 unchanged. Three event-sequenced concurrency tests in `test_command.py` (a `BlockingBrain` and a `QueueingLock` that signals when the second request parks); the acceptance test fails 10/10 on the old endpoint. 1926 backend tests. No prompt or loop change, no gate (#292)

## 2026-09-10

- [x] The honesty guard's cut is a rewrite, not a canned line; an unasked move is Glitch's opinion (decided 2026-09-10 after the guard audit: 6 of 150 deployed turns guarded, 0 of them a real lie, 4 false positives, 2 advice-policy fires) — a claim the board does not back goes back to the narrator with the true fact in plain words (`Brain.rewrite` on the persona prompt, no tools, thinking off; `honesty.corrections`, one "You wrote: … <fact>" line per unbacked claim, addressed to Glitch and never a scolding), the rewrite is checked the same way, and only a second draft that still asserts something unbacked is cut to what the app already says with no usable model text (the move confirmation on a move turn, the stuck line otherwise) — never a third try and never an apology for a sentence the player did not hear, since the guard runs before anything is spoken. `UNTRUE_CLAIM_REPLY`, `UNVERIFIED_CLAIM_REPLY` and `MOVE_ADVICE_REPLY` are gone. A spoken rewrite is remembered as Glitch's; a cut falls to the facts as before. The advice guard is inverted: it fires only when the turn consulted the engine (`get_best_moves` / `analyze_last_move` named moves) and the reply hands over a playable move the engine did not, and a refused `make_move`'s `alternatives` and `get_legal_moves` are licensed as the tool's own reports — the two live fires it reproduces are the London answer "Just play Bf4" (2026-09-06, no tools, cut) and a refusal's own alternative list read back (2026-09-04, cut); with no analysis in the turn a move is his opinion. Two near-miss hedges (`close`, `looking`) for "checkmate's looking real close for me", cut live 2026-09-06 as the ending it threatened. Trace: `rewrite` (`spoken`/`cut`/`lost`), `rewrite_claims`, `rewrite_suppressed`; `guarded` still reads the first draft so the eval floor keeps measuring the model. `evalstats` attributes a rewrite as narrator time; the UI labels the `rewriting` phase. 1923 backend tests (the guard sections of `test_command.py`, `test_closing_pass.py`, `test_trace.py` rewritten around the second draft; `corrections` specced class by class in `test_honesty.py`; the rewrite brief in `test_llama_brain.py`). Gate 48 passed, 1 failed, 13 m 08 s, infra 0: every pass-rate scenario 5/5 ABOVE_FLOOR STABLE but `ambiguous_knight_then_selection` 2/5 (every miss the planner playing a knight at 3 calls — no rewrite ran, and nothing the planner sees changed); re-measured interleaved on one server, four blocks of five a tree: this tree 13/20 (3, 3, 4, 3) against unchanged main 11/20 (2, 4, 3, 2), so the day's rate on that ask is ~60% on both trees and is not this change's (TODO). `long_capture` 5/5 ×3, `judgment_question` 11.0 s, costs unmoved (`fast_path_low` 0, `fast_path_normal` 1, `plain_move` 3, `resign_literal_fast_path` 0)

## 2026-09-06

- [x] Draw offers, the button's offer path (Part B frontend; TODO "Claim-draw button" completed by #274's other half) — the Draw control offers when no rules claim exists: `api.offerDraw` posts the version to `/api/game/offer-draw`, `useGame.offerDraw` applies the returned state and shows the rule's answer as one deterministic line composed from `accepted` and `reason` alone (`src/draw.ts`) — no model on this path, like the resign and new-game buttons, and a reason this build has never heard of still declines truthfully rather than going silent. The game ends only on an acceptance; a decline leaves the board where it was; a stale 409 catches up without a line; a refusal (game over, no engine) moves nothing and says nothing. `App` picks the gated claim or the offer off `claimable_draws`, and the button is disabled only once the game is over. Tests: `draw.test.ts` (the wording never claims a draw on a decline), `useGame.test.tsx` (version cited, declined, accepted, stale, refused), `App.test.tsx` (the click posts and the line appears); `progress.ts` gains the `offer_draw` label. Frontend lint, 301 tests, build green (#277)
- [x] Draw offers, backend (Part B; `docs/draw-offer.md`) — the player offers, code decides for the engine: `GameSession.agree_draw()` (termination `agreement`, `1/2-1/2`, honoured by `is_game_over`, a refused `undo`, `draw_agreed` in saves, the PGN result) and `material_profile()`; the rule in `chessapp/draw_offer.py` — accept iff the player has moved, the engine-POV Stockfish eval is within `DRAW_OFFER_BAND_CP = 50` (mates included via `pov_cp`) and the position is an endgame by material (no queens, `ENDGAME_MAX_NON_PAWN_MATERIAL = 8` per side), else the first failing reason of `too_early` / `engine_ahead` / `player_ahead` / `not_an_endgame`, never the model, personality or tier; the `offer_draw` tool offered on every live turn, not gated but holding the destructive budget (checked before evaluating, spent and the turn abandoned only on acceptance, so a declined offer beside a move still gets the owed reply announced); `POST /api/game/offer-draw` for the button; `_analysis_numbers` learns the verdict's number both ways so an honest quote survives the guard. Schema snapshot regenerated (+`offer_draw`), golden and MCP count updated, BRIEF tool list gains it. 55 tests in `tests/test_draw_offer.py` incl. four pinned fixtures also judged under real Stockfish; two eval scenarios (`offer_draw_routes`, `offer_draw_accepted`) and the gate re-recorded (#276)
- [x] Draw offers, designed (Part B design note) — `docs/draw-offer.md`: the acceptance rule and its two constants, why the model does not decide (understanding vs verdict; personality is tone; the tier is not a factor), the gating decision (not gated, budgeted), the coordinator interaction for an offer beside a move, the endpoint and button shape, the eval plan (#275)
- [x] Claim-draw button (#220 follow-up; TODO "Claim-draw button") — `POST /api/game/claim-draw` mirrors `/api/game/resign`: `_mutation(version)` around `_run_destructive("claim_draw", {})`, so the button asks the gate's own question mid-game (409 + `CONFIRM_QUESTIONS["claim_draw"]`, answered at `/api/game/confirm` from either surface), runs outright where the gate stands aside, and answers a plain 409 with nothing armed when the rules allow no claim (the tool's check, ahead of the gate). The frontend now reads the `claimable_draws` the state document has carried since #220: one Draw button in the bottom bar (Options / Resign / Draw / Hint / Undo) that reads "Claim draw" and dispatches the gated claim while a claim exists, otherwise "Offer draw", disabled until the `offer_draw` path lands (Part B, `docs/draw-offer.md`), and disabled once the game is over. Tests mirror resign's on both sides (api, board controls, board version incl. the stale-version parametrize, trace; BottomBar, useGame, App). No schema change, no prompt change, no gate. (#274)

## 2026-09-05

- [x] MCP confirmation surface, run end to end (audit finding 3; TODO "Run the MCP confirmation end to end once") — from a fresh Claude Code 2.1.261 session with only `chessapp.mcp_server` attached (`--mcp-config`, `--strict-mcp-config`, stdio tee'd both ways): the handshake declared a bare `elicitation: {}`, which the server reads as form mode; `make_move e4` came back with Stockfish's reply; the gated `new_game` sent one form-mode `elicitation/create` carrying `CONFIRM_QUESTIONS["new_game"]` verbatim, and Claude Code rendered it as *MCP server "chess" requests your input* with the Confirm box, *Yes, do it* and Accept/Decline while the model's turn waited; ticking and accepting sent `{"action":"accept","content":{"confirm":true}}` and the result was the starting FEN with `confirmed: true`, narrated as a new game; on a second game Decline sent `{"action":"decline"}`, the result was the refusal with `declined: true` and `retry: never`, and `get_board_state` showed the game untouched. The client's model saw only tool results and, after the no, asked the human in chat — which can only lead to another call and another form, as designed. Wire excerpts recorded in `docs/mcp-confirmation-surface.md` ("Verified live"); `docs/turn-coordinator.md`'s limits paragraph, which still said the surface waited on the note, brought current. No code change. (#273)
- [x] MCP confirmation surface, implemented (audit finding 3; TODO "MCP confirmation surface — implement") — the note's sequence, in `mcp_server._mcp_tool`'s call wrapper and nowhere else: a dispatch that comes back with a *newly* armed op releases the lock and puts the app's own question (`tools.CONFIRM_QUESTIONS[op]`, moved out of `api.py` so the board dialog, the free-text reader and MCP read one table) to the client's human by MCP form-mode elicitation, one boolean field where only `accept` with `confirm: true` is a yes; the yes re-takes the lock and calls `confirm_pending`, the existing sole key unchanged, so a board that moved while the form was open runs nothing (`stale: true`) and so does a question a later gated call superseded (MCP is windowless, the newest question stands — the #270 rule seen from this surface, pinned by identity on the armed op rather than by name); a no, a dismissed form, an unticked accept or a client that errored on the ask leaves nothing armed and says `declined: true`; a client whose handshake declared no form elicitation (a bare `elicitation: {}` counts as form, `url` alone does not) gets the refusal with `confirmation_unavailable: true` and its text corrected, nothing armed. The client's model neither emits the request nor answers it, and the advertised schema is byte-identical. Eleven tool-boundary scenarios through the SDK's in-memory client with an `elicitation_callback`, one per acceptance criterion, including a second client reading the board while the form is open and the capability check on every handshake shape; two `test_board_version.py` tests that called the wrapper as a plain function now run the coroutine on a loop; `docs/turn-coordinator.md` gains MCP as the third answering surface. No prompt, model or loop change, so no gate; backend 1814 passed, 47 skipped, lint clean. Claude Code renders MCP elicitation forms with Accept/Decline (its changelog, 2.1.239; the local CLI is 2.1.261); one end-to-end run with our question in that form is left in TODO (#272)
- [x] MCP confirmation surface, designed (audit finding 3; TODO "Later — MCP confirmation surface") — a design note, no code: `docs/mcp-confirmation-surface.md`. Standalone MCP arms the gate and nothing there can answer it, so `new_game`/`resign`/`claim_draw` are unreachable on a game in progress and the refusal's "runs when they say yes" is untrue on that surface. Four options rejected in writing — a `confirm` tool, "call it again to confirm", a gate-off flag for MCP, and making MCP a client of the live game (a different feature, recorded as such) — and one chosen: MCP form-mode elicitation, a server-initiated question the *client* puts to its human and answers with accept/decline/cancel, which the model neither emits nor answers. The wrapper in `mcp_server` dispatches under the lock as today, releases it, elicits the app's own `_CONFIRM_QUESTIONS[op]` (one boolean field), and on a yes calls `confirm_pending` — the existing sole key, unchanged, staleness check included — while decline, cancel and a client without the capability all leave nothing armed and say so in the result. The schema does not change (the byte snapshot and the MCP equivalence test are the guard), the trust boundary is stated (the server trusts the client's declared capability the way a web server trusts the browser's dialog), and the note closes with the acceptance criteria the implementation PR is written against, all at the tool boundary with no GPU (#271)
- [x] Pending-op policy: one question per command (audit §"Confirmation details"; TODO "Later — pending-op policy") — two refusals in one batch armed two ops and the last replaced the first, while the narrated question could be about the first, so the player's next yes ran an op they had not been asked about. Decided and pinned: the first gated call in a command arms its op and its question, and every later gated call in the same command window — the same op again or a different one — is refused with a result naming the question already pending (`pending: <op>`, `retry: never`) and arms nothing, so the narrated question, the question the reader is handed and the op `confirm_pending` runs are one op by construction. Scoped to the command window (`TurnCoordinator.command_open`), because that is the only place two gated calls meet inside one interaction; across interactions the newest question is still the one a yes answers, and a button press or an MCP call stays one dispatch per interaction. Registry tests for the rule, the same-op re-ask, a mutation between the two calls, the windowless surfaces and the window closing; pipeline tests through a real planner batch — the yes runs the op that was asked about, and a free-text answer is read against that op's question. `_BASE` left as is, and said so: the sentence telling the tool-free narrator it changes the game only through its tools is stale role text but harmless to a phase offered no tools, its neighbours in the same bullet (never decide legality, never track the position) still bind the narrator, and cutting it would be a narrator prompt change with no measured symptom to gate against. Gate 47 passed, 11 m 50 s, infra 0 — the day's first clean 47: `undo_twice_and_replace` 4/5 (the residual `plies=1` misread) and `pgn_is_handed_over_not_recited` 4/5 (one recited header block), everything else 5/5, `long_capture` 5/5 ×3, costs unmoved (#270)
- [x] An ambiguous piece ask is asked, not played (TODO "Later — an ambiguous piece ask is sometimes played, not asked") — "move my kings knight" on a fresh board, Nf3 and Nh3 both legal, was played (Nf3, every time) rather than asked about half the time, while "move the rook" with four rook moves was asked. The lever was the planner's one/several/none procedure and the fix is a deletion: its bullet carried a second copy of the `captures` fact between "match the words against `legal_moves`" and "exactly one entry fits: submit it", the bullet above states it once, so the copy is gone and nothing was added. Nothing added is the finding — every arm that put a fact in made the knight ask worse in the same interleaved screen (define "fits" as piece-and-square 8/20 vs 5/20 old; "which of them to play is the player's decision" 13/20; both 18/20; then, forty a sample round-robin on one server, old 26/40 played against the deletion's 3/40, reorder-ask-first 12/40, deletion plus a one-line fact 5/40) — and the screening found that consecutive samples of one prompt on one server are correlated (the old text alone read 5/20, 19/40 and 31/40 in three batches), so arms are compared only interleaved. Harness, alternating blocks of five on one server, 20 an arm: `ambiguous_knight_then_selection` 8/20 → 17/20 (old's twelve misses all a played knight; the deletion's three: two played knights and one correct four-move clarification phrased as a statement, which the guard's decided rule reads as advice), `ambiguous_move` 19/20 → 20/20, `undo_and_replace` 18/20 → 20/20; the knight scenario clears the floor at 20, so its xfail is off. The bullet's neighbouring asks screened clean on the deletion: "take the pawn" and "bishop to a1" refused not asked 30/30, rook asked 20/20, STT knight played 20/20, castle 20/20, undo's first call `undo` with plies omitted 19/20. Gate 46 passed, 1 failed, 12 m 32 s, infra 0: every pass-rate scenario 5/5 with `ambiguous_knight_then_selection` 5/5 unmarked, `long_capture` 5/5 ×3, costs unmoved, the failure `judgment_question`'s 30 s latency ceiling on a correct trajectory (a 32.9 s narrator thought), re-run five times a tree interleaved at 5/5 and 5/5; the same-day control on the unchanged main 46 passed, 1 xpassed, 11 m 51 s, infra 0 (#269)
- [x] Exact emitted-schema snapshot (audit §"Result shapes, honesty limits, and prompts"; TODO "Later — schema snapshot") — the migration golden normalizes away `title`, `default` and the `anyOf`-null union, the three keys whose stripping each collapsed `undo_and_replace` on gemma-4-12b (2026-07-21), so it could not see any of them go. `tests/tool_definitions_emitted.json` is now the exact output of `definitions()`, both `make_move` flavours (the MCP server's atomic exchange and the brain's split one), held byte for byte — key order, whitespace, em-dashes — by `test_emitted_schemas_match_the_snapshot_byte_for_byte`; a deliberate change regenerates it with `CHESSAPP_UPDATE_SCHEMA_SNAPSHOT=1` and reads as a schema change that runs the eval gate, and the failure names the first line that moved. Three positive tests state what the normalizations hide, on the live registry so a regenerated fixture cannot lose them quietly: every derived argument model titled (`set_difficulty`, the hand-written `oneOf`, pinned as the one exception), every optional typed parameter `anyOf: [<type>, null]` with `default: null`, every handler default carried. No schema change, no prompt change, no GPU (#268)
- [x] A named move is a whole takeback, not a plies count (the two-undo reading miss found measuring #260) — "undo the bishop move and undo the knight move, then play d4" reached `undo(plies=2)`, one exchange for two named moves. The lever is `undo`'s description, measured at 20 samples an arm on one base: the old text 19/20 on `undo_and_replace` and 9/20 on `undo_twice_and_replace` (eleven `undo(plies=2)`); an arm that taught the arithmetic ("the player's move and the engine's reply are two") 19/20 via `plies=2` and 4/20 — a number in the text is copied into the argument; an arm with no numbers that said "never a count, once per named move" 14/20 (six `plies=1`) and 15/20; and the shipped text — the old wording minus its two-item enumeration of what the default pops, plus one sentence, call this again for each further named move with plies omitted every time — 20/20 and 18/20, then 20/20 and 16/20 against the old text's 19/20 and 7/20 in alternating blocks on one server (34/40 against 16/40 over both campaigns). The xfail on `undo_twice_and_replace` comes off; the golden changes in the two description strings only and no schema key is touched. Gate 46 passed, 1 xfailed, 11 m 06 s, infra 0: `undo_twice_and_replace` 4/5 unmarked, `undo_and_replace` 5/5, every other pass-rate scenario 5/5 but the knight-ask xfail at 9/15, `long_capture` 5/5 ×3, costs unmoved (#267)
- [x] Eval coverage: the twelve composition and loop-path scenarios (audit §"Proposed live scenarios"; decided: completeness outranks GPU time) — `undo_twice_and_replace` strengthened (exactly four plies, player to move, nothing armed, completed, 3–5 calls); `resign_never_pretends` strengthened into the audit's `resign_intent_reaches_planner` (the gate's refusal is the contract and the armed op carries the player's color); new `ambiguous_knight_then_selection`, `move_save_resume_finishes_exchange`, `save_then_new_game`, `voice_setting_and_move`, `move_and_judgment`, `resume_and_describe`, `best_move_then_play`, `freeform_confirmation_answers` (cancel / confirm / unrelated), `late_game_tool_composition` on the 84-ply fixture (seeded and control) and `stt_knight_repair` (two phrasings), every one pinning route, stop reason, call count, board end-state and a settings snapshot; `_run_steps`, a multi-step panel runner that snapshots earlier steps for the check and retries a dead step as infra; `evalstats` marks a confirmation turn with more than one call UNKNOWN rather than all-narrator; 33 → 47 eval items. Measured on the same harness before and after PR 3 and PR 4: `move_save_resume_finishes_exchange` reproduced finding 2 exactly (0/5, "the restored position owes an engine reply nobody collected") and passes 5/5 on the settled restore; `ambiguous_knight_then_selection` did not do what the audit predicted — the guard was the smaller of its two failure modes, and the planner plays one of the two knights instead of asking about half the time (26 of 50 samples across eight runs, on both sides of the guard fix), so it ships as a measured-miss xfail with the planner's ambiguity procedure filed as the lever; the other nine compositions came in 5/5 on both builds. Post-fix gate 45 passed, 1 failed (that scenario, xfailed in the same PR), 1 xfailed, 11 m 29 s, infra 0; `long_capture` 5/5 ×3, costs unmoved (#265)
- [x] A clarifying question survives the guard, and every board a batch held counts (audit findings 6, 7; the clarification rule decided) — a question sentence naming two or more legal moves is a clarification, not advice (`honesty.unlicensed_advice` replaces `names_a_legal_move`: one named move is still advice however it is punctuated, a statement naming two still hands both over, and every other sentence in the reply is judged on its own), so "Do you mean Nf3 or Nh3?" reaches the player instead of the correction. The brain route's honesty evidence is every position the command held: the command window keeps the board each mutating dispatch left behind, taken off the registry's mutation hook before the state broadcast, and `_verified_facts` checks all of them, so "you are up a pawn" narrated between `exd5` and the engine's `Qxd5` survives while "up a rook" and a move legal on no board the batch held are still guarded; drags, buttons and MCP open no window and record nothing. The ending class deliberately still does not tell winner from termination (the note above `_CLAIM_CLASSES` says what evidence would change that). Seven pipeline tests through the real planner batch plus the predicate's spec. Gate 31 passed, 1 xfailed, 1 xpassed, 6 m 08 s, infra 0: every pass-rate scenario 5/5 but `ambiguous_move` 4/5 (XPASS) and the two-undo xfail 2/5, `long_capture` 5/5 ×3, costs unmoved. `ambiguous_move` re-measured at 20 samples on the fix: 19/20, the one miss a guessed `Rh3` and none eaten by the guard, so the xfail it carried since the morning comes off here (#263)
- [x] A restored position with the engine to move is settled by the coordinator (audit finding 2, decided) — "play e4 and save this" saves between the player's move and the reply, and loading that save, or taking back an odd number of plies, left the engine to move with nobody scheduled to move it. `TurnCoordinator.settle_engine_turn()` replaces `engine_opening_move()`: from `awaiting_player`, with an engine, a live game and the side to move not the player's, it plays the engine's move and comes back awaiting the player without consuming a turn; `undo`, `resume_game` and `new_game` call it after their mutation and report the move as `engine_move` in both registry flavours, `/api/game/undo` does the same (and is attempt-first now, like the tool), and the pipeline announces a settled move with the one deterministic line every engine move gets, so the low-verbosity new-game line no longer spells the opening move itself. An engine that raises inside `collect_engine_reply` leaves the phase at `player_move_applied` instead of `engine_calculating`, so the next command heals the turn (refused move, owed reply played) as `docs/turn-coordinator.md` claimed and now does. Coordinator, tool, route, MCP and pipeline tests, including the audit's probe end to end. PR 5's `move_save_resume_finishes_exchange` reproduced the miss 0/5 on the pre-fix main. Gate 31 passed, 1 xfailed, 1 xpassed, 6 m 37 s, infra 0: `undo_and_replace` 16/20 (every miss the `undo(plies=1)` misread, which the settle now turns into a legal replacement on the wrong board rather than an illegal one), every other pass-rate scenario 5/5, `long_capture` 5/5 ×3, costs unmoved (#266)
- [x] A refusal leaves the turn alone, and the loop's refusals say how to recover (audit findings 1, 4, 5, 8) — `undo` attempts the takeback before abandoning the open turn, so `make_move(e4), undo(plies=100)` in one batch keeps the engine reply e4 is owed instead of leaving Black to move with nobody to move; `read_answer` treats a verdict cut off by the cap as `unrelated` and still bills the round trip; `dispatch` narrows integral JSON floats to the integer their schema names and turns a handler `TypeError` into a `different_args` refusal, so `undo(plies=1.0)` runs and nothing escapes the tool boundary as a 500 on a board that already moved; `save_game` carries `board_version`, so a second save of a changed board is progress to the results-keyed stall rule and the replacement move after it is reached; the loop's own schema refusals are built through the dispatcher's `refusal` (now on the `ToolDispatcher` protocol, `RETRY_*` in `brain.py`) and carry `retry` and `board_version` like every other no. Decided and pinned: a tool-bearing response cut off by the cap executes its complete calls and the loop goes on; a setting toggled on→off→on in three responses still ends the phase after the third lands. Scripted tests at the tool boundary for each, plus the mixed-batch precedence matrix over the five failure kinds the shipped registry can produce (every call id answered once in order, siblings land, only schema failures spend a correction, repeat vs schema error on the final iteration). Gate 31 passed, 2 xfailed, 5 m 48 s, infra 0: `undo_and_replace` 4/5, every other pass-rate scenario 5/5, `long_capture` 5/5 ×3, costs unmoved; the xfails `undo_twice_and_replace` 0/5 (all `undo(plies=2)`) and `ambiguous_move` 2/5, every miss a correct two-move question eaten by the advice guard and none a guessed move (#264)
- [x] The eval harness measures what it claims (audit findings 9, 10 and the weak pins) — every model-routed scenario now asserts in setup that both fast-path parsers stand aside (`_stays_a_model_eval`) and `_pass_rate` pins the traced route on every sample. The four resignation scenarios had been measuring the resign fast path: the same-day control gate on the unchanged harness shows all 20 of their samples at `route=resign` with 0 model calls. They now reach the planner on "please record a resignation for my side" (`resign_never_pretends` 5/5 at 3 calls, the gate arming the resignation every time; `long_resign` 5/5 ×3 at 3 calls, resigning outright because the FEN-rooted long board has no player plies for the gate to guard), and the literal keeps a zero-model hard scenario, `resign_literal_fast_path`. `_assert_reached_narrator` rejects the canned stuck line; `_expect_san` reads the board (one accepted move, nothing else moved, one engine reply, player to move) instead of the tool's report of itself; `_BOARD_TOOLS` derives the game-ending tools from `DESTRUCTIVE_TOOLS` and `_VERDICT_TOOLS` gains `review_game`; `ambiguous_move` and `destructive_confirm` reject a guard correction and pin the armed op; the two-undo xfail is filtered to `AssertionError`; 17 off-GPU harness tests pin the gates and the premises. The stricter harness paid for itself at once: `ambiguous_move`, hard and 1/1 in the control, came in 12/17 across the day (two guessed `Rh3`; two correct "Which one? Rh3 or Rh2?" eaten by the advice guard — finding 6, PR 4), so it ships sampled at the 0.8 floor and xfailed with the measurement. Gate 31 passed, 1 failed (that scenario, before the conversion), 1 xfailed, 6 m 28 s, infra 0; `undo_and_replace` 4/5, `undo_twice_and_replace` 4/10 (xfail), everything else 5/5, `long_capture` 5/5 ×3, costs unmoved (#262)

## 2026-09-04

- [x] A second `undo` in one turn is progress, not a stall (live 2026-07-30 and 2026-08-08) — "undo my knight move and undo the bishop move and then play my knight move" ended after the second undo with the move never played: the planner did the right thing, and the loop's `no_progress` rule cut it off because it keyed a repeat on the call alone, and `undo {}` is the same call every time while popping a different exchange every time. The rule now keys on the call *and its result* — an identical read answered identically still ends the phase, a repeat that comes back different goes on — pinned at the tool boundary on a real `GameSession`. New eval `undo_twice_and_replace` reproduces the trace (pre-fix 1/5, two of the misses the stall); post-fix the stall is gone from every sample, but the model reads two named moves as `undo(plies=2)` three times in four (5/20), so it ships as a non-strict xfail with the `plies` description filed in TODO. Full gate 31 passed, `undo_and_replace` 8/10, costs unmoved (#260)
- [x] A reply no longer moves the board (walkthrough leftover) — Glitch's bubble sits above the board in the column and grew with the reply: a one-line bubble in a 72px row (the spider's own height), every extra line another 25.2px. Measured live: 61px for a four-line reply on a 459px-wide desktop bubble, 112px for the walkthrough's six-line one, and 162px on a phone, where the same words wrap to seven or eight lines in a 258–323px bubble. The bubble is clamped to three lines now (line-height 1.3, a content-box `max-height`) and scrolls inside itself, with the offset put back to the first line for every new reply, and the row reserves exactly a full three-line bubble — 100.1px, +28.1px on the old 72px — so one line and a hundred measure the same and the board never moves. Three lines and not two because a move reply is a reaction, a blank line, then the move, and a two-line clamp would hide the move; the reserve stops at three because the phone has none to spare — on a 440×800 viewport the stack ends 5px above the fixed bar before the first move and 43px above it after. #251's per-interaction re-measure stays, belt-and-braces now rather than the fix: the Copy PGN chip under a reply still grows the row, and reserving for it would cost every turn of every game a row of dead space for something the player asked for once (#259)
- [x] Illegal drags stay silent, decided (walkthrough leftover) — no code change. Chessground puts a dot on every legal destination the moment a piece is picked up and snaps an illegal drop back without firing an event, which is lichess's own convention: the board has already said where the piece may go, and a message after the fact would be scolding the player for something they were shown. If it ever does confuse anyone the message is about fifteen lines away — `api.state.selected` for the piece and `getKeyAtDomPos` for the square it was dropped on (#259)
- [x] "Switch to Black" reads the same on screen as it does aloud (walkthrough leftover) — `.side-picker` and its button both carried `text-transform: capitalize`, which paints "Switch To White" while the accessible name stays "Switch to white" (a transform changes the paint, not the text) and title-cases the "To" into the bargain. The colour is capitalized in the DOM text now and both declarations are gone, so the label and the accessible name are one string; an App test pins the exact name (#259)
- [x] A PGN by chat comes with real headers and a copy button (walkthrough leftover) — "export the pgn" returned `[Event "?"] … [White "?"] [Black "?"]` and the narrator read the whole dump into the bubble, out loud with voice on, with nothing to copy it with. `GameSession` records the day the game began (`started`, additive in the save), `export_pgn` takes composed headers and still owns `Result`, and one composer (`tools.pgn_headers`) fills the Seven Tag Roster for both the tool and `GET /api/game/pgn` so the two can never disagree: the human is "Player" on whichever side they took, the opponent "Glitch (Stockfish, casual)" with the strength spelled the way it was set. The notation is app-owned text now — a copy chip and a collapsed "Show PGN" under the reply — so `export_pgn`'s description says the reply announces it is ready and recites nothing. New eval `pgn_is_handed_over_not_recited` 5/5, full gate 31 passed (#258)
- [x] A constraint the player states is binding (walkthrough leftover) — "go easy on me without changing the difficulty" set beginner anyway. Fixed on `set_difficulty`'s description, not with a guard: the guard checks claims against facts, and knowing a change was excluded means reading the constraint out of the utterance, which is the model's job. The description now carries two facts and no triggers — strength is this setting and nothing else, and a change the player rules out is not made. Fresh, the miss never reproduced (29/30 pre-fix); seeded with the walkthrough's own thread at verbosity low it did, 12/20 — and a first cut that also listed the asks meaning this call went 9/20 there, the trigger list outranking the caveat, while the facts alone went 20/20. New evals `constraint_rules_out_the_only_lever` (fresh, a lock) and `constraint_survives_a_live_thread` (the reproduction). Full gate 30 passed, all 5/5 (#257)
- [x] "what's the position?" gets a description, not a verdict (walkthrough leftover) — new read tool `describe_position` puts the board into one deterministic paragraph for the narrator, which is handed no FEN (#193) and so had nothing to describe from; `evaluate_position`'s description hands the ask over, and the advice guard licenses the one move a description names. Pre-fix 0/5, new eval `position_is_described` 5/5, full gate 28 passed (#256)
- [x] An impossible request is refused, not queried (walkthrough leftover) — "bishop to a1" came back "Which one?" and "take the pawn" "Which pawn?" because the planner contract left asking as the only option for words that fit no legal move; the matching rule is now a procedure (match first; one fits, submit; several, ask; none, refuse), and the per-turn view carries `captures` (what each capturing move takes), the one fact a capture asked for by its victim needs and SAN cannot give. Pre-fix 0/5 both; new evals `impossible_move_is_refused_not_asked` 5/5 and `impossible_capture_is_refused_not_asked` 5/5 — the capture case needed both halves: the rule alone 5/10, the fact alone 0/5 (#256)
- [x] A question turn may not move a setting the player owns — asserted in both new scenarios as a settings snapshot compared after the turn (#256)
- [x] A pending confirmation understands the player's own words (walkthrough #6) — the literal reader still goes first and free, and only an answer it cannot place goes to the model, which returns confirm/cancel/unrelated and never touches a destructive tool; "just do it" resigns, "forget it" doesn't, "undo instead" falls through. Full gate 25 passed (#255)
- [x] Mistake narration stops inventing the captured piece (walkthrough #5) — `analyze_last_move` now reports what the played and best moves each take, and the guard holds a capture claim to what the move it names takes rather than to the game-wide record; full gate 25 passed (#254)
- [x] Container stops in 0.4 s instead of 10 (walkthrough #4) — python-chess drives Stockfish from a non-daemon thread, so an unclosed engine outlived uvicorn and every `compose stop` ended in SIGKILL; `app.serve` owns and closes it. Health probe is a tested module now and flips unhealthy in ~25 s instead of ~71 (#253)
- [x] "talk more" sets verbosity (walkthrough #3) — the live setting is now in the agent's per-turn view and `set_verbosity`'s description says it is the only way to change it; the guard suppresses a narrated change over a turn that called no setter. Eval `verbosity_up_from_low` 5/5, full gate 25 passed (#252)
- [x] Board clicks land on the square you clicked (walkthrough #2) — chessground's cached screen rect went stale whenever the reply above the board moved it; the board now drops the rect at the start of every interaction. Reproduced and verified in Chromium, Firefox and WebKit (#251)
- [x] Guard stops eating correct hints (walkthrough #1) — a bare "take" is advice, not a report, and capture talk hung on an unplayed move describes a line; new eval `advice_capture_survives_guard` 5/5, full gate 24 passed (#250)
- [x] Design stance: code owns truth and safety, the model owns understanding — CLAUDE.md's "prefer code over prompts" rule amended before the walkthrough fixes (#249)
- [x] Phase-4 manual walkthrough — first systematic hands-on pass, live deployment + phone, traced as the fresh baseline; 6 defects and 8 notes filed to TODO (`docs/qa-walkthrough-2026-09-04.md`)

## 2026-09-01

- [x] Docs clean slate: CLAUDE/TODO/DONE/BRIEF and the architecture docs cut to readable length; style-police prompt tests removed (rules are behavioral now — the eval gate, the guards)
- [x] Hints mode retired — hints are on-request and engine-backed; advice guard now enforces evidence, eval `hints_off_no_advice` → `advice_is_engine_backed`, full gate 23 passed / all 5/5 (PCC #314, #245)

## 2026-08-08

- [x] Night-silk board & piece theme — squares, pieces, the six board states, coordinate color (#243)

## 2026-08-07

- [x] Mobile chrome: inline icons, real tap targets, dvh, placeholder fits (#242)
- [x] Board coordinates: opposite-ink labels in the square corners (#241)
- [x] Board frame square again on iOS Safari; column tightened (#240)
- [x] Captures split to their owners; turn pill dropped (#239)

## 2026-08-06

- [x] Master-screen fixes: voice-first command row + merged status row (#238)
- [x] Transport-guarded the remaining `api.ts` helpers (#237)
- [x] Read endpoints serialized against concurrent mutations (#230/#234)
- [x] Refused board move surfaces instead of crashing submit; piece snaps back (#231/#233)
- [x] Hands-free voice survives a dropped request (#232/#235)

## 2026-08-05

- [x] Claimable threefold / fifty-move draws, backend-complete (#220/#228)
- [x] Eval harness seeds saves where the app actually reads them (#227)
- [x] Pending promotion invalidated when the authoritative board changes (#222/#226)
- [x] Delegate messages serialized one exchange per conversation (#221)
- [x] Game saves written atomically (#219)
- [x] Hint arrows bound to the board version they analyzed (#218)

## 2026-08-04

- [x] Direct mode selectable: `CHESSAPP_AGENT=off`; agent-on stays the default; UI told via `agent_available` (#206)

## 2026-07-29

- [x] TTS fetch bounded by a client deadline (from PCC audit #217)

## 2026-07-28

- [x] Voice stops reading SAN as made-up words (pronunciation normalization; PCC #315)
- [x] Observe beat stopped announcing Glitch's next move — data removal on both channels, no new prompt rules (#193)
- [x] mcp pinned below 2 (2.0.0 dropped `mcp.server.fastmcp`)

## 2026-07-27

- [x] Runaway thought loop capped: planner 2048 / narrator 4096 max_tokens, truncation handled
- [x] Narrator offered no side to play for — mid-turn state view carries no turn/legal_moves/FEN (#188)
- [x] Glitch stopped claiming the player's moves as his own — mover attribution + guard direction checks (#185/#186)
- [x] Player's move shows on the board while Glitch is still reacting
- [x] Guard records what it suppressed; two classes stop firing on opinions
- [x] Glitch stopped apologising for things he never said — canned substitutions no longer remembered as his turns
- [x] Sprint-5 measurements read: narrator excess is tokens at flat rate (r²=0.998); wordiness-cap slice rewritten
- [x] Trace trajectory records each call's arguments
- [x] Eval line records per-call output tokens

## 2026-07-26

- [x] Eval line splits planner vs narrator latency
- [x] Declined question no longer burns four planner iterations (`no_progress` stop)
- [x] `llama_brain` surfaces the provider's error message
- [x] Difficulty changes reach the UI and survive restarts
- [x] E2E gap sweep against the audit's risk list — staleness across surfaces fixed via `live_pending`
- [x] Eval statistics worth trusting: Wilson bounds + block-sequential sampling (`tests/evalstats.py`); decision: evals never enter CI

## 2026-07-25 (the agent-control audit sprints)

- [x] Live phase progress in the UI over the state websocket
- [x] Trace gaps closed: `turn_id`, `correlation_id`, derived `mutations`
- [x] Honesty guard material class ("two pawns down" is arithmetic)
- [x] Turn summaries replace the raw transcript window (`conversation.condense`)
- [x] Honesty guard expanded to all verified reply facts (`VerifiedFacts`)
- [x] `hints_off_no_advice` diagnosed and closed (1/5 → 5/5)
- [x] Structured tool errors: every refusal carries `retry` + alternatives
- [x] Live settings injected into the agent's per-turn state
- [x] Code-owned hints gating (capability withheld, not prompt rule)
- [x] Recovery semantics for provider failure mid-turn (landed moves stand)
- [x] Board-version precondition on mutations
- [x] Tool-free closing pass asserted route-by-route (`test_closing_pass.py`)
- [x] Per-turn mutation limits, code-enforced
- [x] Turn state machine + coordinator; engine reply never model-callable
- [x] Full planner/narrator split (cured `long_capture`: poisoned 1/5 → 5/5)
- [x] Move flow split: observe beat while Stockfish computes; deterministic reply announcement
- [x] Board controls route through the coordinator; one confirmation gate for voice and buttons

## 2026-07-13

- [x] Final voice: keep `am_fenrir(2)+am_michael(1)` — judged on real games
- [x] Glitch does swear (watch-item from #95 closed itself)
- [x] Qwen3-TTS voice-design spike dropped — a Kokoro blend won
- [x] Brain owns the bounded tool loop (STANDARD.md §3); pipeline drops phase two (#124)
- [x] Thinking policy measured: round-trips counted, `ChatResult.usage` kept (#125)
- [x] Authoritative player color used everywhere (#126)

## 2026-07-12

- [x] `voice.py` off the OpenAI SDK onto httpx (#119, VOICE-PLAN Phase 2)

## 2026-07-11

- [x] Post-game screen: verdict modal, review, Copy PGN (UI overhaul 3/3)
- [x] Random starting color + pre-game side switch (2/3)
- [x] One stacked layout at every viewport (1/3)
- [x] Agent eval harness + gemma-4-12b baseline (Phase 2 slice 8 — migration epic complete)
- [x] `agent:` block in `app.yaml` (slice 7)
- [x] Conversation persistence + delegate REST endpoint (slice 6)
- [x] httpx `ChatProvider` replaces the OpenAI SDK client (slice 5)
- [x] stdio MCP server off the registry + `.mcp.json` (slice 4)
- [x] Decorator tool registry via `func_metadata` (slice 3)
- [x] Layered personality: global Glitch vendored, chess flavor on top (slice 2)
- [x] LLM env vars renamed to the workspace standard (slice 1)
- [x] Chill pass on Glitch — Ryan's own lexicon (#104)
- [x] iOS Safari VAD out-of-memory fixed: single-threaded ORT wasm (#103)
- [x] Continuous voice death after rebuilds fixed (#99/#100/#101)
- [x] Custom Glitch voice: Kokoro blend on a dedicated TTS server (#97)
- [x] Glitch verified live + prompt tuning (#95)
- [x] Personalities collapsed to Glitch (#93)
- [x] No new-game confirmation after game over + captured-panel cleanup (#91)

## 2026-07-10

- [x] Hands-free voice conversation mode: on-device Silero VAD (#88)
- [x] File-source capture phrases end-to-end ("d takes e5") (#85)
- [x] Deterministic fast-parse path for plain moves (#83)
- [x] STT hardening: chess-vocab prompt + transcript cleanup (#81)
- [x] System prompt hot-path rewrite (#79)
- [x] Domain-retry loop for failed tool calls (#77)
- [x] Agent-facing board state view (~80% prompt cut) (#75)
- [x] Agent voice on phones fixed: audio unlock on the user's tap (#72)
- [x] Chess.com-style mobile UI (#70)

## 2026-07-07

- [x] Beginner tier: weighted move sampler replaces node-limit+blunder (#61)
- [x] Conversation memory, persisted in saves (#59)
- [x] Difficulty tiers with real target strengths (#57)
- [x] Default strength applied; personality is tone only (#55)

## 2026-07-06

- [x] Post-game review panel in the UI (#49)
- [x] Game review: move classification + accuracy (lichess method) (#47)
- [x] Hints + "what was my mistake" explanations (`chessapp.analysis`) (#46)
- [x] Settings by natural speech (acceptance slice) (#45)
- [x] Personality-biased move selection (#44) — later removed with the personality collapse
- [x] Remaining personalities (#43) — later collapsed to Glitch
- [x] Voice fast-path decision: local-only, no Web Speech API (`docs/voice-fast-path-evaluation.md`)
- [x] Voice options: output on/off, mute, talk more/less (#41)
- [x] TTS path: commentary → audio in the browser (#40)
- [x] STT path: mic → transcription → same command pipeline (#39)
- [x] Speaches container in compose (#38)
- [x] Fully-offline gameplay verified in the compose stack
- [x] Frontend served from the app container (#36)

## 2026-07-04

- [x] docker-compose.yml: app + llama-server + voice placeholder
- [x] Backend Dockerfile (multi-stage, Stockfish bundled)
- [x] Frontend CI job (lint + build + tests)
- [x] Command box → agent endpoint + commentary display
- [x] Game controls: new game, undo, resign, difficulty
- [x] Move history + captured-pieces panels
- [x] Pawn-promotion picker
- [x] Interactive board wired to backend state
- [x] React app scaffold (Vite + TS + Chessground)
- [x] `ScriptedBrain` test fake
- [x] App-assembly entrypoint + live personality switching (`chessapp.app`)
- [x] Personalities as system prompts + clarifying-question path
- [x] React-from-new-state step in the game loop
- [x] Defensive tool-call parser + retry loop
- [x] llama-server brain (`LlamaBrain`)
- [x] Brain interface (`chessapp.brain`) (#17)
- [x] `/api/command` endpoint (#18)
- [x] WebSocket `/ws` state channel (#16)
- [x] FastAPI game lifecycle endpoints (#15)
- [x] Settings tools (#14)
- [x] Write tools through the legality gate (#13)
- [x] Tool registry + validated dispatch + read tools (#12)
- [x] Full offline game vs Stockfish, no LLM — acceptance tests (#11)
- [x] Stockfish analysis tools (#10)
- [x] Stockfish bridge via python-chess UCI (#9)
- [x] Export PGN (#8)
- [x] Save / resume (#7)
- [x] Resign / result recording (#6)
- [x] Undo (#5)
- [x] Move history + captures derivation (#4)
- [x] `GameSession` wrapping python-chess (#2)
- [x] Repo, CI, monorepo layout, README, TODO/DONE, scaffold, BRIEF, CLAUDE.md
