# DONE

Completed tasks, newest first. Moved here from `TODO.md` with the completion date.

## 2026-07-28

- [x] **[M] The observe beat stopped being a move-selection beat** (user-reported the day #188 deployed: *"glitch is still stating what his move will be at the end of his first statement before the real move is stated"* — and still announcing that he almost said something that didn't happen). The trace on the post-#188 build made it unambiguous: **6 of 8 move turns in the evening game ended the reaction with an announced move** — "Nd2. Nice development, bro.\n\nMy turn.\n\n...Be6." then the app's real "a5." — and the one guarded turn ("O-O. Safety first.", claims=['move']) is the second symptom: when the announced move happens not to be backed by any board the turn held, the honesty guard rightly eats the reaction, and the player hears the canned "Scratch that" on an ordinary turn. One root behavior, and #188 didn't cure it because **the data still said everything the deleted prose used to say.** The observe-beat narrator received `_agent_state_dict` read after the player's move landed: `turn` (the engine's color) beside `player_color` is "you are playing black" in JSON — the reactions literally open "My turn." — and `legal_moves` is, mid-turn, the engine's own menu to pick the announcement from. The split `make_move` result carried `fen` + `turn` too, which is how the *brain route* — whose closing brief holds no state block at all — produced the same announcements. And the transcript taught the format: every remembered move turn ended in the app's appended "\n\ne5." announcement, and given those back verbatim the narrator completed the pattern at the beat where the reply doesn't exist yet — the first announced move of a fresh game appears exactly one turn after the first remembered announcement, and turn 7 repeats turn 6's announced "...Be6" verbatim. **The fix is data removal on both channels, no new prompt rules.** `narrate()` now receives `_narrator_state_dict` on every route — the agent view minus every spelling of "it is your move": no `turn`, no `legal_moves`, no `fen` (whose string names the mover); what commentary uses stays (history, `player_color` for capture direction, captures, outcome, saves, settings), and the planner keeps the full view, since mapping an utterance onto a move is what `legal_moves` exists for. The split `make_move` payload reports the move and not the mid-exchange board it left; the atomic (MCP) payload keeps `fen`/`turn`, because a caller with no pipeline gets the settled position. And **a move turn is remembered by the reaction alone** — `CommandOutcome.memory` and the drag route's record now carry the post-guard reaction, never the composed commentary — which **deliberately reverses #189's "appended facts are true and stay"** on the new evidence: truth was never the issue, register is, and the engine's reply is board history the state block injects fresh every turn (the digest's own reasoning). Substituted turns keep remembering the deterministic facts exactly as #189 built. The guard is untouched: #187's every-board width still stands (staleness is not invention) and remains the backstop for announced moves no board backs. **The eval gate earned its keep by going red first:** the initial full run measured `hints_off_no_advice` 9/15 BELOW_FLOOR — every failure the known, TODO-tracked planner `set_hints_mode(enabled=true)` flip (≈11% historical), a scenario that plays no move and opens a fresh conversation, so nothing this diff changes enters its context. Proven rather than asserted: isolated A/B in the same warm server session at a fixed 15 samples — **main 13/15 ABOVE_FLOOR, this branch 15/15 ABOVE_FLOOR**, first planner call token-identical (1,917) across every arm and codebase — the in-suite red is the scenario's session/run-order variance, the same effect the Sprint-5 `play_as_black` item tracks. Two full-gate re-runs then came back green: **all fifteen scenarios ABOVE_FLOOR both times** (`hints_off_no_advice` 4/5 then 5/5, `long_capture` 5/5 in all three conditions both runs — the release blocker holds), final run pytest exit 0, infra 0. 7 tests changed red-first (the sideless view on all three narrate routes and in the split payload; reaction-alone memory on the fast path, the brain route and the drag; the #189 reversal carries its evidence in its docstring); **1,445 backend green**, ruff clean. (#193, #194)

- [x] **[S] mcp pinned below 2** (unplanned, found as a CI-only red on #194's first run). mcp 2.0.0 released 2026-07-28 and drops `mcp.server.fastmcp` — the module the tool layer's `func_metadata` import and the MCP server live on — so a fresh `pip install -e .[dev]` resolved to 2.0.0 and every test module died at import: CI's `test` job collected nothing while local venvs, still holding 1.x, stayed green (main's last green run predates the release by hours). `mcp>=1.20.0,<2`, with the migration to the 2.x layout left as its own future slice. (#195)

## 2026-07-27

- [x] **[M] A runaway thought loop ends in tens of seconds, not at the 300 s read timeout** (live-incident fix, not a backlog line — though it lands half of the long-deferred narrator-budget item). Twice on 2026-07-27, Gemma-4-12B entered a degenerate thought loop on an *ordinary planner call* and generated 20k+ tokens until the provider's read timeout fired: the player watched "thinking" for five minutes, the turn closed as `provider_error`/`unreachable`, and the shared GPU stayed pegged after the disconnect because cancellation does not reliably propagate through llama-swap (one incident ground on 10+ minutes until a manual `/unload`). The cause was an absence: `llama_brain.py` never passed `max_tokens` — the provider supported it, nothing set it, so llama-server ran `n_predict -1`. **Every brain model call now carries a per-phase ceiling** — planner **2048**, narrator **4096**, with `Brain.narrate` on the fast path carrying the narrator's, because it *is* that phase. The numbers were sized from the measured record rather than taste, generous side up, because clipping a legitimate turn is the worse failure: the planner never thinks and its real output is tool calls or a one-line note (tens of tokens in every trace record), so 2048 is ~20× headroom bounding a runaway to ~30 s; the narrator's one thinking turn legitimately reaches 2,633 completion tokens (the token study in `docs/agent-evals.md`; a live 2,408 in the incident-day trace), so the ~1024 a first instinct suggests would truncate real commentary, and 4096 keeps every observed legitimate narration intact while bounding a runaway to ~60 s instead of 300. **A call the cap cut off is a failed turn, never a truncated one that travels** — thinking tokens count toward `max_tokens` on this server, so a `finish_reason == "length"` result may carry empty `content` with the whole budget spent in `reasoning_content`, or a mid-sentence fragment, and neither may move onward. A truncated *planner* turn ends the planning phase under the existing `no_progress` stop with the loop's own note (the fragment is never the handoff note; results that landed first are real and reach the narrator like any no-progress stop's). A truncated *narrator* (loop closer or `narrate`) returns empty text with its cost intact, and the pipeline composes its deterministic lines around it exactly as it already does — the stuck line on the brain route, the move announcement on the fast path. This is deliberately a **safety ceiling against the runaway, not the tighter wordiness cap** the 2026-07-27 token study weighed (the legitimate and repeat-tail distributions overlap too much for one); the TODO line was rewritten to carry only that remaining decision. **Eval gate on an idle card: 23 passed in 3 m 53 s, infra 0, inconclusive 0** — every scenario ABOVE_FLOOR STABLE, `long_capture` 5/5 in all three conditions (the release blocker holds), `hints_off_no_advice` 4/5 (above floor; its one failure is the known unasked-hints flip, its own open TODO line, with normal-size completions — not a cap effect), and no `length` finish appeared anywhere in the run: the caps clipped nothing, as sized. 9 new tool-boundary tests (per-phase caps pinned on every call; the truncation contract on all three routes, including cost-kept-words-dropped); **1,441 backend green**, ruff clean. (#190)

- [x] **[S] The narrator is offered no side to play for** (user-reported the same evening #186 deployed: *"Glitch often states what his move will be at the end of his first statement and then he states his move again at the end"* — "i'll go with d6 to free up the bishop. / d6. / Bb4.", and Bb4 was what the engine played). **The user's diagnosis was exactly right: the cause was something added that wasn't needed.** The trace made it a before/after with the flip at the deploy boundary — the old image's reactions react and never choose ("Kf7. Your king's getting cozy in the middle, man. Your move. / Rac1."), while on the new image **every reaction of the game, from move one, announced a reply of Glitch's own** ("classic. i'll go with c5." — engine played e5). On moves 2–4 the guesses coincided with Stockfish's obvious replies (a recapture, an attacked queen, natural development), which masked it; move 5 diverged and also picked up a mimicked bare-SAN line learned from the transcript, where every remembered turn ends with the deterministic announcement. The narrator speaks during the observation beat, while Stockfish is still computing, so an announced reply is **always** invented — and the guard cannot catch this one by design: "I'll go with d6" is a hedged future (threats are the personality and must survive), and a bare pawn-push SAN is deliberately never a move claim. What #186 needed was one purely negative idea — *the results are the player's moves, not yours* — but it shipped with an **identity**: the fast-path brief named each side's color ("you are playing black") immediately next to a state block that, mid-turn, shows the engine's turn and the engine's legal moves; and the `_BASE` bullet assigned him the engine's replies as "your own moves". Told he is black, on a board where it is black's move, holding black's options, he picked one. **The fix is pure removal.** The brief's attribution is a constant again — no `player_color` read, no color sentence for either side — and the bullet is trimmed to its negative form. Deliberately no replacement prohibition: a first draft added "the engine plays your side, and its move is reported separately" and was deleted before commit, because "the engine plays your side" hands him a side all over again and a prompt rule holds ~half the time — the removal is the fix. Tests updated red-first: the brief now pins that no color is named whichever side the player has, and the prompt pins that nothing assigns the narrator moves of its own. **Eval gate: 23 passed in 3 m 36 s on an idle card, all fifteen scenarios ABOVE_FLOOR, none UNSTABLE** — which is also the honest caveat, recorded here and in the PR: the suite has no scenario pinning "a reaction announces no reply", so this bug rode through a green gate in *both* directions, and the traced turn (its `fen_before` + utterance) is a ready-made scenario if that floor is ever wanted. **1,432 backend green** (net −1: three wording pins replaced by two), ruff clean. Verified on the redeployed container. (#188)

- [x] **[M] Glitch stopped claiming the player's moves as his own** (user-reported, not a backlog slice: *"glitch often acts like the moves I made are what he did. or that he took my piece when i actually took his piece"*). Two slices, because the failure had a generation side and a checking side and neither alone closes it. **The generation side is the house rule pointing at itself again** (#186): nothing in the turn's record said *who* moved or *whose* piece died. `make_move`'s payload reported `san`/`capture`/`check` with no actor, the fast-path brief opened with **"You just acted on the player's behalf"** — which a 12B reads as "I moved" — and on the brain route the narrator closes a transcript in which its own planner literally called `make_move`, so "I took your knight" was the natural reading of everything it was shown. Attribution had to be inferred from move-history parity plus `player_color`, which is exactly "never make the model decide what deterministic state already knows". So the payload now states it as data: `mover` (constant `"player"` on this route, stated anyway, because the reader has no board) and `summary`, one code-composed sentence — *"The player played Nxe5, capturing your pawn."* — with piece words from python-chess and en passant already right because `MoveResult.capture` is; `_engine_move_dict` carries its own, neutrally worded ("played", not "replied", because the same dict reports `new_game`'s opening move, which answers nothing). The brief now opens by attributing the move to the player and naming both sides' colors; `_closing_brief` is untouched because the summaries ride inside the changes JSON it already embeds; and `_BASE` gains the one standing rule — a move carried out at the player's request is the player's move, and what it captured was yours. All additive: no existing payload field moved, and six exact-dict assertions were relaxed to per-field so the next additive field doesn't break them. **The checking side is why the guard never caught it** (#185): capture direction was verified only when an explicit "I"/"you" subject preceded the take-verb, and Glitch's register is subjectless — "Snagged your bishop.", "Your knight is gone." — so every such phrasing fell to the union of both sides' captures and a capture announced in exactly the wrong direction read as verified; and a *credited* move was unguardable outright, because `VerifiedFacts.moves` is one deliberately wide set (played + reported + legal), so "I played Nf3" verified off the player's own Nf3. Now the possessive is direction evidence when there is no subject — whose piece it was says who took it, since the speaker is always the player's opponent — with the subject still outranking it and the union kept for phrasing that pins nobody ("a knight came off"), because fail-permissive-on-real-ambiguity is the guard's contract and a possessive is not ambiguity. A new `owned_move` class checks a credited SAN against the side that really played it: `moves_by_player`/`moves_by_opponent` on `VerifiedFacts`, fed by `GameSession.move_history_by_color()` — the history split replayed off the board like `captured_pieces()`, not index parity, so a FEN-rooted black-to-move session credits correctly — plus this turn's own results (`make_move` → player, the engine's reply → opponent). A caller that supplies neither set falls back to the wide set, so the routes with no pipeline to assemble attribution are untouched. Threats and hindsight survive as they must: "I'll play Nf3" and "you could have played Bb5" hedge out, and "took your knight *back*" is still undo talk. **Eval gate (owed by #186's prompt changes, run on an idle card): 23 passed in 5 m 23 s, all fifteen scenarios ABOVE_FLOOR STABLE (each 5/5), infra 0, inconclusive 0** — the `docs/agent-evals.md` baseline holds. 58 new test cases across the two slices (summary composition incl. en passant, brief attribution both color orders, possessive direction, subject precedence, owned-move credit both ways, the no-attribution fallback, the color split off a black-to-move root); **1,433 backend green** (1,375 before), ruff clean both PRs. (#185, #186)

- [x] **[S] The player's move shows on the board while Glitch is still reacting** (user-reported, not a backlog slice: *"my move is still not shown on the board until glitch is done reacting"*). The state websocket broadcast the board document exactly once per turn, at the close — after the observation narration *and* the engine's reply — so in agent mode the board sat frozen through both, and a typed or spoken move looked like nothing had happened for the several seconds the turn took (a drag only escaped by chessground's own optimism). The fix publishes the document the moment a mutation lands, from the chokepoint every model-initiated mutation already passes through: **`ToolRegistry.on_mutation`**, next to `on_tool` and for the same reason — dispatch is the one road every route's `make_move` takes (fast path, drag, brain loop), so a board frame published from there is one the game really reached. The delta is read off `board_version` around the handler, in a `finally`, because a handler that mutated and then refused still moved the board. `create_app` points it at **one deduped emitter** (one frame per `board_version`), which the endpoints' own end-of-turn sends now go through too — so a dispatched op followed by its endpoint's send is one document, not the same one twice. And state frames now **ride the progress queue** (`StateBroadcaster.broadcast` is `publish` with the state envelope): a turn emits two frames whose order *is* the board — delivered the other way round, the player's piece would snap back — and two paths onto one socket could not promise that, while the single consumer that already keeps `begin` in front of `end` can. The one frontend change is a guard, not a feature: a mid-turn frame carries the *engine's* turn and legal moves, so `App.tsx` offers the board nothing to drag until the turn is the player's again (the same silence a review position gets). Direct mode is untouched — the atomic exchange never dispatches, so it keeps its one end-of-turn frame. Route-level acceptance pins the user-visible property exactly: on the fast path and the drag, the first frame after the connect snapshot is the player's move alone, the next is the finished exchange; a confirmed destructive op broadcasts once, not twice. **Eval gate not triggered** — no prompt, model, tool-schema or loop change; the model sees byte-identical input (the golden schema fixture is the proof; `on_mutation` is registry plumbing, not offer surface). 7 new backend tests (plus the drag-broadcast test strengthened from "the final state arrives" to "the move arrives first"), 2 new frontend; **1,375 backend green**, 196 frontend, ruff clean. (#183)

- [x] **[S] The guard records what it suppressed, and two classes stop firing on opinions** (user-reported follow-up: *"It's still doing it"*, with a screenshot of the canned correction on a symmetrical London). **The first PR is the important one, and it is an admission.** When the guard fires, the offending text survived *nowhere*: `commentary` is replaced, the trace kept only `guarded: true` plus the substituted text, the transcript deliberately keeps it out (the previous slice's own fix — a canned correction fed back is a register the model imitates), and the claim classes rode in `extra`, which the default log formatter drops, so the container log line read `commentary_claimed_unverified_fact` and nothing else. Both live investigations therefore proceeded by *guessing at candidate phrasings* and testing them against reconstructed facts — which is not a debugging strategy, it is how a guard gets loosened blindly, and that is the one way this one becomes worthless. The original "the lie itself is not kept" was reasonable when there was one class and the failure mode was a model faking checkmate; with twelve classes **a false positive is now the likelier failure**, and a false positive is unreadable without the text that caused it. So `_guarded_commentary` returns the classes rather than a bool, `turn_record` carries `guarded_claims` and `suppressed`, and both go in the log **message** rather than `extra`, because a field nobody sees is a field that does not exist. The advice guard is a suppression too and named nothing at all in the record — a turn cut for leaking a hint looked identical to one cut for inventing a capture; it now reports as `move_advice`. Tracing is switched **on for the deployed container** onto the volume the saves already ride: single-user home deployment, the record never leaves the box, and the alternative is what actually happened — a misfire reported from a phone with the evidence already gone. **The second PR is the fix the instrument pointed at**, found by building the *real* `VerifiedFacts` for the reported turn (`1.d4 d5 2.Bf4 e6 3.e3 Nf6 4.c3 Bd6 Bg3`, nothing captured all game, level material, no analysis run) and running a corpus of plausible commentary through it. Two classes fired on lines that were **true**. `draw` read the bare adjective: "drawn" is almost always an *assessment* of a level position ("this is drawn", "a drawn endgame", "looks drawn"), and an assessment is the trash talk the guard exists to protect — while the report is about the *game* ("the game's drawn"). The hedges caught "basically" and "dead drawn" and "looks like" and let the plainest phrasings through, on exactly the symmetrical positions that earn the word; a London against a London is where Glitch reaches for it. It is now read only with the game as its subject, which is **the material class's own distinction already shipping** ("two pawns down" is a count, "getting crushed" is not). `move` read a threat as a report: "Qh5 is coming" names a move that is deliberately *not* playable yet — that is what makes it a threat — and `_FUTURE` held every explicit future ("I'll play Qh5", "Qh5 next", "gonna") and none of the idiomatic ones, so coming/incoming/looming/on-the-way were added. The **ending class is deliberately untouched**: it reads no tense at all by documented decision, because it guards the worst lie the app can tell, so "checkmate is coming" stays a claim. The negatives are the point of the tests — loosening an assessment must not lose the event, so "Stalemate.", "We drew." and "Game's drawn. gg." all still report. **Honest scope, recorded because the previous slice over-claimed:** sixteen live samples of the reported turn came back clean, so this is a corpus-driven fix on two demonstrable false positives and **not** a reproduction of that specific bubble. The instrument is what makes the next one provable rather than guessed at. **Eval gate: 23 passed on an idle card**, every scenario ABOVE_FLOOR STABLE, `long_capture` 5/5 in all three conditions, infra 0/25 (`hints_off_no_advice` 4/5, its own open TODO). 1,369 backend green (1,357 before), ruff clean. Verified on the redeployed container end to end: new image, `CHESSAPP_TRACE_PATH` live, a healthy turn recording `guarded: false`, `guarded_claims: []`, `mutations: 2`. (#180, #181)

- [x] **[M] Glitch stopped apologising for things he never said** (user-reported after a live game, not a backlog slice: *"the honesty guard makes glitch say things like 'I almost said something that didn't happen. That's my bad.'"*). **Neither half of that sentence was in any prompt** — it was the guard's own canned correction, paraphrased back by a 12B that had read it in its own transcript. Two defects compounding, fixed as three PRs. **(1) The guard was checking the wrong board** (#176). The observation beat hands the narrator the position after the player's move, while Stockfish is still computing, but `_verified_facts` was assembled from `ctx.session` *after* the reply landed — so every reaction was judged from a board its own commentary never saw. Two classes flip across that gap and both flip on the most ordinary thing in chess. `material` is counted post-reply, so a recapture turns *"you're up a piece"* (+3 when written) into an unbacked claim (0 when checked); measured on `1.e4 e5 2.Nf3 Nc6 3.Bb5 a6 4.Bxc6 dxc6`, and note that `_CHESS_FLAVOR` **explicitly asks Glitch to react to material** — the prompt was requesting the one class the pipeline was set up to falsify. `moves` unioned the legal moves at `fen_before` and *now* but never those of the mid-turn position, which is exactly the list `_agent_state_dict` hands the narrator: the state block gave it moves and the guard punished it for naming them. So the facts span **every board the turn held** — `_verified_facts` takes `fen_observed` (`_MoveBeats.observed_fen`, kept only when a narration was really written from it) and `VerifiedFacts.material` becomes a tuple of balances any claim may match. Staleness is not invention; the same reasoning already lets `captured_by_player` span the whole game. The negatives hold: a direction no board backs, and a move legal on none of them, are still guarded. **(2) The correction was recorded as Glitch's own words** (#177) — the half that turned a false positive into a personality change. Every canned substitution is first person (*"Scratch that — I said something the board doesn't back up"*, *"I lost the thread on that one"*, *"My brain cut out mid-turn"*) and `condense` hands the last `RECENT_TURNS` back **verbatim** to `brain.narrate`, so the narrator read its own apology as something it had said and produced the register on turns where nothing was guarded at all — each imitation recorded in turn, so the voice outlived the window that seeded it. The rule now: **text the app substitutes *for* what the model said is for the player, not for the model's memory**; text the app *appends* as fact is true and stays. `CommandOutcome.memory` carries it, equal to `commentary` on every turn Glitch spoke on himself and `_remembered_facts` otherwise — the deterministic move confirmation (`"Bxc6. dxc6."`) where a move landed, empty where nothing did, with the player's ask keeping its place either way so a guarded turn never punches a hole in the digest. An empty assistant message cannot ship at a chat template that alternates, so `condense` stands `DIGEST_ACK` in for it — the one line in the app already chosen for having no voice to imitate — while `window()` still records the truth that nothing was said. **The delegate had the same one-field conflation and its test is what found it**: `StoredMessage.memory` diverges only when guarded and is what `history_for_loop` replays, while the caller still gets the correction on the wire (`MessageRead` names its fields, so it never leaks). **(3) A guarded turn was swallowing the engine's reply** (#178), found while fixing the other two: `_move_commentary` concatenated the deterministic announcement onto the reaction *before* the guard ran, so one invented claim took the app's own true line with it — and two tests pinned that explicitly (*"the lie takes the reply line with it"*). It is the one fact a guarded turn cannot afford to drop, since the board moved under the player and the correction says nothing about how. The guard now runs on the **model's half** of the turn and the app's lines are composed around whatever survives; nothing was lost by the reorder, because the announcement, the canned confirmation and the lost-brain line are deterministic truth and there was never anything in them to guard. **Eval gate run twice on an idle card, 23 passed both times**, every scenario `ABOVE_FLOOR STABLE`, `long_capture` 5/5 in all three conditions so the release blocker holds, infra 0/25 (`hints_off_no_advice` 4/5 on the second run — still above floor, and that arm's known ~11% unasked-hints behavior is its own open TODO line, untouched here). 1,357 backend green (1,345 before), ruff clean. **The general lesson is the house rule pointing at itself:** the guard is code owning a fact the model must not invent, and it was still wrong — because it compared the model's words against a board the model was never shown. A check is only as honest as the evidence it is handed. (#176, #177, #178)

- [x] **[S] Read the two open Sprint-5 measurements — one arm answered both (Sprint 5).** The repeat-stop token question and the unasked-hints question both wanted a 20-sample `hints_off_no_advice` run, so they got one: idle card, `CHESSAPP_EVAL_REPORT` set, SHA `afb3066`, **19/20 → [0.80, 0.99], ABOVE_FLOOR, STABLE, infra 0, 6 m 25 s**. **The narrator's excess is tokens, and the rate is flat.** `narrator_ms` 2.56× and `narrator_out` 2.58× between `no_progress` and `completed` — the same number — while `narrator_tok_s` is 0.98× (exact permutation p = 0.30, i.e. no effect) and `narrator_in` is 1,001 vs 993. Pearson r between tokens written and milliseconds spent is **0.9989 (r² = 0.998)**: narration time is tokens and essentially nothing else. That excludes both rival mechanisms the instrument was built to separate — the serving path (rate flat across a 15× range of output) and prefill (the duplicate tool result the repeat-stop turn dispatched costs ~8 prompt tokens, not 30 seconds), the latter a real candidate that is why `narrator_in` was recorded at all. Replicated the same day on a second, independently sampled arm: ms 5.01× / out 4.87× / rate 0.96× / r² = 0.996 — the magnitude of the excess is whatever the tail draws, but the agreement of the first two and the flatness of the third reproduce exactly. **So the thrice-deferred fix is aimed at the right mechanism, and the same data says it will do less than "2.6×" implies** — legitimate passing narrations reach 1,259 tokens against a 521–2,633 tail, so the distributions overlap almost entirely: a cap safe for ordinary narration (~1,300) clips 3/20 samples, and one that catches the median tail (~600) truncates real commentary. Recorded as *a ceiling on the runaway, not a cure for the tail*, which is a materially different slice from the one the backlog had been carrying, so the TODO line was rewritten rather than ticked. **The one number that does not reproduce is the repeat rate itself** (8/20 then 2/20 the same day, against 3/20 and 5/20 before): it is unstable across runs, so none of them is "the" rate and the 40% is not quotable — noted in the doc against the earlier framing. **The second finding is the one nobody was looking for.** With arguments now on the trajectory, **2/20 samples called `set_hints_mode` and both were `enabled=true`** — hints turned *on*, unasked, on a turn where the player only asked "what should I play here?" (3/40 across both arms; 12/105 ≈ 11% counting the 9/65 that raised it). The arm's *only* failure was one of the two, and it flipped the setting and then played a move on the player's behalf (`set_hints_mode(enabled=true) → make_move(move="Bc4")`) — recorded as **suggestive, not established**, since one failure against two flips in twenty samples is a 2/20 coincidence and it must not be quoted as a demonstrated causal chain. **What *is* established is about the gate:** the scenario scored 0.95 ABOVE_FLOOR STABLE while doing this, because it checked that no advice reached the text and never checked the setting. So the `check` now asserts `app.ctx.settings.hints_mode is False` after the turn, pairing with the `setup` that already asserted it going in, and it asserts the *setting* rather than the absence of the call — a turn that legitimately set it back to False is no defect. A post-assertion arm proves it behaves: **19/20, ABOVE_FLOOR, STABLE, 4 m 09 s**, firing on one real flip that **would have passed silently** under the old check (no board mutation, no SAN leak — every existing assertion satisfied) and false-positiving on none, with the gate still green exactly as the point-estimate rule predicted. The remedy is deliberately *not* in this slice: whether the planner should be offered the settings setters at all on a turn that was a question is a capability question, a `src/` change owing a full gate, and it is left open with the argument on both sides written down.
- [x] **[S] The trajectory says what each call *asked for* (Sprint 5; the first step of the unasked-hints item).** `trajectory=[get_best_moves → set_hints_mode]` names the tools and not their arguments, and there is a live suspect behind that blind spot: across the four `hints_off_no_advice` runs on 2026-07-26 a `set_hints_mode` call appears on **9/65** samples where the player asked only *"what should I play here?"* — steady at 3/20, 3/20, 2/20, 1/5, so not a fluke of one arm. Whether it turned hints **on** is the whole question, because that is a setting the player owns being changed by an agent that was asked a question, and no gate covers it today: the settings tools are ungated by design and the honesty guard checks settings *claims*, not unasked changes. The old line could not tell that from a no-op. So each token now renders as `name(arg=value, …)`, and the four rendering rules are each answering a way the line could lie or become unreadable: **JSON values**, so `enabled="true"` (a mis-invocation the schema didn't want) and `enabled=true` (the call) do not read alike; **keys sorted**, because the line is read by scanning many samples for a difference and the model's emission order would manufacture differences that aren't there; **values cut at 24 chars, visibly**, since one trajectory shares a terminal line with eleven other clauses and a pasted PGN would cost the whole record; and **no parens at all when a call took no arguments**, because `get_board_state()` on every read tool is noise on every line to say nothing. The one thing that had to *move* is the rejected-move marker: `make_move(illegal)` beside real arguments reads as an argument named `illegal`, so both failure markers are now `!` suffixes after the arguments — `make_move(move="Qh8")!illegal`, and a dispatch error keeps its bare `!`. The per-sample report record carries the same string, which is what turns "how many samples called `set_hints_mode`, and which way" into a query over the report rather than a hand tally across four runs of scrollback — the same reason the latency slice put `call_ms` there. **Eval gate: not run, and not owed** — `git diff --stat` shows no `src/` paths, so the model sees byte-identical input; `agent_api._tool_call_read` has always put `arguments` on the wire and only the reporting path was dropping them (the same proof-of-innocence rule the last three harness slices worked under). Verified live on an idle card beyond the GPU-free tests: `settings_by_speech` printed `trajectory=[set_difficulty(tier="beginner")]`. **This does not settle the finding, it makes it decidable** — if the flip is real the fix is a capability question rather than a prompt line, and the scenario's `check` should assert `app.ctx.settings.hints_mode is False` after the turn. 6 new tests, **1,338 backend green** (1,332 before), ruff clean.
- [x] **[S] The eval line says what each model call *wrote* (Sprint 5; the second step of the repeat-stop latency item).** The previous slice settled **where** a `no_progress` turn's extra 30 s goes — the narrator's own round trip, and not a harder sample: all three repeat-stop narrations exceeded all seventeen `completed` ones in the same 20-sample arm (exact permutation p = 0.00088). It could not settle **why**, and no amount of further latency can: *"the narrator emitted three times the reasoning tokens"* and *"generation ran at a third of the rate"* are the same milliseconds. That needs tokens per **call**, and the trace has only per-turn totals — the identical blind spot `model_ms` had. So `fakes.ModelCall` now records `prompt_tokens`/`completion_tokens` off `ChatResult.usage` beside the reading it already took, `evalstats.split_tokens` attributes them, and the `[eval]` line and every per-sample record carry `call_in`, `call_out`, `planner_out`, `narrator_in`, `narrator_out`, the derived turn totals — and **`narrator_tok_s`**, which is the whole point: a narration that ran 3× longer at the same rate wrote 3× the tokens and the standing candidate fix (bounding the narrator's thinking budget) is aimed at the right thing, while the same tokens at a third of the rate is the server and a token cap would not touch it. `narrator_in` rides along because prompt size is its own candidate mechanism — a repeat-stop turn *dispatched* the duplicate, so its narrator reads one more tool result, and a longer prompt costs prefill before a token is written. **The interesting half is again the attribution, and this time it was refactored rather than rewritten.** "The narrator is the last call" is true only on some turns, and two clauses on one line disagreeing about *which* call was the narrator would make the line unreadable in exactly the case it exists to explain — so the `SPLIT`/`NO_NARRATOR`/`UNKNOWN`/`NONE` rule moved into `_attribute_phases`, stated once over call *positions* and nothing else, and both `split_latencies` and `split_tokens` now derive from it (the latency behavior is byte-identical, pinned by its existing tests). **Unknown stays `?`, never 0**, and tokens gave that rule a second edge the latencies never had: a round trip that *raised* reports no usage at all, so its phase total is genuinely unknown, and summing the surviving calls would print a partial number as though it were the whole — `_total` refuses, per phase, so a died planner leaves `planner_out=?` while the narrator's own reading survives beside it. **The two seams are printed, not reconciled:** tokens come off the call meter and milliseconds off the trace (the brain measures a call that raised; the provider seam cannot), and they sit either side of `model_calls` so a disagreement about how many round trips happened shows on the line instead of being folded into a wrong attribution. `narrator_tok_s` is the one number needing a reading from both, so it is derived on `EvalRun`, the only object holding both, rather than either half reaching into the other. **Eval gate: not run, and not owed** — `git diff --stat` shows no `src/` paths, so the model sees byte-identical input (the same proof-of-innocence rule the last three harness slices worked under, and the harness-bug precedent is why it is spelled out every time). **The instrument is built and has not been read**, which is recorded as such in both `docs/agent-evals.md` and the TODO line that survives it: repeats are the ~10–25% tail, so answering the question costs a fresh 20-sample `hints_off_no_advice` arm on an idle card, and the thinking-budget cap stays deferred until that arm says which fix it is. 17 new tests, **1,332 backend green** (1,315 before), ruff clean.

## 2026-07-26

- [x] **[S] The eval line says which model call spent the turn's time (Sprint 5; the first step of the repeat-stop latency item).** `model_ms` is a per-turn **sum**, and the finding it was asked about needs the parts: a `no_progress` turn narrates 2–3× slower than a `completed` one at the *same* model-call count, and "the narrator's round trip is slow" and "this sample was hard for the model, which is why it both repeated and rambled" add up to the same total. The readings themselves were never missing — `AgentResponse.model_latencies_ms` has carried one per round trip in call order since the trace-gaps slice, and `turn_record` writes them out — the harness simply never printed them. It does now, on the `[eval]` line and in **every per-sample record** in the JSONL report: `call_ms`, `planner_ms`, `narrator_ms`, and a `phases` attribution saying how confidently those two were separated. **Per sample rather than aggregated, because the question is whether the slow samples and the `no_progress` samples are the *same* samples** — which needs the pairing kept, and which is why the medians in the repeat-stop finding had to be computed by hand out of terminal scrollback. That also closes a claim `docs/agent-evals.md` had been making since the statistics slice: it promised per-sample `model_ms` in the report, and the report carried none (the sample dict held `model_calls` and wall-clock `seconds` only). **The attribution is derived, and it refuses to guess** — the interesting half of the slice, because "the narrator is the last call" is true only on some turns. `evalstats.split_latencies` reads the route and the stop reason off the same trace record the readings come from (so the two can never describe different turns) and answers `SPLIT` on a `brain` route that stopped `completed` or `no_progress` (both reach the narrator — the `no_progress` case being exactly the one under investigation) and on the narrate routes, where there is no planner loop at all and the planner's 0 is real; `NO_NARRATOR` on a budget stop, where attributing the last call to a narrator would invent time that was never spent; `UNKNOWN` on `provider_error`, where the dead call could have been either phase and a median over guesses is worse than one over fewer samples; and `NONE` when no readings came back. **Where the logic lives is load-bearing rather than tidy:** the live suite cannot be TDD'd, so the decision went into `tests/evalstats.py` — pure, GPU-free, unit-tested on every commit — beside the three verdicts already there, and `BUDGET_STOPS` moved there too because the same set decides both "this sample tested nothing" (`INCONCLUSIVE`) and "this turn has no narrator to attribute time to", and two copies could disagree about what a stop means. The route vocabulary is re-declared as literals for the reason `DETERMINISTIC_FAILURES` is (that module must stay importable with nothing installed) and a new `test_the_route_vocabularies_agree` pays for the copy against `trace.ROUTE_*` — so a route added in production shows up here as a failing test rather than as samples silently attributed `UNKNOWN` forever. **And the harness's own reporting seam is now tested** (`tests/test_eval_harness.py`, 5 tests, no GPU): the line, the `EvalRun` and the sample record are pinned off a synthetic trace record, including the untraced-turn case, because the harness-bug precedent — a wrong tool offer in this file silently moving a scenario from 5/5 to 0–3/5 — means an instrument that can lie about what it measures is worse than none. Verified red against the pre-change harness before it went green. **Eval gate: not run, and not owed.** `git diff --stat` shows no `src/` paths: no prompt, model, tool schema or loop code changed, so the model sees byte-identical input (the same proof-of-innocence rule the statistics slice worked under). Nothing in `docs/agent-evals.md`'s recorded numbers moved — the repeat-stop finding is annotated with what the next run can now settle and explicitly marked **not yet measured**. 16 new tests, **1,315 backend green** (1,299 before), ruff clean.

- [x] **[S] A declined question no longer burns four planner iterations (Sprint 5; the spin-off the eval-statistics slice filed).** Asked "what should I play here?" with hints off — an ask whose right answer is "no, hints are off" — the planner spent its whole iteration budget and stopped on `max_iterations`, which reaches no narrator, so the player got the canned *"I lost the thread on that one"* about a question nothing was wrong with. **Reproduced before a line was written**, which is what set the fix's shape: 20 samples on `main` measured **17/20** with 2 `max_iterations` stops, and both stalled trajectories were exact repeats (`evaluate_position → analyze_last_move → analyze_last_move → evaluate_position`; `evaluate_position → analyze_last_move → evaluate_position → set_hints_mode`), each finishing in 1.6–1.8 s across four thinking-off planner turns — a loop spinning, not one doing work. So: **a planner turn whose every tool call repeats one the turn already made is its last** (`stop_reason: "no_progress"`, a fifth value beside the standard's three and chess's `provider_error`). A call identical to one already made cannot bring anything new back, so no further iteration can either — the loop ends the planning phase itself rather than granting iterations that can only repeat. **Three things it deliberately is not.** *Not a refusal:* the repeat is still dispatched, because whether a repeat may **run** is the tool layer's judgment and it already answers that (the phase machine refuses a second player move, a destructive op needs its confirmation), and the trace then shows the repeat that ended the phase instead of hiding it. *Not a budget stop:* verified results came back, so this stop reaches the narrator exactly as `completed` does and the player gets a real answer — pinned at the brain seam and again route-level in `test_closing_pass.py`, where the closing call still carries `tools=None`. *Not the correction path:* a turn whose calls failed schema validation dispatched nothing, and repeating a malformed call is what the (smaller) correction budget is for, so that budget keeps precedence — the `elif` is load-bearing and `test_repeated_schema_failures_exhaust_the_correction_budget` is what holds it. Two existing tests that scripted "the same call forever" to reach `max_iterations` were rescripted to distinct calls: the behavior they pinned *was* the defect. **The measured cost, and an honest one.** The "after" arm is **20/20 → [0.88, 1.00], zero `INCONCLUSIVE`**, with the stop firing on 5 of 20 samples and all five passing — but those five narrated at a median **40.5 s** against 11.5 s for the `completed` samples in the same run, at the same model-call count. The only difference in the narrator's brief was the missing planner note, so the loop was given one of its own (`_NO_PROGRESS_NOTE` — about the *work* being finished, never the machinery that finished it, so no commentary about loops can leak) and the arm was re-run: **20/20 again**, stops at 24.3 s and 29.1 s. Weaker than it looks and recorded as such in `docs/agent-evals.md`: n=2 against n=5, and the `completed` baseline drifted 11.5 → 9.9 s, so some of it is a faster server; a repeat-stop turn is still 2–3× an ordinary one and the residual is filed as its own TODO line (with the harness's per-call latencies as its first step) rather than explained away. What it replaced is the frame: 1.6 s of a canned non-answer. **Eval gate: 23 passed, 3 m 09 s, 75 samples, infra 0/25** — all **fifteen** pass-rate scenarios 5/5, the first recorded run where `hints_off_no_advice` is not the odd one out (4/5 with a `max_iterations` `INCONCLUSIVE` in each of the three before it), `long_capture` 5/5 in all three conditions so the release blocker holds. One observation filed rather than fixed: a `set_hints_mode` call shows up on 9 of 65 samples of an ask that was only a question, and the eval line prints tool names without arguments, so whether it flips hints **on** is unverified. 12 new tests; full backend suite green, ruff clean. (#170)

- [x] **[S] `llama_brain` swallows the provider's message (Sprint 5; spin-off filed by the eval-statistics slice).** `llama_brain.py:202` and `:276` were bare `except ProviderError:` — the loop caught the death, converted it to `stop_reason="provider_error"` so the pipeline could still close the turn (audit 20), and dropped the exception on the floor. A crashed socket and an HTTP 400 therefore arrived at the eval harness **identically**, which stopped being cosmetic the moment slice 3 taught the harness to *retry* a provider death: a context overrun on a long transcript got re-asked five times, spent five of the suite's twenty-five infra retries, and aborted with a message that had to hedge between "llama-server is not staying up" and "or the failure is deterministic". **The fix is a field, not a prose blob** — the TODO line's own wording, and the right call: a message is something a caller has to parse, and this project already refuses that shape (the tool layer's `retry: different_args | never` exists so a refusal says whether to retry rather than making the reader infer it from wording). So `provider.ProviderFailure` is a StrEnum on every `ProviderError` — `unreachable` (the socket never answered), `server_error` (a 5xx; llama-swap mid-restart), `rejected` (a 4xx, which is what llama.cpp answers a context overrun with), `malformed_response` (200, body failed wire validation — version skew), `bad_tool_arguments` — with `transient` as a **property on the enum**, so code owns the retry answer at the one place that knows it and no caller re-derives it. It rides out on `AgentResponse.provider_failure`, a plain `str` next to `stop_reason` for the same reason the token counts are plain ints (the seam stays model-agnostic; the provider's enum never crosses it), through `turn_record` to the trace, which is the seam the harness already reads stop reasons off — so the harness needed no new production surface beyond the field itself. `evalstats.classify` then splits the death: a kind in `DETERMINISTIC_FAILURES` is the new `Outcome.PROVIDER_REJECTED` and fails the item on the **first** sample; everything else stays `INFRA` and is retried. **Unknown means retry, deliberately** — an unclassified `ProviderError` (a test double, a future raise site that forgets) and every non-200 (which carries no kind at all, because the *app* answered, not the provider) default to `unreachable`/retry, because spending a few samples on a deterministic failure is cheap while calling a restarting server deterministic would abort a whole suite on the first crash. `INFRA_ABORTED` consequently now means only what it says. **The one wart is honest duplication:** `evalstats` must stay importable with nothing installed (it is the GPU-free half of the gate), so it re-declares the non-transient set as literals rather than importing `chessapp`, exactly as it already does for `STOP_PROVIDER_ERROR` — and `test_evalstats` pays for that by pinning `DETERMINISTIC_FAILURES == {f for f in ProviderFailure if not f.transient}`, so the two vocabularies cannot drift apart silently. Deliberately out of scope: `api.py`'s own `except ProviderError` sites (the fast-path, resign and confirmed-op narrations) already log with `exc_info`, so nothing is swallowed there; and the delegate wire still stores only `stop_reason`, since the trace is what the harness reads and adding the kind to the API would be surface nobody asked for. 23 new tests, **1,280 backend green**, ruff clean. **Eval gate: 23 passed, 3 m 40 s, 75 samples, infra 0/25** — fourteen of fifteen pass-rate scenarios 5/5, `long_capture` 5/5 in all three conditions (the release blocker holds), `hints_off_no_advice` 4/5 with one `INCONCLUSIVE` `max_iterations` stop, which is the already-filed loop defect reproducing at the same rate for the third measurement running. It was run because the loop file changed, though a healthy turn is byte-identical through it (`provider_failure` is empty unless the provider died); what it actually bought is the harness edits, which cannot be unit-tested — `_measured` reading the field off the trace and `_sample` handing it to `classify` ran 75 times without disturbing a classification. **The new branch went unexercised live**: zero provider deaths across those 75 samples, the same healthy-server caveat the retry path itself carries, so `PROVIDER_REJECTED` is covered by unit tests and nothing more. (#169)

- [x] **[S] Agent difficulty changes reach the UI and survive restarts** (user-reported, not a backlog slice: "I asked Glitch to make it harder and the frontend didn't change"). Two defects under one symptom. *The change was real but invisible:* `set_difficulty` configured the live engine and `Settings`, but the UI's tier selector reads only the mount-time `GET /api/settings` and its own `POST /api/game/difficulty` response — the state websocket is deliberately board-only, so an agent-side change never reached it (voice never had this bug because `/api/command` already returns `speak`; `tier` now rides the same response, null when strength was set by raw skill/elo, and `useGame` syncs the selector from it). *And nothing persisted:* `Settings` was a plain in-memory dataclass rebuilt with the default tier at every assembly, so difficulty survived a browser reload (it lives server-side) but reverted on every backend restart. Settings now write through to `settings.json` under `ToolContext.save_dir` — the volume the save files already ride — at `Settings.__setattr__`, the one chokepoint every `set_*` tool and endpoint passes through (the `board_version` pattern: derive at the chokepoint, never a call at each site someone can forget), restored in `ToolContext.__post_init__` so assembly applies the loaded difficulty to the engine like any other startup value. Best-effort by design: a missing/corrupt/invalid file means defaults, an atomic tmp-and-replace write means no half-file to restore from, and a failed write never fails the mutation (the tracer's rule). All agent-adjustable settings (verbosity, hints, voice) persist the same way, whichever surface set them. Eval gate not triggered — no prompt, model, tool-schema or loop change; the model sees byte-identical input. 8 new backend tests, 2 frontend; full suites green. (#168)

- [x] **[S] E2E gap sweep against the audit's risk list (Sprint 5, slice 4 — audit 22).** Seven of the audit's eight architectural risks were already pinned by the slices that built the architecture, so the sweep's job was to *check* rather than re-assert, and `tests/test_risk_sweep.py` opens by mapping all eight to the files that hold them (drag-through-Glitch → `test_board_controls.py`; observe-before-reply → `test_command.py` + `test_coordinator.py`; no duplicate moves → `test_closing_pass.py` + `test_trace.py`'s mutation counts; hints-off → `test_app.py`'s withheld offer + `test_command.py`'s advice guard; pending-op safety → `test_tools.py`/`test_command.py`/`test_board_controls.py`; provider recovery → `test_command.py`'s "Recovery" block; stale versions and concurrent clients → `test_board_version.py`). A sweep that only ticked boxes would have been worth little; this one found **one real hole, and it was in the confirmation gate**. `/api/command` disarms a pending op on its way past — an unrelated utterance is a new intent, not an answer — but nothing else did, and the *other* surfaces move the board too. So this was three keystrokes away on a live board: **"I resign"** (the gate arms it and asks) → **drag a move** (the board advances; the question is still armed) → **"yes"** → a game two plies further on ended, and the same held for an undo, a resume, or another client's move in between. **The fix is the board-version machinery one layer down, not a `ctx.pending = None` at every surface that mutates** — that would be the "documented in the docstring and hoped for" pattern the house rules exist to refuse, and it would silently miss the next route somebody adds. `PendingOp` now carries the `board_version` it was armed against; both answering surfaces read it through **`ToolContext.live_pending()`**, which drops an op whose board has moved on; and `confirm_pending` reads through it too, because it is the last gate before a game is thrown away and owes its callers the check rather than trusting each of them to have made it. Dropped, not queued: the "yes" then falls through as an ordinary utterance and the brain can ask what they meant (the typed road), and the button road answers 409 like a click with nothing armed at all (the frontend already treats a non-200 confirm as "leave the board alone", so no UI change was needed). **The one wrinkle is *when* the stamp is taken:** the gate arms **mid**-turn and the turn can still mutate after it — "play Bc4 and then start over" arms the reset before the engine's reply lands — so an arm-time stamp would be stale before the player could speak. What they are being asked about is the board at the *end* of that interaction, so `restamp_pending()` re-points it there once, where the command closes; that case is its own test, because it is the regression a naive version check would have introduced. The sweep's other two additions are coverage rather than code: **agent mode had no concurrency test of its own** (the item-8 acceptance test drives `/api/game/move` in *direct* mode, and in agent mode that endpoint is a different code path — the coordinator's beats, a narration, an offloop hop — so the window for a second client is wider), now pinned both ways — two drags on one version admit exactly one turn, and two *unversioned* drags serialize into two whole exchanges with `max_overlap == 1` and one turn id each, which is the honest behavior rather than a bug (a client that wants exclusivity sends a version); plus a double-clicked confirmation runs one op. And **the delegate wire was never asserted to share the pipeline's guards** — it runs the same `_run_command`, which is the design, but "one road in" is a claim about behavior: a conductor-driven move now provably observes the player move before the reply, its commentary is scrubbed by the advice guard with hints off, and a provider death mid-turn keeps the landed move, collects the reply and stores `stop_reason="provider_error"` so the caller can tell a half-run turn from one that never started. One boundary is pinned as a *decision* rather than a fix: `/api/game/hint` answers with hints off, because `hints_mode` governs what **Glitch volunteers** (the tool it is offered, the moves its commentary may name) and the Hint button is the player asking on the trusted path with no model in it — the recorded call from when hints gating shipped (#154), now a test so it stays deliberate. 12 sweep tests + 5 unit tests on the new stamp; **1,261 backend tests green**, 192 frontend, ruff clean. **The eval gate had nothing to regress and was run anyway:** tool schemas are byte-identical (the golden fixture is the proof) and no prompt or loop code was touched, but the confirmation road is one the suite exercises live — **23 passed in 3 m 22 s, 75 samples, infra 0/25**, fourteen of fifteen pass-rate scenarios 5/5, `long_capture` green in all three conditions (the release blocker), `hints_off_no_advice` 4/5 with one `max_iterations` `INCONCLUSIVE` — the already-filed loop defect at the rate the 20-sample campaign measured, not a new one. Recorded in `docs/agent-evals.md` as a confirmation run; the baseline is unchanged. (#167)

- [x] **[S] Eval statistics worth trusting (Sprint 5, slice 3 — audit 23; the old flaky-floor item).** The project's only behavioral gate was deciding on a coin flip: nine copies of `assert result.rate >= 0.8` over five samples, which cannot resolve a 60–100% band — `long_capture[poisoned]` measured 3/5 then 4/5 on one build (red once, green once), `main` gave 5/5 then 3/5, and `play_as_black` gave 5/5, 2/5, 0/5, 5/5 across four same-build runs. A gate that flaps gets re-run until green, which is exactly how the 2026-07-25 baseline became a hand-assembled "composite of consecutive runs in which every 502-hit scenario was re-run". **Three defects hid inside that one number and each got its own fix.** *Infrastructure was scored as behavior:* llama-server crash-restarts every 3–8 minutes, and since audit 20 that arrives as a **200** carrying `stop_reason="provider_error"` — indistinguishable, to the old harness, from the model quietly doing nothing, so `evalstats.classify` sorts every sample and an infra death is re-taken rather than counted (bounded per scenario and across the suite; exhausting the budget is `INFRA_ABORTED`, a hard failure, which is also what a *deterministic* provider error now looks like instead of a 0/5 blamed on the model). *Vacuous passes counted as passes:* a budget stop reaches no narrator, so `hints_off_no_advice` — the suite's one purely-negative check — scored passes for never having been tested (three of four in one recorded run), and now raises `VacuousRun` → `INCONCLUSIVE`, counted as a non-pass and reported as what it was. *And `TODO.md`'s own first suggestion was measurably wrong:* at a true rate of exactly 0.80, more repetitions against `rate >= floor` makes the gate **worse** — fixed-5 goes green 0.737 of the time, fixed-20 **0.630**, four times the GPU for a worse gate. The verdict is now a **one-sided 95% Wilson bound** (z=1.645 — the FAIL test uses one tail) with **block-sequential** sampling: red only when the upper bound is *below* the floor (evidence, not a bad sample), green on the point estimate, and only the genuinely ambiguous 3/5 buys another block of five to a cap of 20. At n=5 the whole table is six upper bounds (0.351/0.565/0.728/0.857/0.954/1.000), so **a healthy scenario still costs exactly five samples** and the suite-level odds of a spurious red drop ~3× for ~4% more samples. **The whole slice lives in `backend/tests/`, and that is load-bearing rather than tidy:** the harness-bug precedent (a wrong tool offer silently moved `capture_names_victim` from 5/5 to 0–3/5) means every harness change owes a proof the model saw byte-identical input, and `git diff --stat` showing no `src/` paths *is* that proof. It is affordable because `api._run_command` already traces every route, so a `_CollectingTracer` on the existing `tracer` seam hands the harness the real `stop_reason` (plus `route`, `mutations`, `guarded`, `model_calls`, `model_ms`) on **both** seams — where adding `stop_reason` to the panel response would have been a production edit inside a harness slice. The 200-assert `_run` used to carry **moved rather than vanished** (`_run_once`, which also asserts `stop_reason != "provider_error"`, on the eight single-shot scenarios); `_run_panel` stopped inventing `"stop_reason": "completed"`; the nine literal asserts became `_assert_floor` + a one-table `_FLOORS`, with `blocking=True` for `long_capture` alone because `TODO.md` declares it release-blocking and an unresolved gate on a release blocker is not a pass. Every decision is a pure function in the new `tests/evalstats.py` with 54 unit tests in `tests/test_evalstats.py` — the eval suite itself cannot be TDD'd, so the *gate* is now regression-tested on every commit even though what it gates is not. `CHESSAPP_EVAL_REPORT` writes JSONL per scenario as it finishes (header with knobs/floors/SHA, suite line with the infra budget consumed — a baseline should say what budget produced it), which is what stops the tables in `docs/agent-evals.md` being hand-transcribed. **The limits are documented rather than papered over:** the gate separates ≈0.95 from ≈0.5–0.6 and nothing finer (against a drop to 0.7/0.6/0.5 it fires 0.32/0.59/0.79), power depends on the floor so a floor must never be lowered for quiet, optional stopping leaks slightly toward false passes (0.414 vs fixed-5's 0.337 at a true 0.60, via the 4/5 early accept fixed-5 has identically), and **the i.i.d. assumption is known false** — `play_as_black` at 5/5 isolated and 0/5 mid-suite is a cluster of size 1, which this slice makes *measurable* (per-block rates, an `UNSTABLE` flag, per-sample `model_ms`) and explicitly does not fix, naming the session-scoped sampler as unpaid work. Two spin-offs filed: `llama_brain.py:202`/`:276` discard the `ProviderError` message, so a dead socket and an HTTP 400 read the same; and a question that should be declined in one turn burning four planner iterations is a loop defect, not a floor to slide. Audit 21's scheduled-run bit is **dropped by decision** — evals never enter CI. 1,244 backend tests green (54 new), `src/` untouched. **The campaign ran and the floors did not move — which is a result, not an omission.** Full suite at default knobs: 23 items, 75 samples, **3 m 40 s**, infra 0/25, `long_capture` 5/5 in all three conditions, nothing escalated. Forced to 20 samples: `undo_and_replace`, `play_as_black` and `long_capture[poisoned]` all **20/20 → [0.88, 1.00]**, so for the first time the suite *affirmatively* demonstrates its 0.8 floor rather than merely failing to disprove it (n=20 is the smallest sample that can — `wilson_lower(19,20)=0.804`); raising a floor on one clean session would be fitting a threshold to a good afternoon. **The one real finding is the vacuity defect reproducing at scale:** `hints_off_no_advice` measured **17/20 → [0.68, 0.94]** with **3 samples stopping on `max_iterations`** — the planner burning its whole budget on an ask whose answer is "no, hints are off" — which the old rule would have counted as three passes and reported as `20/20`. It is an 85% scenario and the old harness could not have told you. The A/B holds: `long_capture[poisoned]` pooled 15/15 → [0.85, 1.00] on the old harness against 20/20 → [0.88, 1.00] here, intervals overlapping, with every cost pin unchanged (`fast_path_low` 0 calls, `fast_path_normal` 1, agent-path move 3) — the harness did not move what the model sees. **Two caveats recorded rather than buried:** the session saw zero provider deaths across 135 live samples against a documented 3–8-minute crash cadence, so the retry path was exercised only by unit tests and a fake-runner pre-flight; and the `play_as_black` order effect did not reproduce in either position, leaving that experiment inconclusive (its 20-sample arm also overlapped another pytest on the one GPU) — re-filed as its own TODO line. (#164)

## 2026-07-25

- [x] **[M] Live phase progress in the UI (Sprint 5, slice 2 — audit 19; absorbs the old "show tool calls live" item).** A turn stopped being a black box in Sprint 1 and the UI never found out: it still showed one spinner over "validating your move", "Glitch is reacting" and "Stockfish is calculating" alike, which says *waiting* when the question the player has is *what for*. The phases now ride the state websocket as they happen (`progress.py`, `type: "progress"` on the envelope that was always there for a second message kind), keyed by `correlation_id` — the id the trace slice minted before anything on the wire carried it, so a line the player saw and the record of the turn behind it are one search apart. **Nothing narrates.** Every event comes from the chokepoint that already owns the fact it reports: `TurnCoordinator._enter` (now the *only* writer of `_phase`, which is what the observer buys), `ToolRegistry.dispatch` after validation (so a label is always a tool that really ran — an unknown name and rejected args report nothing), and the brain's own two phases in the brain's own vocabulary. Same rule that made `mutations` a `board_version` delta: a hand-placed "now we are calculating" is a second copy of the turn sequence, free to drift. The backend ships phase names and the UI owns the words (`frontend/src/progress.ts`), because copy is not machine truth. **This is what finally made `agent_observing` a phase the app enters** — the gap `docs/turn-coordinator.md` had explicitly assigned to this slice. Two ways in, one conditional `mark_observation()`: the pipeline marks it directly on the fast path and the board drag (so it holds whatever brain is behind the narration), and on the brain route the narrator turn happens *inside* `get_agent_response`, which holds no coordinator by design — so the brain reports `narrating` and app assembly wires that report to the mark. `begin_observation` stays strict; the conditional form is the one callers need, because both narration sites are also reached on turns where nothing moved. **The unglamorous half is the one the feature actually rests on: the pipeline's blocking steps moved off the event loop** (`api._offloop`). A loop parked inside a model call or a Stockfish search delivers nothing, so without that hop every event would arrive in a burst after the turn it described had already ended — live progress that is not live is a transcript. Reports cross back with `call_soon_threadsafe` onto a queue one pump drains, which is also what keeps `begin` in front of `end`. The mutation lock is held around all of it either way, so no ordering changed; the incidental win is that one client's LLM turn no longer stalls another's `/api/state`. Deliberately silent: direct mode, the board's control buttons and MCP — none of them opens an interaction, and the reporter's rule is that an event outside one does not exist, so that is derivation rather than three special cases. Reporting never costs a turn (every observer swallowed, like the tracer). The liveness guarantee is pinned in two halves because no single test in the harness can carry both — `TestClient` gives a websocket session its own event loop, which would let a "live" assertion pass against a blocked server — so one test shows the events are published while the turn is provably still inside the model and the other shows the loop stays free; delete the hop and the second goes red (verified). Eval gate: not strictly triggered by the #148/#149/#153/#160/#161 precedent (no prompt, model, tool-schema or control-flow change — what the model sees is byte-identical), but the loop file changed and the sequencing moved threads, so the standing release-blocker was measured anyway: **`long_capture` 5/5 in all three conditions**, plus `fast_path_low` at 0 model calls (a plain move is still zero-LLM), `fast_path_normal` at 1, and the agent-path move at 3. 1,190 tests green backend, 192 frontend. (#162)

- [x] **[S] Close the trace gaps (Sprint 5, slice 1 — audit 18).** The record already said what a turn did; it now says *which* turn it was and how much of the board it moved. Three additions, each derived rather than tallied: `turn_id` (the coordinator's counter, captured as the interaction opens), `correlation_id` (one per interaction, `uuid4` hex-12, also stamped on the warnings that turn logs — `commentary_leaked_move_advice` and `observe_narration_failed` were previously tied to a record only by timestamp), and `mutations`, read as a `ToolContext.board_version` delta because that is the one chokepoint every board mutation already passes through. **The mutation count is only legible next to the turn id, which is the point:** a healthy agent-mode move turn is 2 (player move + engine answer), so a third under one id is the duplicate the phase machine exists to refuse, and a mutation on a route that should have had none is a bypass. Per-model-call latency (`model_latencies_ms`, plus a `model_ms` sum `turn_record` derives so the parts and the total cannot disagree) is measured in the brain, not the provider — a round trip that *raised* has a latency too, and only the caller of a raising call is still around to record it; `LlamaBrain.clock` is injected so the timing is deterministic in tests. The five routes' cost plumbing collapsed into one `_ModelCost` (`.of(response_or_narration_or_None)` → `.as_trace()`), which is what keeps a route from recording three of the four numbers. **Also closed the gap `docs/turn-coordinator.md` had explicitly assigned to this slice:** a destructive op answered by *button* wrote nothing down while the identical op answered by voice was fully traced — `/api/game/new`, `/api/game/resign` and `/api/game/confirm` now record one line on a new `control` route (armed-and-asked, confirmed, or declined; the op name stands in for the utterance exactly as the structured move does on the `board` route, and a decline records no tools rather than a fabricated result). Still deliberately untraced, and now stated as such: `/api/game/undo` (not a destructive op, reaches the session directly, and visible anyway as the next record's `fen_before` not matching the previous `fen_after`) and direct-mode drags (no agent to diagnose). Nothing on the wire changed; `correlation_id` is server-side only until the live-progress slice decides the event shape. Eval gate: not strictly triggered — no prompt, schema, tool or control-flow change, only accounting around the existing calls (the #148/#149/#153/#160 precedent) — but the loop file changed, so the standing release-blocker was measured anyway: **`long_capture` 5/5 in all three conditions**, plus the two fast-path cost scenarios (zero-LLM at low verbosity, one `narrate` call when chatty) and the 3-call agent move. 1,147 tests green. (#161)

**Sprint 4 closed — P3: context quality (audit 17).** Two slices, both about what a turn is allowed to carry: the brain's context became a deterministic digest instead of twenty turns of prose (#159), and the honesty guard grew the last fact class the audit's list named (#160). The sprint's planner/narrator anchor had already moved to Sprint 1.

- [x] **[S] The honesty guard's material class (Sprint 4, slice 2 — carried out of Sprint 3, whose slice 5 deliberately left this one class).** "You're two pawns down" is operational — material balance is a piece count, board truth, no Stockfish in it — so it is verified like every other fact. `GameSession.material_balance()` is the count: the player's advantage in pawns, positive when ahead, read **off the board** rather than from `captured_pieces()`, because a promotion adds material nobody captured. `VerifiedFacts.material` carries it as `int | None` — `None`, not `0`, for a turn that supplied no count, since a level board is itself a claimable fact ("we're dead even") and the guard must fail closed on evidence rather than on a default that reads as one. **The bar the class had to clear to ship was the one `TODO.md` set: tell "you're getting crushed" from "you're two pawns down".** It does, by only reading a *quantified* claim — a direction (`up`/`ahead`/`down`/`behind`) plus a named amount (a/one/two/…, pawn/knight/bishop/piece/rook/queen), in either order, from either side's mouth — so an opinion about the position stays an opinion and survives untouched. The arithmetic is side-aware (`I'm up a piece` is the same fact from the other end; no subject at all is ambiguous, so either reading verifying is enough) and **direction is the fact while magnitude is verified to within a pawn**: material talk names the nominal trade ("up a knight" for a knight-won-for-a-pawn, which the board counts as two), which is how everybody who plays chess says it, while being told you are ahead when you are behind is the invention the class exists for. "The exchange" claims a direction and nothing more — it has no fixed value in ordinary use. Swept over the 46 recorded live turns: **0 matches**, so the class adds no false positives to real Glitch prose. Eval gate not triggered — no prompt, model, tool-schema or loop change; nothing the model sees differs (the precedent #148/#149/#153 set), and no eval scenario asserts commentary wording. 1,127 tests green. (#160)

- [x] **[M] Turn summaries replace the raw transcript window (Sprint 4, slice 1 — audit 17).** A brain no longer gets twenty turns of prose; it gets the last four verbatim behind a deterministic digest of what the player asked for earlier (`conversation.condense`, designed in `docs/turn-memory.md`). **The digest carries no board facts, no settings and no saves** — those are injected fresh into the state block every turn, and a summary restating them would be the second, ageing copy that caused the self-poisoning bug in the first place, so two of the audit's five categories are correctly *absent*: already solved, one layer down. What is left is the part that genuinely only exists in the conversation — the player's standing asks, in their own words — and Glitch's side of those older turns is dropped entirely, because personality competing with the tool decision is this project's measured failure mode. **No model writes the summary.** Everything code can verify is already injected; the residue is words, and copying words needs no model — an LLM rollup would be a third phase on a 12B, unguarded, inventing facts in the one place nobody reads. So `condense` is pure and unit-tested with no provider. One policy, both entry points (`Transcript.memory()` for the panel and the board drag; `agent_api.history_for_loop` for the delegate wire); `window()` stays the raw record and the `Brain` seam is untouched — a brain still just receives chat messages. Two synthetic messages, not one, because the digest rides as a user turn and the chat template alternates; the ack is dropped when it would be the thing that breaks alternation. Bare-move turns (`e4`, `Bxe6`, `e2e4` — how a board drag records itself) are dropped from the request list: already in `history`. Measured on the eval suite's real 20-turn thread: 40 messages → 10, 1,440 chars → 1,049 (~27%), and flat from there however long the game runs. **Eval gate: 23 passed, every pass-rate scenario 5/5 in one run** — the third clean full-suite sweep on record and the first on condensed context. All nine long-transcript conditions are direct probes of this change and all nine are 5/5, `long_capture[poisoned]` included; `live_like` now measures the digest, and `poisoned` still measures self-poisoning because every probe's poison lands inside the verbatim window (noted in `docs/agent-evals.md`, which is load-bearing rather than incidental now).

**Sprint 3 closed — P2: settings and honesty as enforceable policy (audit items 11–14, 16).** All five slices merged. The sprint's rule was the house rule one layer out: what the model may *say* is code's call too, not just what it may do. Hints became a withheld capability plus a response guard (#154), live settings became per-turn injected state (#155), refusals became structured recovery data (#156), the advice guard's licence became scoped to reported moves (#157), and the honesty guard became a verified-facts check over every operational claim (#158). `long_capture` stayed 5/5 in all three conditions across every slice's gate.

- [x] **[M] Expand the honesty guard to verified reply facts (Sprint 3, slice 5 — audit 13).** The ending guard proved the shape; this takes it to every operational fact a turn produces. `honesty.VerifiedFacts` is the turn's record — ended/drawn/check, captures per side, the moves the turn accounts for, saves, the live settings, the analysis numbers — assembled by `api._verified_facts` from the tool results, the engine's reply and the board. `unverified_claims` reads the commentary against it class by class (ending, draw, capture, check, move, save, hints, voice, difficulty, verbosity, evaluation) and names the classes the turn cannot back; both guard sites (the converged pipeline point and the board drag) run it. An ending claim keeps `UNTRUE_CLAIM_REPLY` — its job is telling the player the game is still live — everything else gets `UNVERIFIED_CLAIM_REPLY`, and the turn traces as `guarded` either way. **Every class keeps the ending guard's bar** (an unhedged assertion in its own sentence) and is hedge-aware per class, because "I'll take your knight" is the personality and "I took your knight" is the claim; where the two can't be told apart the guard fails *permissive*. Deliberately narrow where narrowness is what makes it safe: a bare pawn push is never a move claim (a square is spelled the same way), captures verify existence rather than tense, and evaluation covers only engine-shaped numbers. **Sized against the real corpus rather than guessed:** sweeping the 46 recorded live turns surfaced two false positives, both fixed and pinned — reciting the move list (history is board truth, and now in the facts) and reading out a PGN (a date is not a score) — leaving 2/46 guarded, exactly the two known lies. Eval gate: 23 passed, every scenario 5/5 in one run, `long_capture` green in all three conditions; analysis scenarios re-run against the final build. Material talk ("you're two pawns down") is the one listed fact class left out — board-derivable, needs its own side-aware arithmetic, filed in `TODO.md`. (#158)
- [x] **[S] `hints_off_no_advice` diagnosed and closed (Sprint 3, slice 4).** Red on `main` for three slices at 1/5; now 5/5, twice measured, and green in the first single-run clean sweep of the whole eval suite on record (23 passed, no 502s, no xfails). The backlog offered two candidate causes and the measurement picked one and then found a second nobody had proposed. **The evidence test was too loose:** `_advice_evidence` was a boolean — any successful `get_best_moves`/`analyze_last_move` switched the advice guard off wholesale — so a planner that answered "what should I play here?" with a mistake analysis licensed every *other* legal move too, and the narrator handed over lists (`['Bc4', 'c3', 'd3']`). `api._reported_moves` replaces it: a result licenses exactly the SANs it reported (`get_best_moves`' candidates, `analyze_last_move`'s `played` and `best`), and only those come off the list the guard checks — the mistake-analysis exemption survives, including when the better move is still playable. **And the model was granting itself permission:** with the licence scoped, the remaining failures ran `evaluate_position → set_hints_mode(True)` and then named moves, the guard reading `ctx.settings` *after* the loop. The hints setting that governs a turn is now snapshotted before the brain runs — the same instant app assembly resolves the tool offer from, so the two can't disagree; the setting change stands and takes effect next turn. #155's `settings` block was A/B'd out and is not the cause (4/5, and three of those four "passes" were `max_iterations` stops that never reached a narrator — filed for the eval-statistics slice, since the scenario's rate was mixing correct behavior with budget stops). Both halves pinned deterministically in `test_command.py`; `FakeEngine` gained `evaluate_position` so the mistake-analysis path is exercisable without Stockfish. (#157)
- [x] **[M] Structured tool errors: a refusal carries what recovery needs (Sprint 3, slice 3 — audit 14).** Every "no" the tool layer produces now goes through one builder (`ToolRegistry.refusal`) and carries `retry` — `different_args` or `never`, code's answer to "is calling again worth a round trip?" rather than something the model infers from wording — plus `board_version`, the board the refusal was about. Where a better call exists the refusal names it: `GameSession.move_alternatives` reads the rejected string for its destination square and (when it says one) its piece type, and hands back the legal moves matching whichever it gave — which is also, for free, the disambiguation of an ambiguous SAN (`Nd2` → `Nbd2`/`Nfd2`, told apart from illegal by `chess.AmbiguousMoveError` so the `reason` says which it was). Capped at `ALTERNATIVES_MAX` and named `alternatives`, not `legal_moves`, because the fallback branch is the whole position and nothing may read it as the complete set. A rejected move stays `ok: True, legal: False` — legality is a result, not a fault. Handlers that need to attach recovery data raise `ToolError` (`resume_game` returns the saves that exist); an unclassified `ValueError` defaults to `never`, since a loop that cannot say why it failed must not spend its budget finding out. Planner prompt: the vague "correct it with another call" rule is replaced by one that points at the keys, staying inside the compactness bound. Eval gate: the slice's own gate is green — `long_capture` **5/5 in all three conditions**, plus `illegal_move_honest` and `ambiguous_move`; the full suite came back 22/22 hard scenarios with `hints_off_no_advice` at 2/5. That one was **already red on `main`** and is not this slice's doing — A/B'd three ways (this build 3/5, 2/5; prompt line reverted 0/5, 1/5, 1/5; clean `main` 2/5, 0/5, 2/5), so reverting the prompt measures *worse* and `main` measures the same. Filed as its own backlog item with the mechanism (`_advice_evidence` stands the guard down whenever `analyze_last_move` ran) and the full table in `docs/agent-evals.md`. (#156)
- [x] **[S] Inject live settings into the agent's per-turn state (Sprint 3, slice 2 — audit 12).** `_agent_state_dict` now carries a small `settings` block read fresh off `ctx.settings` every turn: `difficulty` (exactly the one of tier / skill_level / elo that `Settings` has set, so the block can never imply two are in force) and `voice_output`. Verbosity and hints stay out — they already re-resolve into the system prompt per command, and the block ships in the prompt every turn on a 12B. Same rationale as `saved_games`: a fact the app holds is handed over, never inferred from stale conversation prose. (#155)
- [x] **[S] Code-own hints gating (Sprint 3, slice 1 — audit 11).** Hints-off is a capability the code withholds, in two layers because the measurement demanded both. The cut: `LlamaBrain.tool_definitions` may now be a live per-command provider (the same seam as the two prompts), and `build_app` resolves the offer off `ctx.settings` — hints off means `get_best_moves` is simply not offered, and a call at it is a schema-level unknown that never dispatches (trusted callers — MCP, the delegate wire, `/api/game/hint` — keep the full registry; the eval harness mirrors the shipping offer). The guard: the cut alone still measured 2/5–3/5, because the model invents moves from its own head — so the pipeline's advice guard (`honesty.names_a_legal_move`) replaces commentary naming a currently-playable move with `MOVE_ADVICE_REPLY` when hints are off, the turn changed nothing, and no analysis tool the player asked for reported it (mistake analysis keeps its moves). **The suite's last xfail is gone:** `hints_off_no_advice` 0/5 → 5/5, hard assert; `long_capture` stayed green; baseline re-recorded in `docs/agent-evals.md`, including a new run-order-sensitivity observation on `play_as_black` filed for the eval-statistics slice. (#154)

**Sprint 2 closed — P1: mutation safety in code (audit items 6, 7, 9, 20).** All four slices merged; the sprint's house rule held throughout — every one turned a prompt-hoped or undefined behavior into a policy the code owns: the mutation budget (#148), the tool-free closing pass (#149), the version precondition (#152), and provider-failure recovery (#153). What remains of the audit's P1 band is Sprint 3's settings/honesty work.

- [x] **[S] Recovery semantics for provider failure mid-turn (Sprint 2, slice 4 — audit 20).** A dead llama-server is now a fact about the turn, not an exception escaping after the board changed. The brain loop converts a `ProviderError` — mid-loop or in the narrator — into `stop_reason="provider_error"` carrying every tool result that verifiably ran, so the pipeline still closes the turn (collects the engine reply the coordinator was owed, broadcasts) and answers 200. What the player is told is code-picked from the one fact that matters for retry advice: nothing changed → `PROVIDER_LOST_RETRY` ("say it again", safe); something changed → `PROVIDER_LOST_TURN_STANDS` plus the deterministic reply announcement, so a landed move is never invited to be replayed. The two unguarded `brain.narrate` calls after a confirmed destructive op (confirmation + resign routes) now degrade to the canned `_destructive_confirmation` line — never a 500 between the mutation and the broadcast (the fast path already degraded correctly). Happy-path prompts, schemas, and sequencing untouched, so the eval gate wasn't triggered. (#153)
- [x] **[S] Board-version precondition on mutations (Sprint 2, slice 3 — audit 7).** `GameSession.revision` bumps inside each mutating session method and surfaces as `ToolContext.board_version` → `state.version` on `/api/state`, the websocket document, and every mutation response. Every mutating request (move, undo, new, resign, confirm, command, delegate messages) takes an optional `version`; stale means 409 `{stale: true, version, state}` before anything changes — including before `abandon_turn`. Check and mutation are welded under `ctx.mutation_lock` (acquired off the event loop), so two clients on the one shared session cannot advance the same turn twice; omitted means prior behavior (client opt-in). MCP takes the lock but gains no parameter — its advertised shape is frozen by the eval floor. The brain never sees a version anywhere. (#152)
- [x] **[S] Tool-free closing pass (Sprint 2, slice 2 — audit 9).** The planner/narrator split already made the closing pass tool-free by construction, so the slice reduced to asserting it: `tests/test_closing_pass.py` runs a real `LlamaBrain` (scripted provider) over the real registry + coordinator through the full pipeline and pins, route by route, that the model call producing player-facing text carries no tools (brain loop, fast path, board drag), and that once the turn's mutation budget is spent — the phase machine's one player move, the command window's one destructive op — the tools the planner still holds cannot mutate (the attempt comes back as error result data, board unchanged). CLAUDE.md's planner/narrator paragraph now states the guarantee as enforced-and-pinned. Tests + docs only; the eval gate was not triggered. (#149)
- [x] **[S] Per-turn mutation limits, code-enforced (Sprint 2, slice 1 — audit 6).** One player move and one engine move per turn were already the phase machine's; the new piece is the destructive-op budget, and it is *command*-scoped because `new_game`/`resign` abandon the turn they run in — a turn-scoped flag would reset itself and enforce nothing. The pipeline brackets each user command in `begin_command`/`end_command` (`try/finally`); the two destructive tools check the budget before the gate and spend it only after the session changed, so a second destructive op inside one command (the gate-aside hole: reset a finished game, then resign the fresh board) comes back as ordinary `TurnStateError` result data. Buttons, `/api/game/confirm`, and MCP stay windowless by design — one dispatch per interaction by construction. The "at most once per player turn" docstring prose is deleted from both `make_move` variants (a rule code owns does not also live in the prompt); eval gate re-confirmed the 2026-07-25 baseline (21 passed + `resume_not_denied` re-run after a 502, per the composite procedure; `hints_off` xfail unchanged). (#148)

**Sprint 1 closed — P0: the turn coordinator (audit items 1–5, 10).** All four slices merged same-day as the replan; the target architecture holds: player intent → validated player move → Glitch observes → Stockfish replies → Glitch closes the turn, with the deterministic coordinator, never the model, owning the sequence. Slices 3 and 4 were stacked branches merged after slices 1–2 squashed; their merge also restored two doc hunks (CLAUDE.md's planner/narrator paragraph, `docs/turn-coordinator.md`'s cross-reference) lost to a wrong-side conflict resolution when #144 landed.

- [x] **[L] Turn state machine + coordinator** (audit 3, 10). Explicit per-turn states, a turn ID, and rejection of actions that don't belong in the current state; the engine reply is the coordinator's job, never a model-callable tool. (#143)
- [x] **[M] Full planner/narrator split** (audit 15). Two model phases: the planner keeps the bounded tool loop on a compact persona-free contract; the narrator speaks as Glitch from verified results, offered no tools. Eval-gated; cured the `long_capture` regression (poisoned 1/5 → 5/5). (#144)
- [x] **[M] Split the move flow: the observe beat** (audit 2, 5). `make_move` and `/api/game/move` apply the player's move only; Glitch reacts while Stockfish calculates; the reply lands with a deterministic announcement. Skippable by construction (verbosity=low, provider failure). (#145)
- [x] **[M] Board controls route through the coordinator in agent mode** (audit 1, 4). A drag sends the structured move into the same beats (trace route `board`); UI new-game/resign go through the same confirmation gate; direct mode is deliberate and visible via `agent_available`. (#146)

**Backlog replanned around the external agent-control audit.** `TODO.md` rebuilt from `docs/agent-control-audit-2026-07-25.md` (the ChatGPT report Ryan supplied, code-checked and annotated). The 2026-07-14 context-sprint framing is superseded; its surviving items (hints gating, structured tool errors, turn summaries, live tool UI, eval statistics, sampling experiment, fresh trace baseline) folded into the new sprint structure rather than closed. The `long_capture` RED regression and the schema-cut warning carry over as standing constraints. The in-progress per-tool docstring-hardening thread (branch `chore/clean-tool-schema-noise`, `make_move` cut done-not-committed) is parked — Sprint 1 rewrites the move tools; its docstring lessons are noted in the new TODO for salvage there.

## 2026-07-21

**Clean the tool-definition schema noise (context sprint, audit finding 5) — ATTEMPTED 2026-07-14, SHELVED: the eval gate says no** (record moved from `TODO.md` at the 2026-07-25 replan; shipped as docs in #138). The mechanical cut worked — one `_clean_schema` in `_derive_schema` stripped the `title`/`anyOf:[X,null]`/`default` pydantic keys, dropping the offered-tool payload 7,842→6,844 chars (~249 tok, 13%), golden-guarded, 771 unit tests green. But the **`undo_and_replace`** eval collapsed: **88% → ~6%** (main 4,5,3,5,5/5; cleaned 1,0,0,0,0/5) — the model stops doing the undo→make_move chain when the schema is minimized. Isolation run (undo_and_replace, floor 80%): `title`-only 40%, `anyOf`-null-only 7%, `default`-only 67% — **every component regresses below the floor; no safe subset exists.** The finding's premise ("teaches the model nothing / zero semantic change") is empirically false for gemma-4-12b: the pydantic/JSON-Schema shape is almost certainly what its tool-use training expects, so the minimal schema is out-of-distribution and the token saving is not free. **Do not re-attempt on this model.** Revisit only if the brain model changes (re-run `test_eval_undo_and_replace_is_one_turn` as the gate) or if a way is found to shrink descriptions without touching schema structure. Branch `chore/clean-tool-schema-noise` holds the reverted code + the SCHEMA_CLEAN_MODE isolation harness in history if the data's ever wanted.

## 2026-07-14

**Token + call-count observability in traces (context sprint, audit finding 1 — the "do first").** `ChatResult.usage` was parsed off the wire and dropped, and `turn_record` counted nothing, so every cut this sprint makes was invisible in the JSONL. Per-turn cost is now threaded explicitly (no hidden collector): `_RunState` sums tokens and counts calls across the loop, `AgentResponse` carries the totals out, the fast path's `narrate` returns a typed `Narration` with the same accounting, and `_run_command` writes `{model_calls, prompt_tokens, completion_tokens}` into the trace (deterministic routes record a real zero). No behavior change; every diet below it now lands with a measured before/after. Shipped in **#136** (bundled with finding 2 + the backlog reframe / audit doc), and — because #136 updated neither TODO nor DONE — logged here after the fact.

**Shrink the base prompt; move its rules onto the tools (context sprint, audit finding 2) — merged eval-RED, regression carried.** `_BASE` is now identity + the non-negotiable contract; the move-translation block, judgment-question routing, and the resign/new_game confirm dance moved onto `make_move` / `evaluate_position` / `analyze_last_move` / `new_game` / `resign`, where a rule sits with the capability it governs. System prompt 1,521 → 755 tok; the tool-less `narrate` sheds the full 766. Shipped in **#136**. **Known regression, deliberately not patched here:** `long_capture` `poisoned` 4→0/5 and `live_like` 4→3/5 — in noisy context the model stops recognizing a descriptive move ("grab the pawn on e6") as `make_move` (poisoned runs made zero tool calls), from pulling the move-*recognition* signal out of the prompt's primacy slot. The cure is coupled to the tools-return-info / context-injection slice (findings 2/4/7), which now owns re-running those scenarios as its acceptance gate. Logged here (with the open caveat) because #136 merged it without updating the backlog.

**Sprint closed — "agent behavior: see it, then fix it" (opened 2026-07-13).** The premise held: live play didn't match the green suite because the suite pinned the tool boundary, not behavior. Turn tracing (`CHESSAPP_TRACE_PATH`) made turns readable, real games surfaced the bugs, and every one was the same shape — the app asking a 12B to decide what deterministic code already knows. Shipped (all logged in detail below and under 2026-07-13): the color-aware `undo` (#115) that opened the sprint, then the four tool-signature fixes (`undo` ply count, `resign` color #132, `analyze_last_move` color #133, `new_game` side #134), the capture-phrase parser (#131), the self-poisoning fix (#130), the resign route + honesty guard (#132), and the eval harness's real-trace scenarios with pass-rate-over-N measurement (#129). Move correctness through the model went from essentially unmeasured to gated on a number. Three threads carry into the 2026-07-14 context sprint rather than closing here: the eval-gate floor flakiness, live tool-call UI, and hints-off policy (now audit finding 9).

## 2026-07-13

**`new_game` can now assign a side — and the advertised conductor handoff works end to end (trace review finding 2, `play_as_black` 0/5 → 5/5, xfail → hard assert).** `new_game()` took no arguments, so "let's play chess as black" was a thing the tool could not say — and that is the *exact* intent string in the deep link `app.yaml` advertises to conductor (`/?intent=let's+play+chess+as+black`), so the handoff was broken at the one seam it existed for. `new_game(player_color=…)` now puts the player on the side they asked for and the engine, owning white, opens; omitted, they keep the side they have. The subtle part is the gate: the requested color rides along in the armed op, because the confirmation gate replays `pending.args` on the player's "yes" — a gate that dropped it would confirm "new game, I'll take black" into a game as *white*, silently discarding the only thing the player asked for.

**`analyze_last_move` can now mean "my" move (trace review finding 1, `my_mistake_is_mine` 0/5 → 5/5, xfail → hard assert).** The tool took no arguments and always analyzed the literal last ply — which, on the player's turn, is *always* the engine's reply. So it could not answer the question in its own docstring, and the model did not fail loudly: told "I meant MY last move, the c3 one", it re-called the same no-arg tool and reported the engine's `Bxe4` **as if it were c3**. `analyze_last_move(color=…)` now defaults to `session.player_color` (the same shape `resign`'s color default took), with an override so the player can ask about the other side without the model guessing. The house rule again: an ask the tool cannot express is an ask the model fabricates compliance with — fix the signature, not the prompt.

**`resign` gets faked: the agent may no longer *do* a destructive op unasked, nor *say* it did.** The dangerous one (trace review finding 6, `long_resign[poisoned]` red at 2/5, a coin-flip on `main`): *"I'm calling that. Game over, bro."* — zero tool calls, board still live. The player is **told** the game ended and it hasn't. A prompt rule was never going to hold; the destructive-gate slice measured a 12B honoring one about half the time. Two structural layers, and neither asks the model for anything:

- **The player conceding is not a judgment call.** `fastparse.parse_resign` settles an utterance that is *entirely* a resignation (every clause a resignation or a filler, so "you know what, I give up. I resign" fires and "i give up on this bishop" does not), and the pipeline dispatches `resign` itself on a fourth route (`ROUTE_RESIGN`) with **zero model calls**. It goes through the same registry and the same confirmation gate, so a false positive costs a question, not a game — which is exactly what lets the parser be more generous here than `parse_confirmation` dares to be. `long_resign[poisoned]` 2/5 → **5/5**, deterministically.
- **The honesty guard closes the class, not just the tool.** The parser does nothing for the rest of finding 6 — a quiet `Rh7` narrated as "you actually have me in checkmate", a won game narrated as a loss. So at the one point all four routes converge, the pipeline now checks the closing text against the board: commentary asserting the game ended or restarted, when the board is live and no destructive tool succeeded, is replaced with the truth and traced as `guarded` (`honesty.claims_destructive_outcome`). Deliberately narrow — trash talk, threats, questions and hypotheticals ("one more move and it's checkmate") are the whole point of the commentary and must survive; `test_honesty.py` is the spec.
- **`resign`'s default color** (finding 8, old TODO #3) folded in: it defaulted to the *side to move*, which is only coincidentally the player. It now defaults to `session.player_color`.
- Evals: **20/20 hard scenarios, 3 xfailed, nothing red** (was 19/20 with `long_resign[poisoned]` failing). Like the capture family, the resign scenarios are now zero-LLM and read as a tripwire on the parser; their real coverage moved to `test_fastparse.py` and `test_command.py`.

**Capture phrasings: the parser now settles them, so the model never gets the chance to fabricate.** "bishop takes" / "pawn takes" / "bishop takes pawn" were the biggest everyday hole in the agent (trace review finding 3, evals 0/5, 0/5, 0–3/5): `fastparse.parse_move` only understood a capture that named a **target square**, so every other phrasing fell to the model — which asked a clarifying question while itself naming the only legal square, invented geometry (naming the f1 bishop for a capture the c1 bishop makes), or said "Bxh6 is a solid choice" and played nothing.

- **Nothing here needed judgment.** When exactly one legal capture matches the named piece — or the named victim — the *board* knows the move. The house rule again: never make the model decide what deterministic state already knows. `parse_move` grew one anchored capture pattern (piece and/or victim and/or square, in either order), feeding the `_constrained_moves` machinery that already existed; a constraint the phrase omits is simply not applied. Its "fire only when exactly one legal move fits" rule is untouched and is the whole safety story — two bishops that can both take is still ambiguous, still `None`, still the agent's clarifying question.
- These are now zero-LLM `fast_path` moves. `api.py` needed no change: it already dispatches `make_move` for any non-`None` `parse_move`.
- **Four eval scenarios were retired, including one that passed 5/5.** `capture_names_square` ("take the h6 pawn") worked through the model, but a deterministic pass beats a sampled one — and an eval that can no longer reach the model isn't an eval. The coverage moved to `test_fastparse.py`, where it is free, exhaustive and run on every commit. `long_capture`'s utterance moved to "grab the pawn on e6" to keep that probe model-bound.

**Self-poisoning: the agent believed its own bad news.** One prior assistant turn saying a tool failed — the real one from the traces, "I can't save the game right now because the save directory isn't set up" — made the model stop calling `resume_game` **entirely** on a later turn (5/5 → **0/5**), and confabulate a reason for a file sitting on disk: *"it hasn't been saved yet."* Transcript **length** was the standing suspicion and it was wrong: 20 real turns changes nothing. The model was reading its own past prose as a standing fact about what the app can do, and never re-checking.

- It was the house rule again — **the model deciding what deterministic state already knows.** Whether a save exists is a question the filesystem answers, and the app had simply never answered it: the agent's own prose was the *only* thing in its context claiming to know. So `saved_games` now rides in the board-state block the brain gets every turn (`api._agent_state_dict` via `tools.saved_game_names`), next to `legal_moves`. **0/5 → 5/5.**
- **Nothing was removed from or rewritten in the transcript.** The tempting fix — suppress or annotate the assistant turn whose tools failed — is lossy (the thread stops making sense) and could as easily have made the failure *more* salient. The stale sentence is still there. It just has to argue with a fresh fact now, and the fact wins.
- **The premise was tested before it was built, and the probe closed another hypothesis.** `legal_moves` was *already* injected fresh every turn, so seeding the capture probe with the two real "Illegal move." refusals asked the load-bearing question directly: can stale prose talk the model out of a move the board plainly lists? It cannot — `Bxe6` still lands (poisoned 4/5). Fresh state beats stale prose, which is the whole bet. It also means **the live capture miss was never self-poisoning** — that hypothesis is closed, and both it and the resign miss remain unexplained.
- The eval is opt-in and CI never runs it, so the mechanism is guarded deterministically in `test_command.py`: seed the transcript with the real poisoning turn, and assert the state the brain is handed *still* lists the save.

**Destructive-op confirmation is structural now — the harness's last xfail is a hard assert.** gemma-4-12b honored the "confirm before `new_game`/`resign`" prompt rule only ~50% of the time, so a bare "new game" was a coin flip on your game. The rule is no longer the model's to honor: `tools.py` `_gate` refuses an unconfirmed `new_game`/`resign` — it doesn't mutate, it arms the op on the `ToolContext` and returns an ordinary rejection *result*, which the agent reads and asks from exactly as it reads an illegal move (CLAUDE.md's "domain rejections are results, not corrections"). `confirm_pending` is the only key, and the pipeline turns it on a bare "yes" (`fastparse.parse_confirmation`) with **no model call** — the same deterministic treatment the fast path gives a plain move.

- Three properties make it hold rather than merely usually work. **Confirmation is not a tool argument** (a `confirm` param would just move the coin flip — the model could open its own gate), so it lives on the context, reachable only from the pipeline. **Re-arming is not confirming**: a second unconfirmed call inside one turn is refused again, so an agent retrying in-loop can't self-confirm; only a new *user* turn can. **The armed op never survives its turn**, so a stale "yes" can't reset a game three turns later.
- **The prompt inverted, and had to.** It used to say *ask first, don't call*; it now says *call, and relay the refusal*. Under the old wording a compliant model never calls the tool, so nothing arms, so the player's "yes" has nothing to confirm and the brain re-asks forever. The gate replaces the rule; it doesn't back it up. `test_personality` pins the new contract.
- No confirmation when there's nothing to lose: game over, or not a single move played. The gate guards a game in progress, not the idea of one.
- The `resign` adherence rate TODO wanted measured first was never worth measuring — the fix makes model adherence irrelevant for both tools, so the number would only have said how bad a problem we'd already deleted.
- The design also lands the machinery the backlogged **pending-move-proposal** item needs (`ToolContext.pending` + `parse_confirmation`), shrinking it if it's ever picked up.
- Eval gate re-run (prompt + loop change): **8/8 scenarios pass, zero xfails**; every other scenario's trajectory, model-call count and latency flat against the previous baseline. `docs/agent-evals.md` finding rewritten as closed, baseline re-recorded, and the conductor guidance **reversed** — conductor should no longer add its own confirmation round-trip for chess's destructive phrasings.

**Personality & voice — Glitch is done (epic closed, no code shipped).** The 2026-07-11 direction — one hand-dialed personality (gen-z Jarvis: fully casual, low-key troll, help stays real, swearing allowed) plus a designed custom TTS voice instead of stock Kokoro `af_heart` — is complete. Both open questions resolved by live play rather than by a change:

- [x] **Final voice: keep `am_fenrir(2)+am_michael(1)`.** Judged on real games, not the audition — the standing default was already the right answer, so no `TTS_VOICE` change and no rebuild. A fresh [audition artifact](https://claude.ai/code/artifact/17586122-56a1-4682-af06-311cfdab49f7) stands if this is ever reopened: 13 takes (8 stock `am_*` voices + 5 blends) reading one deliberately-roasting line, with median-F0 and words-per-minute measured for each. Its numbers independently reproduce #97's (Fenrir 138 Hz, Michael 121 Hz, Onyx deepest, Eric fastest). Note the #97 PR body is now stale on one point: Kokoro is no longer a service in this repo's `docker-compose.yml` — it moved to the shared `../speech/` stack on port 8410.
- [x] **Glitch does swear.** The watch-item from #95 (zero swears in the first two live games despite escalated authorization) closed itself — it swears every once in a while across more games. The escalated authorization was enough; the planned few-shot tone-block exchange was never needed.
- [x] **Qwen3-TTS voice-design spike — dropped, not deferred.** It was explicitly gated on "only if no Kokoro blend has enough smirk". A Kokoro blend won, so the whole spike (freeing ~2 GB VRAM off the shared llama-swap context, standing up vLLM-Omni) evaporates. Revive only if the voice grates in longer sessions.

**Agent loop rework — one real tool loop (epic, 3/3 slices).** The pipeline was two phases, not a loop: the brain was called once, its tool calls dispatched, and `react()` commented on the results with no tools offered. Success was terminal, so the agent never saw a successful tool result while it still held tools — every multi-step intent ("play the best move" = `get_best_moves` → read → `make_move`) was structurally impossible.

- [x] **Brain owns a bounded tool loop; pipeline drops phase two and the domain-retry loop** (#124). `LlamaBrain.get_agent_response` is now the STANDARD.md §3 loop (`max_iterations=4`, separate `max_corrections=2`, `completed | max_iterations | correction_limit`): tool results go back as real `role: "tool"` messages on a growing prompt, domain rejections are results the model corrects from inside the turn, schema failures are corrections. The closing tool-less turn *is* the commentary and cannot act, enforcing the CLAUDE.md separation structurally. `react()`, `_failed_calls`, `_retry_command`, `MAX_TOOL_RETRY_ROUNDS` are gone; `Brain.narrate` survives for the fast path, which has no closing turn of its own. Collapsed the two nested retry loops (10 model round-trips worst case) into one bound.
- [x] **Thinking policy for the loop — measure** (#125). The rule was already implemented; nothing *counted* model round-trips (no logger in the brain/provider, `ChatResult.usage` discarded, the evals printed only a duration and a tool-call count). The eval harness now wraps the live provider in a `CountingProvider` (`tests/fakes.py`) and hard-asserts model calls and the per-turn thinking flag. Measured over 3 runs: the fast path is **0** model calls at verbosity=low and **1** (narrate) above it; every tool-using utterance hits the loop's floor of **2** (tool turn + closing turn), never a third, never near `max_iterations`; `judgment_question` shows the `thinking=[off,on]` flip live at 3.4–6.3 s. Fixed stale pre-loop latency prose in `docs/agent-evals.md` (the "12–22 s band" was superseded by the loop rework itself).
- [x] **Use the authoritative player color** (#126). `_run_command` re-derived `player_color = ctx.session.turn`, which names the wrong side whenever a command arrives on the engine's turn (a question asked while the engine thinks; any command in an engine-less session). `_agent_state_dict` now reads `session.player_color` — the field the session already owns — and the redundant parameter is gone: whose *turn* it is is board truth and changes every ply, but which side the human plays is session state and doesn't.

## 2026-07-12

- [x] **`voice.py` off the OpenAI SDK onto httpx** (#119, the chess slice of `../agent-standard/VOICE-PLAN.md` Phase 2): the voice layer now speaks the OpenAI audio wire over plain httpx like the brain's provider — multipart `POST /audio/transcriptions`, JSON `POST /audio/speech` → mp3 — with a Pydantic wire model and typed `SpeechRequestError`/`SpeechResponseError`, dropping the `openai` runtime dep entirely. Adopted the fleet voice contract's env var names (`SPEECH_BASE_URL`, `TTS_BASE_URL`, `TTS_MODEL`, `TTS_VOICE`, `STT_MODEL`) in compose/docs and marked the frontend voice modules (`vad.ts`, `wav.ts`, `tts.ts`, `MicButton.tsx`) canonical for fleet vendoring (header line; drift checked by `agent-standard/check-sync.sh`). Tests: SDK-shaped fakes → `httpx.MockTransport` server fake asserting on the actual wire, +4 typed-error tests. Backend 606, frontend 154, evals 5 passed / 1 xpassed.

## 2026-07-11

- [x] **Post-game screen** (user-requested UI overhaul, slice 3/3): `PostGameModal` pops up at game over — verdict from the player's side ("You won"/"You lost"/"Draw"), humanized termination + result, New game (rolls a random side), Copy PGN (`GET /api/game/pgn` → clipboard, confirms in place), and Close to study the final board with a status-line "Results" chip to reopen. Hidden-not-unmounted on dismiss so a fetched review (a whole-game Stockfish run) survives reopening; a new game unmounts and resets it. Replaces the inline `ReviewPanel` rendering — review now lives inside the modal. (#116)
- [x] **Random starting color + pre-game side switch** (user-requested UI overhaul, slice 2/3): `player_color` is `GameSession` state, persisted in save files (older saves default white); `POST /api/game/new` takes `color: white|black|random` (default random) and the engine makes the opening move when the player draws black — on the UI path and in the `new_game` tool (tool description unchanged, so no eval-gated prompt change). Board orients to the player's side; "Playing as … / Switch to …" shown until the first player move. Color-aware takebacks: undo without `plies` pops the player's full exchange vs the engine and refuses the engine's lone opening (409) — fixes the old 1-ply default that left the human playing the engine's side after an undo. (#115)
- [x] **One stacked layout at every viewport** (user-requested UI overhaul, slice 1/3): the mobile stack (agent bubble + board + move strip + fixed bottom bar + options sheet) is now the app's only layout; deleted the desktop-only subtree (`GameControls`, `MoveHistory`, `useIsMobile`). Wide screens get the same centered column (capped 540px) with the bottom-bar buttons width-capped into a centered cluster. (#114)

- [x] Agent eval harness + gemma-4-12b baseline (Phase 2 slice 8 — **final slice; the agent-standard migration epic is complete**): `tests/test_agent_evals.py`, chess's copy of the workspace standard's golden-task harness (`../agent-standard/STANDARD.md` §6, mirroring PCC's `test_agent_evals.py`). Six scenarios, one per behavior family, each driving one utterance through the **whole delegate path against the live runtime** — a real `LlamaBrain` (gemma-4-12b behind llama-swap) over a real Stockfish (`/usr/bin/stockfish`) — via the same seam the conductor uses, `POST /api/agent/conversations/{id}/messages`, then asserting on the `MessageExchange` wire: trajectory *shape* (successful = `error` null; a played `make_move` also checks `result.legal`), board end-state read back through `GET /api/state`, and `stop_reason` — never exact call sequences (temp 1.0). Opt-in `CHESSAPP_AGENT_EVALS=1` with a module-level `skipif` so CI and default `pytest` never touch the GPU. Mirrors PCC's `_run`/`_successful`/`_mutations` idioms, adapted to the wire (`_legal_moves`, `_board_mutations`, an `[eval]` stats print per scenario). **Fast-path guard:** every scenario asserts `parse_move(utterance, fen) is None` in setup, pinning the eval stays a *model* eval even if `fastparse` grows. Fixtures: function-scoped `eval_app` builds a fresh app+game per scenario by mirroring `build_app` through `create_app` (so the test holds the `ToolContext` to set up positions via the session — bypassing the engine reply — and read settings/end-state); module-scoped Stockfish; the model stays warm on llama-swap across scenarios. TDD: module/fixtures + skip guard red-first, default `pytest` green with evals skipped, then scenarios. Scenarios: `plain_move` ("play e4" — parser lets the verb through; one legal `make_move`, board `[e4, reply]`), `judgment_question` ("how am I doing?" 4 plies in — routes through `evaluate_position`, zero board mutations), `ambiguous_move` ("move the rook", both rook files opened by 1.a4 a5 2.h4 h5 — asks, no move), `settings_by_speech` ("make it easier" — `set_difficulty` weaker than the `casual` default), `honest_illegal` ("castle kingside" move 1 — no fabricated move, board intact), `destructive_confirm` ("new game" mid-game — should confirm first). **Baseline: 3 consecutive full suite runs, the 5 real scenarios pass 3/3 (15/15), warm model** — `plain_move`/`settings_by_speech` ~1–2 s, `honest_illegal` ~2 s, `judgment_question` ~12–22 s (the react step turns *thinking ON* to react to an analysis result, so gemma emits a chain-of-thought — the slowest scenario by design), no cold load observed (gemma stayed warm on :8200, first call ~7 s). No schema self-corrections needed (chess's small closed tool schemas), and `honest_illegal` self-corrects *honestly* — the domain-retry loop feeds the `legal:false` rejection back and gemma concedes in words (`stop_reason=completed`, not `correction_limit`), never faking a move. **The hard part / finding:** `destructive_confirm` was flaky — gemma called `new_game` immediately ~half the time instead of asking. Tightened the scenario first per the eval discipline (trivial 2-ply stub → a 10-ply developed, castled Ruy Lopez) to rule out "nothing to lose"; the ~50% confirmation rate didn't move (9 probes: 5 fired / 4 asked), so it's a **real prompt-adherence gap at temp 1.0, not scenario flakiness**. The prompt carries the rule (`_BASE`) and `test_personality` pins it; the model just doesn't follow it reliably. Left as a **non-strict `xfail`** (invariant is correct; XPASS/XFAIL ratio is the signal), the prompt deliberately **not** changed (evals gate prompt changes — a fix ships in its own PR), recorded as a finding in new `docs/agent-evals.md` + a `TODO.md` item (likely structural fix: a pipeline-owned pending-destructive-op state, same shape as the backlogged pending-move-proposal). New `docs/agent-evals.md` (what it is, run command, gating rule, scenario table, baseline table, the finding, Phase-3 notes on chess's live delegate latency + the destructive-op caveat). Bookkeeping: removed the eval-harness line and the now-complete "Agent-standard migration (Phase 2 epic)" section header from `TODO.md` (voice-on-OpenAI stays in Backlog); added the eval-gate line to `CLAUDE.md`'s Development Process. Full suite **579 passed, 6 skipped** (default run, evals skipped); ruff clean. **Phase 2 done-when met:** chess passes its full suite + the new evals, and a delegate call plays a move end-to-end (slice 6's live smoke + this slice's `plain_move`).
- [x] `agent:` block in `app.yaml` (Phase 2 slice 7): chess now declares its delegate agent in the workspace manifest per `../agent-standard/app-yaml-agent-block.md` — one-sentence third-person `description` (moves, analysis, game control, difficulty), `api: /api/agent` (the slice-6 delegate surface, relative to `upstream`), and five routing-hint `examples` kept concrete and disjoint from PCC's task/project phrasings ("set the *chess* difficulty…", "start a new *chess* game" — disambiguated on purpose; a bare "start a new game" could route two ways once more game apps exist). Conductor discovers the block and builds chess's delegate tool from it; `gen.py` ignores it. YAML validated; no code paths touched, no new tests (manifest-only, same as PCC #54).
- [x] Conversation persistence + delegate REST endpoint (Phase 2 slice 6): chess now speaks the workspace delegate contract (`../agent-standard/delegate-api.md`) so a conductor agent can drive it over HTTP — the §5 surface mounted under `/api/agent/*`, mirroring PCC's `routes_agent.py`/`schemas/conversations.py` field-for-field so both apps speak one byte-compatible wire (`POST /conversations` → 201 ConversationRead; `POST /conversations/{id}/messages` → 200 MessageExchange of two MessageReads; `GET` detail oldest-first; `DELETE` 204; `GET` list, active/most-recent-first for parity). New `chessapp.agent_api`: the Pydantic wire models (`ConversationCreate`/`Read`/`Detail`, `MessageCreate`, `ToolCallRead`, `MessageRead`, `MessageExchange`), an in-memory `ConversationStore`, `resolve_actor`/`DELEGATE_ACTORS={"agent:conductor"}`, a ported per-IP sliding-window rate limiter, and the router. **The core of the slice was the pipeline extraction, not the endpoint:** `/api/command`'s inner machinery (fast-parse seam → `brain.get_agent_response` → registry dispatch → `MAX_TOOL_RETRY_ROUNDS` domain-retry loop → react/low-verbosity fast-path → board-change broadcast) is now a shared `_run_command(text, transcript) -> CommandOutcome` closure in `api.py` that both routes call against the **same single game session**; `/api/command` shrank to a thin wrapper (supply the web panel's `ctx.transcript.window()`, record the settled turn back onto it) and stayed **byte-identical** — its existing `test_command.py`/`test_domain_retry.py`/`test_api.py` suites are the guard and all passed untouched. The delegate route runs the identical pipeline with the conversation's *own* text-turn history as `transcript` (windowed to the panel's 20-turn budget via `history_for_loop`), leaving `ctx.transcript` untouched by delegate calls; the broadcast fires inside the pipeline, so a conductor-played move shows up live on the web board (pinned by a WS test and confirmed in the live smoke). **Locked design decisions, all realized:** in-memory store, no DB/files (soft-delete via a `deleted_at` mark; `store.get` returns `None` for a missing *or* soft-deleted id, so both 404 indistinguishably across GET/POST-messages/DELETE — the rule conductor's recreate-once retry relies on); `stop_reason` is `completed`, or `correction_limit` when the retry loop exhausted its rounds with failures still unresolved (a wordy concession is a normal completion; chess never emits `max_iterations` — the two-phase brain has no iteration loop); `ToolCallRead` maps `ok:false` results → `error` (result null), every other result including a legitimate `legal:false` move rejection → `result` as compact JSON (error null), exactly one set, all rounds' calls in order; `tool_calls` is `null` (not `[]`) when no tools ran; the user turn is committed to the store *before* the pipeline runs, so a `chessapp.provider.ProviderError` → 502 keeps it (pinned); rate limit via `CHESSAPP_AGENT_MESSAGES_PER_MIN` read live per request (default 10, matching PCC's `agent_messages_per_min`), 429 + `Retry-After` on breach. **Three hard parts.** (1) Keeping `/api/command` byte-identical while giving the delegate each call's arguments: the reaction and the UI body must keep seeing exactly `{"name","result"}` (a react-equality test and the frontend contract both depend on it), so the pipeline carries a **parallel `tool_args` list** aligned by index rather than fattening `tool_results` — `CommandOutcome` returns both, `/api/command` exposes only `tool_results`, the delegate zips them into `ToolCallRead`. (2) `stop_reason` detection reuses the existing loop's post-state — `correction_limit` iff `direct_reply is None and failures` after the retry budget — no new control flow, so the byte-identical guarantee holds. (3) **Divergences from PCC, documented in the module docstring:** the in-memory store (vs SQLite — compliant because the 404→recreate rule exists for exactly this, and it matches chess's own in-memory game session with explicit save/resume); and actor attribution as a **structured log line** rather than an `activity_events` row (chess has no audit table), bound from the resolved `X-Agent-Actor` and kept **off the wire** so `MessageRead` stays byte-compatible — chess also had *zero* logging infra, so this slice introduces a stdlib `logging` logger (no new dependency). A circular import (`api` needs the router builder; `agent_api` needs `CommandOutcome`'s type) is avoided by typing `CommandOutcome` under `TYPE_CHECKING` and duck-calling the pipeline. TDD red→green: `tests/test_agent_api.py` written first (22 tests mirroring PCC's, red on the missing module) — CRUD + null-title create + list ordering; missing-and-soft-deleted 404 across all three verbs; ScriptedBrain-driven exchange persistence and wire shape (result XOR error, `legal:false` as a result, `tool_calls` null when none); `completed` and `correction_limit` cases; title auto-derivation; history replays text turns only (asserted via ScriptedBrain's recorded transcript — no tool payloads); fast-path "e4" plays with no phase-one brain call; delegate move broadcasts to `/ws`; provider failure → 502 with the user turn retained; content validation (empty/whitespace/too-long → 422); rate limit 429 + `Retry-After`; and X-Agent-Actor recognized-vs-absent/unrecognized asserted via `caplog`. Live end-to-end smoke outside pytest: a real uvicorn on scratch port :8001 (real `/usr/bin/stockfish` + real gemma-4-12b behind llama-swap, Docker app untouched) — created a conversation, `POST "e4"` landed the fast-path move (stockfish replied `h6`, Glitch in character: "h6? bold. definitely a choice.", `stop_reason=completed`, tool_calls shape verified over the wire), `/api/state` confirmed `history=['e4','h6']`, a natural-language turn with `X-Agent-Actor: agent:conductor` answered ("it's the start… your move.", `tool_calls: null`), then `DELETE` → 204 and re-`GET` → 404. Backend 579, ruff clean. Slice 7 (`agent:` block in `app.yaml`) can point its `api:` at `/api/agent`; slice 8's eval harness can drive goldens through the same `_run_command` seam the delegate uses.
- [x] httpx `ChatProvider` replaces the OpenAI SDK client (Phase 2 slice 5): the LLM brain now speaks the OpenAI chat wire to llama-server over plain `httpx` — no SDK — mirroring PCC's `LlamaCppProvider` pattern, with chess's two-phase `Brain` orchestration unchanged on top. New `chessapp.provider`: a `ChatProvider` Protocol, a `ChatResult` output model (+ `_Wire*` Pydantic models validating the response at the boundary), `LlamaCppProvider` POSTing to `{base_url}/chat/completions` (sampling temp 1.0/top_p 0.95/top_k 64 as *plain body fields*, `chat_template_kwargs` thinking toggle, 300s read / 10s connect timeout for the llama-swap cold load), and typed errors `ProviderError` → `ProviderRequestError` (HTTP/timeout/connection) / `ProviderResponseError` (200 but invalid body) / `ToolCallArgumentsError(tool_name, detail)`. `LlamaBrain` lost its `client` and `model` fields (the provider owns model + wire) and consumes a `ChatProvider`; all orchestration — prompt assembly, schema validation, correction retries, thinking policy, live prompt resolution — stayed exactly where it was. **The one real design decision — the `ToolCallArgumentsError` seam:** the provider is all-or-nothing on JSON parsing (one malformed-JSON tool call raises for the whole turn, like PCC), so `get_agent_response` catches it and treats it as a *correctable failure identical to a schema violation* — feed the error back (it carries the tool name, so the correction turn names the offender), retry to the budget, drop on exhaustion, never crash. Consequence worth flagging for slice 6: a completion mixing a good call with a malformed-JSON call is now all-or-nothing where the good call used to survive exhaustion; the `test_exhaustion_keeps_valid_calls_and_text` partial-validity case is therefore now exercised with a *schema violation* (`{"move": 5}`) — the only failure mode that still returns a `ChatResult`, hence the only one where per-call partial validity is still meaningful. The thought-block invariant relocated *down* a layer and got stronger: `reasoning_content` is validated by the wire model but never lands on `ChatResult` and never in `to_message()` (pinned in test_provider), so the brain structurally cannot leak it — the two old brain-level reasoning-drop tests moved to the provider. **Deliberate divergences from PCC (chess is leaner):** no `ToolSpec` class — chess hands the registry's OpenAI tool dicts (`tools.py definitions()`) straight through, unwrapped; no `chat_structured` (chess has no structured-output need); no structlog (chess has no logging infra, and `llama_brain.py` never had any); kept `to_message()` as a tested contract but dropped `tool_result_message` (chess never feeds `role: tool` messages back — tool execution lives in `api.py`, not a provider-side loop). The model-profile sampling comment is chess's existing `# BRIEF-mandated…` wording carried verbatim into the provider (note: it points at BRIEF, not `../agent-standard/model-profile.md` as the migration map claimed — left as-is rather than rewrite an authoritative comment on a stale recon note). `app.py`'s `openai_client=` injection seam became `provider=` (and `create_llama_brain` builds a `LlamaCppProvider` when none injected, `api_key` param gone — nothing else used it); `api.py` needed zero changes since it only ever touched the `Brain` protocol. TDD red→green: `tests/test_provider.py` written first (12 tests, red against the missing module — `httpx.MockTransport` capturing payloads: sampling/top_k-plain-field/chat_template_kwargs, tools + `tool_choice: auto` verbatim, tool-arg parsing incl. malformed-JSON and non-object → `ToolCallArgumentsError`, reasoning never in `to_message()`, HTTP-500/ConnectError → `ProviderRequestError`, empty-choices wire → `ProviderResponseError`), then the provider to green; `tests/test_llama_brain.py` and the two `tests/test_app.py` live-switch tests reworked off the OpenAI `FakeClient`/`FakeOpenAIClient` shape onto a `ScriptedProvider` double at the ChatProvider seam (new in `tests/fakes.py` alongside `text_turn`/`tool_calls_turn` builders; repeats its last turn like the old fake, and can raise a scripted exception to model a provider failure) — every behavioral assertion survived the move (retry sequences, correction-prompt content, thinking toggles per phase, transcript hygiene between system and current turn, per-request live prompt resolution), with raw-`create()`-kwargs assertions becoming recorded-`chat()`-call assertions and the sampling assertions migrating to test_provider. `pyproject.toml`: `httpx` promoted from dev to a runtime dependency (voice keeps `openai` for Speaches); Dockerfile unchanged (httpx installs via the package). Live wire smoke test outside pytest: one real `LlamaCppProvider.chat()` round trip against the live gemma-4-12b at `127.0.0.1:8200/v1` (trivial message, no tools, thinking off) survived the cold model load and returned a clean `finish_reason=stop` completion with usage populated — proving the httpx payload is wire-correct against the real server (reply: "Hello, how are?"). Backend 557, ruff clean.
- [x] stdio MCP server off the registry + `.mcp.json` (Phase 2 slice 4): chess gains Claude Code tooling parity with PCC — a new `chessapp.mcp_server` wraps `build_registry(ctx)` in a FastMCP server whose 19 tools are the registry's tools *verbatim*, run with `python -m chessapp.mcp_server` (root `.mcp.json` wires it up exactly like PCC's, `cd backend && exec .venv/bin/python -m chessapp.mcp_server`). Chess's registry (unlike PCC's) already carries a pre-derived schema per tool — including `set_difficulty`'s hand-written `oneOf`, which no plain function signature can express — so handing wrapper functions to FastMCP's own `add_tool`/`func_metadata` derivation (PCC's approach) would silently produce a *looser*, wrong schema for that one tool. Instead each tool is built directly as an `mcp.server.fastmcp.tools.base.Tool` with `parameters` set to the registry schema verbatim and a permissive pass-through `fn_metadata` (`_PassthroughArgs`, `extra="allow"`, overridden `model_dump_one_level`) that performs no validation of its own — every call reaches `ToolRegistry.dispatch` untouched, so `dispatch`'s own `jsonschema.validate` remains the *only* place a bad call is rejected, and (confirmed by FastMCP's `_setup_handlers`, which registers `call_tool(validate_input=False)`) no MCP-layer schema check second-guesses it first. That's what keeps a schema violation (`set_difficulty` with no args) and a domain failure (`resume_game` for a nonexistent save) coming back as the ordinary `{"ok": false, "error": ...}` payload with `isError` staying `false`, never a protocol-level tool error. `build_mcp_context()` mirrors `build_app`'s Stockfish discovery (`_engine_from_env()` reading `CHESSAPP_STOCKFISH`, `None` → engine-less, not a crash) and applies the default difficulty tier to a discovered engine the same way `build_app` does — the "never leave an engine unconfigured" invariant holds on the MCP path too. New dev dep `pytest-asyncio>=1.4.0` + `asyncio_mode = "auto"` (the tests drive a real `mcp.ClientSession` over an in-memory transport, the same protocol path a real client exercises). TDD red→green against the pre-written `tests/test_mcp_server.py` (schema-equivalence against `build_registry(ctx).definitions()` modulo the slice-3 golden test's normalizations, a live `make_move` round trip, schema-violation and domain-failure envelopes, engine-less analysis, and `build_mcp_context`'s discovery seam) — 5 of 6 green outright; the 6th (`test_domain_failure_comes_back_as_ok_false_not_a_tool_error`) turned out to have a bug in the red test itself (its `ToolContext` never set `save_dir`, so `resume_game` failed with "saving unavailable" before the missing-save check it means to assert), fixed in this slice with `save_dir=tmp_path` per `test_tools.py`'s pattern. End-to-end stdio smoke test outside pytest: a raw JSON-RPC `initialize`/`tools/list`/`tools/call` exchange piped into the real `python -m chessapp.mcp_server` subprocess confirms the handshake, all 19 tools listed, `get_board_state` and an engine-less `evaluate_position` both round-trip correctly; separately confirmed `build_mcp_context()` finds a real `/usr/bin/stockfish` via `CHESSAPP_STOCKFISH` and applies the `casual` default tier. Backend 547, ruff clean.
- [x] Decorator tool registry (Phase 2 slice 3): migrated the hand-written JSON-schema `Tool` dataclasses to the decorator/FastMCP `func_metadata` pattern (the workspace agent standard — `../agent-standard/STANDARD.md` §1, PCC's `registry.py` as reference). Tools are now registered with a new `ToolRegistry.tool()` decorator used on the 19 `build_registry(ctx)` closures: name from `__name__`, description from the docstring, argument JSON Schema derived from the typed signature via `mcp.server.fastmcp.utilities.func_metadata` (new `mcp>=1.20.0` dep, pulls pydantic which fastapi already required). The hand-written `parameters=` dicts are gone — constraints moved into type hints (`Annotated[int, Field(ge=1, le=…, description=…)]`, `Literal[…]` for enums, `Field(pattern=SAVE_NAME_PATTERN)`). Two closures were renamed to their real tool names (`analyze_last_move_tool`→`analyze_last_move`, `review_game_tool`→`review_game`) with the analysis imports aliased. **Three hard parts:** (1) `set_difficulty`'s exactly-one-of `oneOf` (tier/skill_level/elo) can't come from a plain signature — it keeps a documented `parameters=` override on the decorator (still `check_schema`-validated at registration; its dynamic strength-bounds description is set via `__doc__` assignment since an f-string isn't a constant docstring). (2) `additionalProperties: false` — `func_metadata` doesn't emit it, so `_derive_schema` adds it (chess keeps closed schemas: dispatch rejects extra args and Gemma is prompted against them; the existing boundary tests require it). (3) **Schema equivalence** pinned by a new test against a golden fixture (`tests/tool_definitions_golden.json`, dumped from the pre-migration `definitions()`): compared modulo four documented, harmless normalizations — Pydantic's advisory `title` and `default` keys, the `anyOf:[S,{null}]` nullable-union form it emits for optional typed params (vs the golden's optional-by-omission), and whitespace runs in descriptions (a wrapped docstring joins with newlines where the old string joined with spaces). **Dispatch semantics unchanged:** kept chess's jsonschema-validated `{"ok": False, "error": …}` never-raise envelope (the `api.py` retry loop depends on it) validating against the derived schema, rather than swapping in pydantic `model_validate` — jsonschema is needed for the `oneOf` case anyway, and this keeps the boundary tests (extra-props / non-dict / wrong-type / out-of-range rejection) passing byte-identically. `func_metadata` is the schema-*derivation* vehicle; validation stays jsonschema. TDD red→green: equivalence test green pre-refactor (trivial), 3 decorator-derivation tests red against the missing `.tool()` method, then all green; `test_tools.py`'s 59 boundary/handler tests pass unchanged (only the removed `Tool` dataclass fields stayed identical, so even `test_register_duplicate_name_raises` needed no edit). Backend 541, ruff clean.
- [x] Layered personality (Phase 2 slice 2): the global Glitch character graduated to `agent-standard/personality-global.md` in Phase 0 and chess now vendors it back verbatim (`backend/src/chessapp/personality-global.md`, one `<!-- vendored -->` header line, `check-sync.sh` reports `OK`), rather than hand-rolling its own copy of the generic tone content. `_BASE` (app base prompt — behavioral contract + speech→SAN move guidance) is untouched; the old `_GLITCH` block is gone, split into the vendored global layer (brevity contract, slang whitelist, swearing permission, "help is always real") loaded at import via `_load_global_personality()` (PCC's pattern), and a new `_CHESS_FLAVOR` layer holding only the chess-specific competitive material — the trolling contract (occasional needles, dry blunder line, fake sympathy, long-game callbacks), the "when they get you" cope beat, and the chess-specific help tax ("never troll the player into worse chess"). Composition order is base → global → flavor → dynamic (verbosity/hints unchanged). Verified packaging empirically: a fresh non-editable `pip install .` and a full `docker build` of `backend/Dockerfile`'s build stage both land the vendored `.md` next to `personality.py` and `SYSTEM_PROMPT` composes correctly inside the image — no `pyproject.toml`/`.dockerignore` changes needed (hatchling's default wheel inclusion already covers non-Python files in the package dir, and the repo's `.dockerignore` `*.md` rule is non-recursive so it only strips root-level docs, not nested package data). TDD red→green: 6 new composition tests in `test_personality.py` (vendored file exists, header stripped, base/global/flavor markers present in order) plus the 11 existing character/hot-path tests, all passing unchanged against the recomposed prompt. Backend 536, ruff clean.
- [x] Rename LLM env vars to the workspace agent standard (Phase 2 slice 1): `CHESSAPP_LLAMA_URL`/`CHESSAPP_MODEL` → `LLAMACPP_BASE_URL`/`LLAMACPP_MODEL`, the shared naming every workspace agent app now uses (`../agent-standard/STANDARD.md`). Defaults moved off the old single-server assumption to the actual llama-swap endpoint and model: `http://127.0.0.1:8200/v1` / `gemma-4-12b`. Clean rename, no back-compat alias — deploys are clean `~/deploy` clones where compose and code land together. Touched `backend/src/chessapp/app.py` (`build_app_from_env`, `DEFAULT_LLAMA_BASE_URL`, `DEFAULT_MODEL`), root `docker-compose.yml`, and a `backend/Dockerfile` comment. TDD red→green: two new `test_app.py` cases pin that `build_app_from_env` reads the new names and that the no-env defaults match the standard. Backend 530, ruff clean, `docker compose config` validates. Remaining Phase 2 slices tracked in TODO.md.
- [x] Chill pass on Glitch — Ryan's own lexicon (#104): live feedback from the first real phone games ("it says too much… more chill") turned into three tone-block changes, each pinned by a token test. Brevity inverted: one short line is the norm, two sentences the hard ceiling ("one or two sentences" was read as a target, not a limit). Trolling is occasional and earned — "most turns: no troll, just play"; the needle-move list was being performed every single turn. The slang became a whitelist dictated by Ryan himself: word/bet/ight/for sure (acknowledgment), clean/nasty/filthy/sheesh/goes hard (props), cooked (losing), fr/deadass (emphasis), bro/dude/man (address) — one term at a time; the generic gen-z examples ("wild", "down bad") are gone. Spot-checked live: "Sicilian defense, huh? Bet." / "Standard. Your move, bro." — and eval questions still route through evaluate_position. Backend 528.
- [x] Fix: iOS Safari VAD out-of-memory — single-threaded ORT wasm (#103): with the #100 diagnostic on screen, Ryan's iPhone reported the real hands-free blocker: "no available backend found. ERR: [wasm] RangeError: Out of memory, [cpu] Error: previous call to initWasm() failed" — onnxruntime-web's threaded runtime allocates a large upfront WebAssembly.Memory that iOS Safari refuses under memory pressure (also why it had worked before and stopped: borderline all along). vad-web's ortConfig hook now pins ort.env.wasm.numThreads = 1 (plenty for the ~2MB Silero model), keeping the library's error-only log level. **Confirmed working on the real iPhone** — the first live verification of hands-free voice on iOS Safari, closing the riskiest half of the phone-test backlog item. Frontend 151.
- [x] Fix: continuous voice died on the phone after a rebuild (#99, #100, #101): Ryan's iPhone lost hands-free mode right after the image rebuild. Root cause: every rebuild renames the hashed JS chunks, and a tab still running the cached old build 404s importing the *lazy-loaded* VAD chunk on mic tap — `createVad` swallowed the error and silently degraded to push-to-talk (the rest of the app kept working, so it read as "voice is broken"). Reproduced exactly via a page loaded during an app restart: `Failed to fetch dynamically imported module: /assets/dist-*.js`. Three fixes, TDD each: (#100) `createVad` failures now surface on screen through a new `onUnavailable` event — "Hands-free unavailable (reason) — tap-to-talk instead." — because a phone has no console and silent degradation is undiagnosable; (#101) `staleChunkReload` recognizes the dynamic-import failure wording of Chrome/Safari/Firefox and reloads the page once (sessionStorage one-shot guard, cleared on the next healthy start) to pick up the fresh index.html; (#99, found en route) the Docker image answered 404 to every WebSocket handshake — uvicorn's ws support needs a ws library it doesn't declare, the dev venv happened to have one (so CI stayed green through the in-process TestClient), `websockets>=13` is now a declared runtime dep with a test pinning uvicorn's protocol autodetection, verified `101 Switching Protocols` against the built image. The hands-free loop itself reproduced clean throughout (Chromium desktop + mobile viewports, direct and via gateway, 7+ full cycles; Playwright's Linux WebKit lacks MediaRecorder entirely and can't stand in for iOS — noted for the phone-test backlog item). Frontend 150, backend 526.
- [x] Custom Glitch voice — Kokoro blend on a dedicated TTS server (#97): Glitch's designed voice is a weighted blend of stock Kokoro voices served by a new `kokoro` compose service (`ghcr.io/remsky/kokoro-fastapi-cpu:v0.5.0`, CPU on purpose — zero VRAM, GPU stays with the brain; host debug port 8030). Speaches can't blend or clone, so `SpeechClient` gained an optional `tts_client` / `create_speech_client(tts_base_url=…)` / `CHESSAPP_TTS_URL` seam: TTS goes to Kokoro, STT stays on Speaches, and unset means one backend for both exactly as before (six new boundary tests, red first). Auditioned all eight `am_*` voices plus five weighted blends speaking the same Glitch line, measured pitch/pace (fenrir 140 Hz unhurried, michael 119 Hz slowest/warmest, eric young-but-rushed, onyx too deep); shipped default `am_fenrir(2)+am_michael(1)` as the closest measured fit to the "laid-back young American, audible smirk" spec, with a playable audition artifact for the by-ear final pick (retune = edit `CHESSAPP_TTS_VOICE`, nothing else). Verified end-to-end against live containers: `/api/voice/speak` returns Kokoro mp3 for the blend, `/api/voice/transcribe` still answers from Speaches. Backend 525.
- [x] Verify Glitch live + prompt tuning (#95): first real games against the live gemma-4-12b stack — two games driven through `/api/command` from a working-tree backend on :8001 (shared llama-swap brain; the Docker app's live game state untouched). The character works: deadpan low-key voice, dry blunder roast ("you just gave me your queen. that's... a choice."), long-game callbacks ("after that queen situation"), real help + a jab, effortless slang, and illegal moves rejected cleanly. Four gaps found and fixed in the prompt, each pinned as a token test first: reactions ran 3–4 sentences (tone block now caps at one or two); "who's winning?" was answered from vibes with a wrong material count and no tool call (`_BASE` now routes judgment questions through evaluate_position/analyze_last_move — re-verified live, the tool gets called and the answer matches the score); the react step narrated a capture that never happened (`_BASE` now forbids inventing moves/captures/threats the tools didn't report); winning the engine's queen earned confusion instead of the props-then-cope beat ("wins material off you" is now an explicit trigger). Open watch-item in TODO: zero swears in eight earned moments despite escalated authorization — Gemma softens; next lever is a few-shot exchange. TDD red→green. Backend 519.
- [x] Collapse personalities to Glitch (#93): the eight selectable personalities are replaced by one hand-dialed character — **Glitch**, a gen-z Jarvis: effortlessly competent, fully casual, low-key troll-y (understated needles, dry one-liner roasts, obviously-fake sympathy, long-game callbacks to earlier mistakes), one honest beat of props before the cope when beaten, help that stays genuinely good plus one jab, and explicit permission to swear (load-bearing: the safety-tuned serving model softens every curse without it). Dialed in interactively with Ryan (2026-07-11) — one voice done exactly right beat eight approximations. Removed: the `PERSONALITIES` roster, `Settings.personality`, the `set_personality` tool, and the settings-response field (frontend type + mocks updated). `system_prompt_for(verbosity, hints_mode)` keeps the live-settings provider seam untouched, and the tone-only invariant tests survive reframed (engine reply never detours through MultiPV). Character beats pinned as substantive tokens in `test_personality.py`; BRIEF/CLAUDE record the decision. Companion slices (live tone verification, Kokoro-blend custom voice, Qwen3-TTS spike) tracked in TODO. TDD red→green. Backend 516, frontend 143.
- [x] Fix: no new-game confirmation after game over + captured-panel cleanup (#91): two live-game paper cuts. (1) The destructive-tool prompt rule made the agent ask "are you sure?" even after checkmate; the base prompt now carves out the exception — when the board state shows `game_over` is true there is nothing left to lose, so "new game" calls `new_game` immediately. Pinned across all personalities in `test_personality.py`. (2) The captured-pieces panel overflowed its right edge: the visible "Captured" heading and per-side "white"/"black" labels are gone (aria-labels stay for assistive tech), and every piece glyph now carries U+FE0E (text presentation) so the black pawn — an emoji-default codepoint — renders at the same size as the other pieces instead of as an oversized emoji. TDD red→green. Backend 598, frontend 143.

## 2026-07-10

- [x] Hands-free voice conversation mode (#88): one mic tap now opens a conversation session instead of push-to-talk. An on-device Silero VAD (`@ricky0123/vad-web`, WASM AudioWorklet, self-hosted under `/vad/` — the local-only-voice decision intact) endpoints each utterance after ~1s of silence; the clip is WAV-encoded client-side (new `wav.ts`) and fed down the *existing* transcribe → `/api/command` pipeline (voice still has no path of its own); the mic reopens only after the agent turn *and* its spoken reply finish — half-duplex by construction (`playText` now resolves at playback *end*, not start; new `audioIdle()` gate), so the agent can never hear itself. A second tap — possible in every state, including mid-agent-turn — ends the session; an STT outage exits with an error instead of hammering a dead service; empty transcripts (VAD misfires) resume silently; VAD load failure degrades to the old push-to-talk, and browsers without MediaRecorder still render no mic at all — the text box always works. Assets (model, worklet, ORT WASM) are copied to gitignored `public/vad/` by `scripts/copy-vad-assets.mjs` as predev/prebuild hooks — postinstall can't work (the Docker build runs `npm ci` before the source tree is copied) and `vite-plugin-static-copy`'s dev middleware mis-orders itself under Vite 8 and 404s everything; the VAD stack is a lazy-loaded chunk (main bundle stays 244 kB); the backend registers `application/wasm` for the static mount (streaming WASM compilation requires the MIME type). Verified end-to-end in a real Chromium against the live stack with a Web Audio fake mic looping a TTS-spoken clip: capture → endpoint → exact speaches transcript → agent → auto-resume → tap-out. TDD red→green. Frontend 142, backend 591.
- [x] Fix: file-source capture phrases end-to-end (#85): first live-game bug of the reliability epic — "d takes e5" fell through the fast path (grammar knew piece words, not file sources), the agent rejected the utterance *while offering the identical move back* as "dxe5", and on the player's "yes" it announced "moving forward with dxe5" without calling make_move. Fixed per layer: `fastparse` gains the file-capture form (`<file> takes/captures/x/ex [on] <square>` + optional promotion clause), constrained to a pawn from that file per SAN dxe5 semantics (a knight-only capture falls through — pieces are named by piece), so "d takes e5" now executes with zero LLM calls; the voice normalizer glues split file captures ("d ex e5" → "dxe5") and the whisper prompt shows "d takes e5"; the hot-path prompt gains the "d takes e5" → make_move("dxe5") few-shot plus the missing rule — when the player accepts a proposed move, call make_move in that same turn; never announce a move without calling it. A deterministic pending-proposal fallback is parked in TODO in case the prompt rule isn't enough. TDD red→green, 30 new tests. Backend 589.
- [x] Deterministic fast-parse path for plain moves (#83): fifth agent-reliability slice — the seam BRIEF reserves. An utterance that is *entirely* one unambiguous legal move skips the phase-1 LLM call and goes straight to `make_move` through the same validated registry (one road in, one pipeline, minus a model round-trip; at verbosity=low a canned deterministic confirmation — move, engine reply, outcome if the game ended — stands in for the reaction too, making a plain move a zero-LLM turn). New `chessapp.fastparse.parse_move(text, fen) → SAN | None`, pure and never guessing: legality and disambiguation come from the board itself (python-chess), not pattern cleverness. Notation (SAN/UCI, case- and check-suffix-forgiving; typed case settles `bxc6`-vs-`Bxc6` collisions), spoken phrases ("knight to f3", "takes on d5", "pawn to e8 promoting to a knight"), castling words ("castle kingside", "long castle", "0-0"; bare "castle" fires only when one side is legal). A phrase becomes constraints — piece, target, must-capture, promotion — and fires only when exactly one legal move satisfies them all; zero matches (illegal), several (two knights reach f3; "pawn to e8" hits four promotions), and anything not purely a move ("play e4", "can I play e4?") fall through to the agent unchanged. No retry loop on the fast path (the parse matched a currently legal move); fast-path turns are recorded in the transcript and broadcast as usual. TDD red→green: 50 parser unit tests + 10 pipeline tests (a scripted brain with no phase-1 script fails loudly if consulted). Backend 559.
- [x] STT hardening (#81): fourth agent-reliability slice — two deterministic defenses against spoken moves transcribing badly. `SpeechClient.transcribe` now passes a chess-vocabulary `STT_PROMPT` to the whisper call (whisper conditions on it as if it preceded the audio, biasing toward chess terms over homophones and gluing squares the way the prompt formats them); new `normalize_transcript` repairs unambiguous slips before the text enters the command pipeline — split squares ("e 4"/"b six" → "e4"/"b6"), the knight/night homophone, "king side"/"queen side" — deliberately conservative: only slips with exactly one chess reading; fuzzier mishears stay the agent's job (its prompt teaches repair against the legal-move list, #79). Applied inside `transcribe`, so every audio→text path returns repaired text, including the `/api/voice/transcribe` response the browser feeds into `/api/command`. TDD red→green at the SpeechClient and endpoint boundaries; no live Speaches. Live check of the `prompt` param against real Speaches parked in the manual-testing phase. Backend 499.
- [x] System prompt hot-path rewrite (#79): third agent-reliability slice — the shared `_BASE` personality prompt now teaches the hot path (a spoken move landing as a `make_move` call), inherited by all 8 personalities. Added: speech→tool few-shot examples ("pawn to e4" → `make_move("e4")`, castling as `O-O`/`O-O-O`, promotion/underpromotion syntax `e8=Q`/`e8=N`); the move string must be the matching entry from the board state's `legal_moves` — no match means illegal or misheard, say so or ask, never invent a string; points the agent at `player_color`; one `make_move` per player turn (the engine replies inside the same call — never move for its side or answer the reply); `resign`/`new_game` require a short confirmation question first; and a mangled-voice-transcript warning ("e 4" → "e4", "night to f3" → "knight to f3") — repair obvious slips before matching, ask when the repair isn't obvious. Guarantees pinned as data in `test_personality.py` across every personality (substantive tokens, not exact wording; no live LLM). TDD red→green. Backend 479.
- [x] Domain-retry loop for failed tool calls (#77): second agent-reliability slice — an illegal-move guess no longer ends the turn. After dispatch, `/api/command` checks results for `ok: false` (dispatch/domain errors) or `legal: false` (rejected moves) and feeds a correction command back to the brain against the *fresh* agent state, bounded at `MAX_TOOL_RETRY_ROUNDS` (2) rounds, before falling through to react; rejected moves carry the current legal-move list so the retry picks from truth instead of guessing again. A retry round that answers in words instead of tools (clarifying question / concession) is treated as the final user-facing reply — react is skipped so the answer isn't buried. All attempts stay visible in `tool_results`; the correction is plumbing, never recorded in the transcript. Lives in the pipeline, not `LlamaBrain`, because domain errors only surface after registry dispatch — so any `Brain` benefits (generalizing the brain-internal schema-retry idea), and the deterministic core stays the referee. TDD red→green with the scripted brain (10 new tests: happy path, correction content, fresh-state retries, wordy-reply escape, retry bound, transcript hygiene, WS broadcast); three pre-existing single-failure tests now script the retry round their failure earns. Backend 431.
- [x] Agent-facing board state view (#75): first agent-reliability slice — the brain no longer sees the UI `_state_dict`, whose per-ply `fens` and `dests` were ~75% of the prompt and grew every move (measured ~80% prompt cut at ply 40; bounds late-game growth, the BRIEF's KV-cache watch-item). New `_agent_state_dict` in `api.py` — fen, turn, `player_color`, `in_check` (new `GameSession.is_check()`), SAN history, captured pieces, `legal_moves`, `game_over`/`outcome` — used in both `get_agent_response` and `react`. `player_color` is the side to move when the command arrives (the engine replies inside `make_move`, so it's never the engine's turn here), captured once per command so react's view still names the right color when the player's own move flips the turn or ends the game. The UI keeps its full document for the response body and WS broadcast; the mutation/broadcast check now compares agent views (any board change moves the fen). Brain protocol docstrings document the view shape. TDD red→green at the tool boundary (scripted brain, no live LLM). Backend 421.
- [x] Fix: agent voice almost never played on phones (#72): `playText` called `audio.play()` on a fresh `Audio` element seconds after the user's tap (LLM round-trip + TTS fetch), and mobile autoplay policy rejects playback that far from a gesture — iOS Safari categorically (play must trace to the gesture, per element), Android within a ~5s transient window (hence the occasional success); desktop's sticky activation masked the bug entirely, and the autoplay catch swallowed it silently. Fix is the standard unlock pattern: all playback goes through one module-level shared `<audio>` element, and `unlockAudio()` primes it with a 4-sample silent WAV data URI synchronously inside the gesture handlers (command submit, mic tap, voice-output toggle); the unlock latches only on a successful play, and interrupted clips release their object URLs without revoking the live one's. Verified end-to-end against the live stack (WebKit in the Playwright Docker image, `HTMLMediaElement.play` instrumented): silent clip resolves inside the Send gesture, TTS reply plays on the same element, muted mode stays silent, unlock primes once. Test browsers can't reproduce the mobile *block* (sticky activation) — recorded in the new project verify skill (`.claude/skills/verify/SKILL.md`) along with the app-driving recipe. TDD red→green. Frontend 124.
- [x] Chess.com-style mobile UI (#70): phone-first stacked layout below 768px (desktop layout untouched — a `useIsMobile` matchMedia hook switches between two subtrees, so no CSS-hidden duplicate controls in the a11y tree; jsdom without matchMedia defaults to desktop). Top to bottom: command input + mic/mute/send, spider mascot (inline SVG in the corner-web night-silk motif) with the agent's commentary in a speech bubble, full-width board, compact captured row, one-line move strip with ◀ ▶ **review** arrows (client-side walk of the new per-ply `fens` list in the state document — view-only replay, never mutates; any authoritative broadcast snaps back to live; Undo/Hint disabled while reviewing), and a fixed bottom bar Options/Resign/Hint/Undo with Options opening a bottom sheet (new game, difficulty, voice). Backend: `GameSession.position_fens()` (replayed from the move stack, root first) exposed as `fens` everywhere `_state_dict` goes, and `GET /api/game/hint` wrapping `get_best_moves(n=1)` (503 no engine, 409 game over/no candidates), drawn as a green chessground autoShape arrow. Verified end-to-end with Playwright at 390×844 against a live server. TDD red→green. Backend 416, frontend 115.

## 2026-07-07

- [x] Beginner tier was still too strong — replaced node-limit+random-blunder with a weighted move sampler (#61): #57's sub-floor weakening (`skill 0 + max_nodes=150 + 25% coin-flip random blunder`) still played ~1300+, because between its random blunders it played solid Skill-0 chess and *never hung a piece* — a real beginner got outplayed. Measured against a depth-12 reference, the old beginner threw away ≥200cp on only ~16% of moves; the rest were club strength. Stockfish has no built-in sub-1320 level (`UCI_Elo` floor 1320, `Skill Level 0` ≈ ~1350), so the fix is a better external weakening layer, not a UCI knob. `DifficultyProfile` now carries `temperature`/`sample_depth` instead of `max_nodes`/`blunder_chance`; sub-floor tiers score **every legal move** at a shallow depth and pick one weighted by `exp(-centipawn_loss / temperature)` (pure `sample_weighted`, `MAX_SAMPLE_LOSS=1000` cap so a hopeless move keeps a tiny nonzero weight). Sampling the *whole* legal pool — not Stockfish's top-k, which are all decent — is what lets it hang material like a novice; higher temperature = flatter = sloppier. beginner T=700 / casual T=250 (depth 4). Raw `skill_level`/`elo` still clear the sampler (and vice versa). New `scripts/calibrate_difficulty.py` gauntlet confirms the ordering through the shipped code: beginner 1.5/6 vs casual, casual 0/6 vs the 1320 floor. Upper tiers and the tool/API contract unchanged. TDD red→green. Backend 406.
- [x] Conversation memory — agent remembers prior turns, persisted in saves (#59): the brain was stateless — every `/api/command` sent only system prompt + current command, so "as I said before" / "the queenside one" went nowhere. New `chessapp.conversation.Transcript`: one turn = user command + the commentary the user actually saw (final answers only per BRIEF — never thoughts or raw tool payloads); brains read a bounded `window()` (20 turns) so prompt/KV-cache growth stays capped. Brain seam extended (`get_agent_response`/`react` take a `transcript`, default empty — protocol stays backward-compatible); `LlamaBrain` inserts it between system prompt and the current turn, `react` stays utterance-free. `ToolContext` owns the transcript; the command endpoint threads the same prior-turns window into both phases and records the turn once commentary settles. Persistence: the transcript rides in the save file under a key `GameSession` ignores (no version bump; old saves load with an empty transcript), roles validated on load (`user`/`assistant` only) so a tampered save can't inject system-prompt content. `new_game` deliberately keeps the thread; only resuming a save swaps it. TDD red→green: +20 tests. Backend 401.
- [x] Difficulty tiers with real target strengths (#57): "Beginner" played ~1300+ because tiers mapped straight onto Stockfish `Skill Level`, and no UCI knob goes lower (`UCI_Elo` floors at 1320). Named tiers are now the difficulty contract across tool/API/UI: beginner ~500, casual ~1000, intermediate ~1500 (`UCI_Elo`), advanced ~2000 (`UCI_Elo`), maximum (skill 20). Sub-floor tiers get a deterministic weakening layer in `engine.py` (`DIFFICULTY_TIERS`): skill 0–2 + starved node budgets (150/800 nodes) + per-move random-legal-move blunder chance (25%/10%) with an injectable RNG so the blunder path is unit-testable. Raw `skill_level`/`elo` stay as escape hatches and clear the tier weakening (and vice versa); default is the `casual` tier, applied at assembly. Frontend selector speaks tier names, controlled by `GET /api/settings`. Node counts/blunder rates are first-cut calibration — tune from real games. TDD red→green. Backend 382, frontend 75.
- [x] Difficulty is real now — default strength applied, personality is tone only (#55): two bugs had the engine playing full strength no matter what the UI said. (1) No default difficulty was ever applied — Stockfish boots at Skill Level 20 and the uncontrolled dropdown displayed "Intermediate" without sending it; now `Settings.skill_level` defaults to `DEFAULT_SKILL_LEVEL = 5` (Casual), `build_app` applies the settings difficulty to any attached engine, and the selector is controlled by `GET /api/settings` (placeholder, never a lie, before load or off-preset). (2) The personality style layer picked replies from a full-strength MultiPV analysis (≤150cp below best), bypassing difficulty for the flashy personalities — **product decision: personality affects tone only, never move choice or settings** (BRIEF/CLAUDE.md updated); `chessapp.style` deleted, every engine reply is `EnginePlayer.play_move` at the configured strength. TDD red→green at the API and tool boundaries. Backend 357, frontend 75.

## 2026-07-06

- [x] UI: post-game review panel (#49) — `ReviewPanel` in the web UI surfaces `GET /api/game/review` after game over: on-demand button (whole-game Stockfish analysis is too heavy to auto-run), per-color accuracy + classification counts, and the move list with inaccuracies/mistakes/blunders flagged alongside the engine's better alternative. Purely presentational over the backend's numbers; `fetchReview` in the typed client returns null on 503/409, which the panel renders as "Review unavailable" with the button left in place for retry. Rendered only when `game_over`, so a new game unmounts and resets it. TDD: +4 component tests (mocked client, same pattern as the other panels). Frontend 70 tests.
- [x] Game review: move classification + accuracy (#47): whole-game review as glue over our own Stockfish bridge, adapting lichess's published method (win-percent logistic fit + per-move accuracy curve) instead of depending on the lila codebase. `chessapp.analysis.review_game` replays from the root FEN (live session untouched), analyses each position once (a position's eval doubles as the previous move's outcome), and returns every move classified with cp loss, best alternative, and accuracy, plus per-color accuracy and classification counts. Exposed two ways from the same numbers: the `review_game` agent tool ("how did I play?") and trusted `GET /api/game/review` (503 no engine, 409 empty game). Pure math (win_percent, move_accuracy) always tested; whole-game reviews live against Stockfish (scholar's mate: blunder found and counted, mate costs 0, blundering side scores lower accuracy). **Epic complete: Phase 3 done.**
- [x] Hints + "what was my mistake" explanations (#46): new `chessapp.analysis` — `analyze_last_move(engine, session)` replays the game (root FEN + move stack, never live truth), compares the played move to Stockfish's best from the same position, and returns mover-POV centipawn loss + lichess-style classification (good <50 ≤ inaccuracy <100 ≤ mistake <300 ≤ blunder); mate-delivering moves cost 0 by construction. Exposed as the `analyze_last_move` tool (error data without an engine or moves) so the agent narrates deterministic verdicts instead of judging in its head. `hints_mode` is now real: `system_prompt_for(..., hints_mode)` appends an offer-hints instruction (layered with verbosity), wired through the live prompt provider so "give me hints" changes the next command's prompt. Thinking mode per BRIEF: `LlamaBrain.react` turns thinking ON when reacting to analysis-tool results (`evaluate_position`, `get_best_moves`, `analyze_last_move`), OFF for plain moves. Shared `pov_cp` helper extracted to `engine.py` (style.py now reuses it). TDD: +28 tests.
- [x] Settings by natural speech (#45): closed as an acceptance-test slice — the capability was already built incrementally (settings tools in the registry, live personality/verbosity switching through the prompt provider, difficulty applied to the live engine, voice toggle via `speak`), and voice input lands in the identical `/api/command` pipeline as typed text, so settings-by-voice needed no new production code. New `test_settings_by_command.py` pins each setting end-to-end at the tool boundary with a scripted brain: difficulty (skill + elo) reaching a live-engine double, personality/verbosity/hints landing in the same truth `GET /api/settings` serves, voice flipping the `speak` flag in the same response, bad enum values coming back as error data (never HTTP failures or corrupted settings), and multiple settings in one utterance.
- [x] Personality-biased move selection among Stockfish MultiPV candidates (#44): new `chessapp.style` module — `StyleProfile` per personality (flashy ones prefer captures/checks among near-best candidates, `beginner_bot` takes the weakest still-reasonable option, precise ones just play best), `choose_candidate` (side-to-move POV scoring, `tolerance_cp` sanity cap, forced mates never passed over), `play_styled_move` (every pick still goes through `submit_move` — legality stays the engine's call). Wired into the `/api/game/move` engine reply via `ctx.settings.personality`, so "be the villain" changes how the engine replies, deterministically (no RNG: same position + personality = same move). TDD: +20 style unit tests, +2 API tests with a style-aware fake engine.
- [x] Remaining personalities (#43): full brief roster selectable — trash_talker, grandmaster, villain, silent_assassin, beginner_bot, streamer join friendly_rival + calm_coach. Each is a tone layer over the shared behavioral contract (tools-only, engine owns truth, clarify when ambiguous); `PERSONALITIES`/`SYSTEM_PROMPTS` lockstep pinned by test.
- [x] Evaluate Web Speech API fast path vs local-only privacy mode: **decision — local-only, no Web Speech API path** (`docs/voice-fast-path-evaluation.md`; CLAUDE.md stack note updated). Chrome/Safari implement `SpeechRecognition` by sending mic audio to Google/Apple servers — a hard violation of the "fully offline / fast and private" invariant — and Firefox lacks it / Chrome's needs internet, so a local fallback would be required anyway (two paths, one of which breaks the contract, vs one that always works). Grounded with a live measurement through the real stack: a Kokoro-spoken ~3s command ("Knight to f3, then castle kingside") transcribed by CPU-only `faster-whisper-small` in **~2.5s warm with a perfect chess-vocabulary transcription** — comfortably fine for turn-based chess. Escape hatches recorded (distil/tiny model via `CHESSAPP_STT_MODEL`, GPU image, Speaches realtime WS for interim text — all local). Web Speech *synthesis* rejected on the same grounds (erratic per-device voices; Kokoro is one local voice everywhere). **Epic "Voice" complete; Phase 2 complete.**
- [x] Voice options: output on/off, mute, talk more/less (#41): the brief's voice options, all real. Verbosity now shapes the agent's system prompt — `system_prompt_for(personality, verbosity)` appends a talk-less/talk-more instruction for `low`/`high` (`normal` unchanged, unknown falls back safely), resolved live per command through the same provider seam as live personality switching, so `set_verbosity` ("talk less") changes the very next reply (pinned by an assembled-app end-to-end test with a fake OpenAI client). `GET /api/settings` exposes the agent-adjustable settings document (the same truth the tools mutate) for the UI; `POST /api/settings/voice` is the trusted mute path (the speaker button shouldn't need the LLM), mirroring `set_voice_output`. Frontend: 🔊/🔇 toggle in `CommandBox` (hidden until settings load so it never guesses), `useGame` loads `voice_output` on mount, reflects only server-confirmed values, and syncs from the command `speak` flag — so "turn on voice" said *to the agent* flips the UI switch too. TDD (red→green): +8 backend, +6 frontend. Backend 281, frontend 66, CI green.
- [x] TTS path: agent commentary → audio in the browser (#40): the agent's replies become speech. Backend: `SpeechClient.speak(text)` → mp3 bytes (Kokoro via Speaches; `CHESSAPP_TTS_MODEL` / `CHESSAPP_TTS_VOICE`, defaults `speaches-ai/Kokoro-82M-v1.0-ONNX` / `af_heart`), `POST /api/voice/speak` (text → `audio/mpeg`; 503 no service / 502 upstream / 422 blank; board state untouched), and `/api/command` responses now carry `speak: bool` — the user's `voice_output` setting, which was already agent-togglable via `set_voice_output`, so "turn on voice output" said to the agent works end to end (server owns the decision, client owns playback). Frontend: `tts.playText` (fetch → blob → `Audio`; object URL released on ended and on blocked autoplay; silent no-op when voice is unavailable since the reply is on screen anyway); `useGame` voices commentary fire-and-forget when — and only when — the backend says `speak`. TDD (red→green): +7 backend (speak unit, endpoint 200/503/502/422, speak flag on/off via scripted brain), +6 frontend (tts module incl. URL-leak cases, hook speak/silent). Verified live: `/api/voice/speak` through the real Speaches container returns a playable Kokoro mp3 (64kbps mono MPEG). Backend 273, frontend 60, CI green.
- [x] STT path: mic in browser → transcription → same text pipeline (#39): speak a command and it lands in the exact same `/api/command` pipeline as typed text — voice never gets its own path to the game. Backend: `chessapp.voice.SpeechClient` (STT via the OpenAI SDK pointed at Speaches; client injected in tests, same pattern as the brain — no live speech server in the suite) + `POST /api/voice/transcribe` (multipart audio in → `{text}` out; 503 with no speech service configured, 502 when the upstream fails, board state never touched). Env wiring `CHESSAPP_SPEACHES_URL` / `CHESSAPP_STT_MODEL` (default `Systran/faster-whisper-small`); `python-multipart` added for `UploadFile`. Frontend: `MicButton` push-to-talk (MediaRecorder; mic released as soon as the clip is captured; renders nothing in browsers without recording support; inline errors for blocked mic / unavailable voice) wired into `CommandBox` with `onTranscript={onSubmit}` — the transcript goes down the identical pipeline as a typed command. TDD (red→green): 6 backend tests (SpeechClient unit + endpoint 200/503/502/state-untouched) and 5 MicButton tests (fake MediaRecorder/getUserMedia). Verified live: app run against the real Speaches 0.8.3 container → `/api/voice/transcribe` round-trips (model installed once via `POST /v1/models/<id>` — documented in compose — then fully local). Backend 266 tests, frontend 54; CI green.
- [x] Speaches container (OpenAI-compatible STT+TTS) in compose (#38): first Phase 2 (Voice) slice — the commented placeholder became a real service. Pinned `ghcr.io/speaches-ai/speaches:0.8.3-cpu` (latest stable; **CPU image on purpose** — the llama service already fills the 12GB GPU, and faster-whisper + Kokoro handle single-user home voice fine on CPU). HF cache mounted at `/home/ubuntu/.cache/huggingface` — verified against the image config that it runs as `ubuntu`, not root, so the llama-style `/root` mount would have silently re-downloaded models every start. `CHESSAPP_SPEACHES_URL=http://speaches:8000/v1` added to the `app` service as the seam the STT/TTS slices consume; unset ⇒ voice endpoints 503, gameplay untouched (same optionality contract as the brain). Speaches 0.8 wants a one-time explicit model install (`POST /v1/models/<id>`), documented in the compose comments. Infra-only slice (no tests, mirroring the compose slice). Verified live: container boots healthy, `/health` OK, `/v1/models` responds; compose config validates.
- [x] Verify fully-offline basic gameplay in the full compose stack: brought up `app` + `llama` together and proved the whole experience needs no internet. Method: recreated the compose network with `internal: true` (an override file), which removes *all* external connectivity — stricter than unplugging the WAN — and drove everything from inside the containers via `docker exec` (published ports don't work on internal networks). Confirmed the network was really cut (`curl https://huggingface.co` from the llama container fails), then: llama-server loaded the host-cached GGUF via `-hf` with no internet in ~6s (the `-hf` flag is happy with a warm HF cache offline — the one open risk from the compose slice), and a full brain-driven multi-move game ran through `/api/command`: `play e4` → `make_move`, `your move as black` → `make_move` (Nf6), `play Nf3 for me`, `your move` (d5) — plus `evaluate_position`, `get_best_moves`, `save_game`, and `/api/game/undo`, and the baked-in UI served at `/`. One architecture observation confirmed the core principle under fire: on one read-only turn the model's *commentary* claimed a move it never submitted, but the authoritative history was untouched — the LLM can narrate wrongly yet cannot corrupt state, exactly the referee/orchestrator split the design demands. Verification-only task (no code change; the stack shipped in #34–#36), so no PR of its own — logged here. **Epic "Deployment" complete; Phase 1 (MVP) complete** bar the explicitly-deferred GBNF fallback note under the Agent-brain epic.
- [x] Frontend delivery in compose (#36): the `app` container now serves the built UI at `/` from the same origin as the API, so any browser on the LAN loads the whole app from `:8000` — no proxy, no CORS, and the frontend's relative `/api` + `/ws` URLs (already relative since the wiring slice) just work. Backend: `create_app`/`build_app` gain an optional `static_dir` mounted at `/` via `fastapi.staticfiles` (`html=True`) *after* the API routes so `/api/*` and `/ws` keep priority; `CHESSAPP_STATIC_DIR` wires it from the environment, and it stays optional (unset → API-only, `/` 404s — the UI is an add-on the same way the brain and engine are). Image: `backend/Dockerfile` gains a `node:24-slim` frontend stage (lockfile-first `npm ci` for layer caching → `npm run build`) whose `dist` lands at `/opt/frontend`, served by default via `ENV`. The build context moves to the repo root (the image needs `backend/` + `frontend/`), so `.dockerignore` moved to the root and widened; compose points at the new context. TDD (red→green): two `test_app.py` tests — static dir configured → `/` serves index, hashed assets 200, API keeps priority; no static dir → `/` 404s while `/api/state` works. Verified live in the composed container: `/` serves the UI, the hashed JS bundle 200s, and a move with engine reply works. Backend 260 tests + ruff green; CI (lint/test/frontend) green.

## 2026-07-04

- [x] docker-compose.yml (app + llama-server + Speaches placeholder, home-network config): root `docker-compose.yml` ties the layers together — `app` builds from `backend/Dockerfile` (Stockfish bundled), publishes `8000:8000`, and points the brain seam at `CHESSAPP_LLAMA_URL=http://llama:8080/v1` on the compose network; `llama` runs `ghcr.io/ggml-org/llama.cpp:server-cuda` with the *verified* balanced Gemma-4 26B config from the runtime notes (`-hf …:UD-Q4_K_XL -ngl 99 -ncmoe 12 --no-mmproj -fa on --cache-type-k/v q8_0 -c 8192 --jinja`, sampling `--temp 1.0 --top-p 0.95 --top-k 64`), GPU via `deploy.reservations.devices` and the host HF cache bind-mounted (the `-hf`→`~/.cache/huggingface` gotcha, or the ~14GB GGUF re-downloads every start). `app.depends_on: llama` is **ordering only, not a health gate** — the app must not wait ~100s for the model to load before serving an engine-only game, keeping the "LLM-off gameplay works" guarantee crisp (with `llama` down, `/api/command` errors but the board plays fully). Saved games persist to a `chess-saves` named volume via `CHESSAPP_SAVE_DIR=/data/saves`; both services `restart: unless-stopped` for an always-on home box. Speaches (Phase 2 voice STT+TTS) is a commented placeholder — left unpinned/unstarted until the Voice epic wires the browser mic/TTS paths. MTP speculative decoding is intentionally omitted (no drafter is published for this MoE — see runtime notes). One-line `backend/Dockerfile` fix folded in: the image now pre-creates `/data/saves` owned by `app` (`mkdir -p /data/saves && chown -R app:app /data`) so the mounted named volume inherits app ownership on first creation and stays writable by uid 1000 — without it the root-owned volume broke `save_game` for the non-root runtime user (caught during verification). Infra-only slice (declarative compose + Dockerfile, no Python), so no new unit tests, mirroring the Dockerfile/CI-job slices. Verified: `docker compose config` validates; brought up `app` alone (`--no-deps`) → `healthy` in ~2s, `/api/state` 200, `POST /api/game/move {move:e2e4, engine_reply:true}` → `history ['e4','e5']` (bundled Stockfish replied inside the composed container, proving engine-on/LLM-off gameplay), `/api/command` → connection error to the down `llama` (expected — brain configured but unreachable), and the save volume is `app app`-owned + writable (`WRITE_OK`) after the Dockerfile fix. Backend ruff clean. (Full-stack `app`+`llama` offline verification + frontend-in-compose delivery split into follow-up TODO items.)
- [x] Backend Dockerfile: containerize the "app" layer for the compose stack. Multi-stage `backend/Dockerfile` — a `python:3.12-slim` build stage compiles the wheel into a venv at a fixed `/opt/venv` (relocatable to the same path in runtime, so the `chessapp` console-script shebang stays valid; only `pyproject.toml` + `src/` enter the build context, guarded by a new `backend/.dockerignore`), and the runtime stage installs the Stockfish Debian package (binary at `/usr/games/stockfish`) and copies the venv. Stockfish is bundled *in* the app image, not a sidecar — it's a calculation tool driven in-process via python-chess UCI, so a full deterministic game works with the LLM off; the llama-server brain stays a separate container reached over `CHESSAPP_LLAMA_URL`. `ENV` attaches the engine by default (`CHESSAPP_STOCKFISH`, still optional — unset to run engine-off), binds `0.0.0.0:8000` for home-network reach, and puts the venv first on `PATH`. Runs as non-root `app` (uid 1000); a self-contained `HEALTHCHECK` (python urllib against `/api/state`, no curl in slim) that the compose slice can hang `depends_on` on. Infra-only slice (no app code → no new unit tests, mirroring the CI-job slices); verified by building the image and driving a live game in the container: `/api/state` → 200 in ~2s, `POST /api/game/move {e2e4, engine_reply:true}` → `history ['e4','e5']` (bundled Stockfish replied, proving the engine is wired), `docker inspect` health → `healthy`, process runs as `app`. Backend 258 tests + ruff still green.
- [x] Frontend CI job (lint + build + tests): added a `frontend` job to `.github/workflows/ci.yml` running in parallel with the backend `lint`/`test` jobs. Job-level `defaults.run.working-directory: frontend` overrides the workflow-level backend default; steps mirror the documented local dev loop — `npm ci` → `npm run lint` (oxlint) → `npm run build` (`tsc -b && vite build`) → `npm test` (vitest, 49 tests). Node 24 (matches the dev environment) with npm caching keyed on `frontend/package-lock.json`. Config-only slice (declarative workflow, no app code), so no new unit tests; verified all three commands green locally and the PR's own `frontend` check passed (29s) alongside backend lint/test (#32).
- [x] Text command box → agent endpoint, commentary display: frontend-only slice — the `/api/command` pipeline (string → brain → tool call(s) → engine → react) already existed and returns `{commentary, tool_results, state}`, so no backend change. `api.ts` gained a typed `sendCommand(text)` client that POSTs `/api/command` and returns the `CommandResponse`, or `null` when the agent is unavailable (503: no brain configured) — mirroring `postLifecycle`'s ok-check convention. `useGame` gained `commentary`/`agentThinking`/`sendCommand`: the command applies the returned authoritative state (unchanged for a question/read-only command, since the agent acts only through tools and can never corrupt state) and surfaces the commentary; a null response leaves the board untouched and shows "The agent is unavailable."; `agentThinking` gates the in-flight state via a `try/finally`. New presentational `CommandBox.tsx` (a text input + Send button + commentary display) owns only its ephemeral input text, submits trimmed/non-empty commands to the parent, clears on submit, and while `thinking` locks the input and shows a "Thinking…" hint in place of the prior reply. `App` renders it below the board status. TDD throughout (red→green): `CommandBox.test.tsx` (+4: submit-and-clear, whitespace-trim/blank-guard, commentary display, thinking lock) and two `useGame.test.tsx` cases (command applies state + commentary; unavailable agent leaves state untouched + "unavailable" notice). Verified the live endpoint returns the exact `{commentary, tool_results, state}` shape the frontend consumes (scripted-brain `make_move` → commentary + updated fen/history) and that a no-brain context yields 503 → the frontend's unavailable path. `npm run lint`/`build`/`test` green (49 frontend tests, +6).
- [x] Game controls (new game, undo, resign, difficulty): wired the four controls into the React UI plus a backend HTTP path for difficulty. New/undo/resign already had endpoints; difficulty was only reachable through the LLM `set_difficulty` tool, so added a trusted `POST /api/game/difficulty` — exactly one of `skill_level`/`elo` (pydantic `model_validator` → 422 on bad shape), range-validated via `engine.validate_skill_level`/`validate_elo` *regardless of whether an engine is attached* (409 on out-of-range), so the setting is always sane and re-applies when an engine attaches later; no broadcast since it doesn't touch board state. Frontend: new `GameControls.tsx` presentational component (New game / Undo / Resign buttons + a difficulty `<select>` over Stockfish skill-level presets) with Undo disabled when `history` is empty and Resign disabled once `game_over`; presets + `DEFAULT_SKILL_LEVEL` live in `difficulty.ts` (kept out of the `.tsx` so oxlint's `only-export-components` stays clean, matching the `promotion.ts`/`PromotionPicker.tsx` split). `useGame` gained `newGame`/`undo`/`resign`/`setDifficulty`: lifecycle actions apply the authoritative returned state, and `api.ts`'s `postLifecycle` returns `null` on a non-ok response (409 refusal, e.g. nothing to undo) so the hook leaves state untouched; the difficulty select is uncontrolled (starts at a sensible default, emits only on change) and applies nothing since it isn't a board mutation. `api.ts` gained typed `newGame`/`undo`/`resign`/`setDifficulty` clients. TDD throughout (red→green): backend `test_api.py` (+5: skill/elo apply, settings updated + last-one-wins, exactly-one → 422, out-of-range → 409), frontend `GameControls.test.tsx` (+4) and `useGame.test.tsx` (+5: new clears feedback, undo submits `{plies:1}`, refused lifecycle leaves state, resign, difficulty doesn't bump revision). Verified all four over live HTTP (difficulty apply/422/409, move→undo→409, resign, new). Backend 258 tests + ruff clean; frontend 43 tests + oxlint/build clean (#29).
- [x] Move history + captured-pieces panels: frontend-only slice — the state document already carries `history` (SAN plies) and `captured` (`{white,black}` of piece-type letters; white's list = the black pieces White took), so no backend change. Two pure helpers + two presentational components, TDD throughout. `src/moves.ts` (`pairMoves` groups a flat ply list into `{number, white, black|null}` rows, black null on an incomplete final pair); `src/pieces.ts` (`PIECE_GLYPHS` full white/black unicode set + `materialScore` summing standard values, unknown symbols → 0). `src/MoveHistory.tsx` renders a numbered white/black table (placeholder before any move); `src/CapturedPieces.tsx` shows each side's captures using the *opponent's* colour glyphs (White captures black pieces) plus a `+N` material-advantage badge on whoever is ahead (net `score(white) − score(black)`; even material shows no badge). `App` gains a flex `game-layout` (board column + right-hand `panels` aside, wraps/stacks on narrow screens). Tests (red→green): `moves.test.ts`, `pieces.test.ts`, `MoveHistory.test.tsx`, `CapturedPieces.test.tsx`. Verified the backend serves the exact shapes the panels consume (played `e4 d5 exd5 Qxd5` → `history` SAN list, `captured {white:['p'],black:['p']}` = even → no badge). `npm run lint`/`build`/`test` green (34 frontend tests, +12).
- [x] Pawn-promotion UX (piece picker): the interactive board sends bare `from+to` UCI, so a pawn reaching the last rank was rejected as illegal (backend needs the `e7e8q` suffix). Frontend-only slice — the backend already accepts promotion UCI and `legal_destinations` already dedupes the four promotions to one target, so the board offers `e7→e8` and the frontend now supplies the piece. New pure `src/promotion.ts` (`isPromotion(fen, from, to)` parses the FEN piece-placement field: pawn on the origin + destination on rank 8/1 — no chess.js dependency); `src/PromotionPicker.tsx` a presentational modal overlay (Q/R/B/N buttons with colour-correct unicode glyphs, backdrop-click cancels, `role="dialog"`). `useGame` owns the orchestration since it owns move submission: `play` detects a promotion via the live fen (held in a `stateRef` so the callback identity doesn't churn) and, instead of submitting, sets `pendingPromotion={from,to}`; `completePromotion(piece)` submits `from+to+piece` and clears it; `cancelPromotion()` clears it and bumps `revision` so the board re-syncs and the pawn — already moved to the last rank by Chessground — snaps back. `App` renders the picker over the board (relative-positioned `.board-wrap`) when a promotion is pending. TDD throughout (red→green): `promotion.test.ts` (push/capture/underpromotion-target detection, non-pawn + empty-square negatives), `PromotionPicker.test.tsx` (four pieces offered, UCI letter reported, backdrop cancels, inside-click doesn't), and three `useGame.test.tsx` cases (defer-not-submit, submit-with-piece, cancel-snaps-back). jsdom can't drive real Chessground drag geometry (same limit as the wiring slice), so the drag→picker→submit path rests on the hook tests plus a live backend round-trip verifying `e7e8q` → `e8=Q+` while bare `e7e8` is rejected. `npm run lint`/`build`/`test` green (22 frontend tests).
- [x] Interactive board wired to backend state: the React board now renders the live position from the backend and submits moves. Backend: `GameSession.legal_destinations()` groups legal moves by origin square in coordinate form (`{"e2": ["e3","e4"]}`, promotions deduped to one target, empty once the game is over) — python-chess stays the sole move-truth source; the frontend renders these, never generating them. Exposed as a new `dests` field on the `/api/state` document. Frontend: `src/api.ts` (typed `fetchState`/`submitMove`/`stateSocketUrl` client) + `src/useGame.ts` hook, which loads the initial state, keeps it live over the `/ws` broadcast, and submits moves via `POST /api/game/move`. Illegal-move feedback + snap-back: the backend's response is authoritative in both cases — on rejection the hook surfaces the reason and re-applies the *unchanged* state, and a monotonically-increasing `revision` (bumped on every apply) forces `Board` to re-sync so the illegally-moved piece snaps back even though the FEN didn't change. `Board.tsx` became interactive (movable `dests`/`turnColor`, `movable.events.after` → `onMove`, `viewOnly` once the game is over; `onMove` held in a ref so a new handler identity per render doesn't churn the board). Vite dev proxy added for `/api` + `/ws` → `localhost:8000`. TDD throughout: backend `legal_destinations` + state-shape tests (red→green), frontend `useGame.test.tsx` (fake WebSocket + mocked fetch prove initial load, WS push, legal-apply, illegal-revert-with-feedback) and `Board.test.tsx` (fen re-sync, interactive mount). jsdom can't exercise real Chessground drag (no layout/pointer geometry), so the drag→submit path is covered by the hook tests + a live backend round-trip check (`dests` present, legal flips turn, illegal returns `legal:false`+reason+unchanged FEN); promotion UX deferred (bare `from+to` UCI → follow-up in TODO). `npm run lint`/`build`/`test` green; 253 backend tests pass.
- [x] Scaffold React app in `frontend/` (Vite + TypeScript) with **Chessground** as the board layer (decision: GPL is already app-wide via python-chess/Stockfish, so lichess-grade board UX wins over react-chessboard's MIT). `src/Board.tsx` is a thin React wrapper owning a Chessground instance's lifecycle: a mount-once effect creates/destroys the instance (no prop deps), a sync effect pushes `fen`/`viewOnly` via `api.set()` without re-creating it. Board is display-only for this slice — the backend stays the source of truth; wiring to state + moves is the next slice. Vitest (jsdom) + Testing Library toolchain added; `Board.test.tsx` (TDD, red→green) asserts the board mounts (`.cg-wrap`), renders the 32 start pieces (excluding Chessground's `.ghost` drag piece), and destroys on unmount. Vitest config split into `vitest.config.ts` so the build's `tsc -b` never has to reconcile Vitest's bundled Vite types with Vite 8. Vite demo cruft removed; `npm run lint`/`build`/`test` all green, dev server boots clean.
- [x] Fake/scripted brain implementation for tests (`tests/fakes.py::ScriptedBrain`): the one canonical no-LLM `Brain` double for the whole suite, extracted from the two divergent copies that had grown inline (a full recording double in `test_command.py`, a minimal single-response one in `test_app.py`). It pops canned phase-one `AgentResponse`s in order (under-scripting raises `IndexError` so a test asking for more turns than it planned fails loudly), pops scripted `react` texts with a `"(reaction)"` default, and records both `calls` and `react_calls` for assertions. `test_fakes.py` pins the double's own contract; `test_command.py` and `test_app.py` now import it instead of redefining it. Closes the Agent-brain epic bar the deferred GBNF fallback.
- [x] App-assembly entrypoint + live personality switching (`chessapp.app`): `build_app` assembles the full app around one shared `ToolContext` (session + settings + optional engine), builds the `LlamaBrain`, and wires it to `create_app`. `LlamaBrain.system_prompt` now accepts a callable provider (not just a fixed string), re-resolved every request; `build_app` passes `lambda: system_prompt_for(ctx.settings.personality)`, so `set_personality` — which mutates exactly that setting — switches personality live on the next command (verified end-to-end through the assembled app with a fake OpenAI client). `create_llama_brain` gained `system_prompt_provider` and an injectable `client`. `build_app_from_env` + a `main()` uvicorn shim (`chessapp` console script) make it runnable; both engine and brain stay optional so LLM-off / Stockfish-off gameplay still works. Closes the last open item of the Agent-brain epic.
- [x] Personalities as system prompts + clarifying-question path (`chessapp.personality`): the two Phase-1 personalities (`friendly_rival`, `calm_coach`) are now real system prompts, each layering tone over a shared base that pins the non-negotiable contract — the agent acts only through tools, never referees legality, and asks one short clarifying question instead of guessing on ambiguous input. `system_prompt_for(name)` resolves the prompt (falling back to the default for any unknown name); `SYSTEM_PROMPTS` is kept in lockstep with `tools.PERSONALITIES` by a test. `create_llama_brain` now takes a `personality` name and resolves it to the prompt (the brain stays personality-agnostic, carrying the resolved string). Closes the clarifying-question item too: the endpoint already handled the no-tool-call reply mechanically, and the "ask, don't guess" directive now ships in every personality. Live switching via `set_personality` is deferred to when an app-assembly entrypoint exists (see TODO).
- [x] Agent game loop — react-from-new-state step: split `/api/command` into two phases (`get_agent_response` → tool call(s) → engine executes → `react`). Phase two reads the *new* board state + what changed (tool results), never the raw utterance, so commentary is grounded in what happened and a future deterministic fast-parse path stays free to drive phase one without an LLM. No-op replies (questions/clarifications) skip the reaction; read-only tools still trigger it so their answers are grounded in the result. `LlamaBrain.react` offers no tools and reads `content` only (no thought-block leak) (#20).
- [x] Defensive tool-call parser + retry loop in `LlamaBrain`: each tool call is validated against its schema (name known, arguments parse as JSON, args satisfy the JSON schema); an invalid call feeds the specific error back to the model and retries (bounded by `max_retries`, default 2). On exhaustion the valid calls are kept and invalid ones dropped — never a crash, complementing the registry's dispatch-time guard.
- [x] llama-server brain (`chessapp.llama_brain.LlamaBrain`): the OpenAI-SDK `Brain` behind the seam, pointed at llama.cpp on localhost. Injected client (tests mock it — no live LLM); tool schemas sourced from the registry; reads `content` only (drops Gemma's separate `reasoning_content` so thought blocks never leak); thinking OFF by default (per-request `chat_template_kwargs.enable_thinking`), ON for analysis. **Verified end-to-end against a live Gemma-4 `UD-Q4_K_XL` run: structured tool calls work natively, so the GBNF fallback is deferred as unneeded.**
- [x] Brain interface (`chessapp.brain`): `Brain.get_agent_response(board_state, command) → AgentResponse{text, tool_calls}` — landed with the command endpoint; also closes the first Agent-brain-epic item (#17)
- [x] Text command endpoint `/api/command`: string in → brain seam (`Brain.get_agent_response`) → tool calls through the validated registry → commentary + tool results + new state out; broadcasts on change; scripted fake brain in tests (#17). **Epic "API layer" complete.**
- [x] WebSocket `/ws` state channel: snapshot on connect, broadcast to all clients after every successful mutation, dead sockets dropped silently (#16)
- [x] FastAPI app: game lifecycle endpoints (move w/ optional engine reply, new, undo, resign, PGN) + full state fetch; illegal moves are data, domain failures are 409s (#15)
- [x] Settings tools: `set_difficulty` (exactly one of skill_level/elo, applied to live engine), `set_personality`, `set_verbosity`, `set_hints_mode`, `set_voice_output` (#14). **Epic "Tool layer" complete.**
- [x] Write tools through the legality gate: `make_move` (legal/illegal as data), `undo`, `new_game`, `resign`, `save_game`/`resume_game` (traversal-safe names), `export_pgn` (#13)
- [x] Tool registry + JSON schemas + validated dispatch (un-crashable LLM boundary) + read tools: `get_board_state`, `get_legal_moves`, `get_move_history`, `get_captured_pieces`, `evaluate_position`, `get_best_moves` (#12)
- [x] Full offline game vs Stockfish with no LLM in the loop — acceptance tests: engine-vs-engine to a result, scripted-user-vs-engine, save/resume mid-game (#11). **Epic "Deterministic core" complete.**
- [x] Stockfish analysis tools: `evaluate_position` (white-POV cp/mate), `get_best_moves` (MultiPV candidates with SAN + scores) (#10)
- [x] Stockfish bridge via python-chess UCI: `EnginePlayer` with `Skill Level` / `UCI_Elo` difficulty; CI installs stockfish, local tests skip without it (#9)
- [x] Export PGN (SAN movetext, Result incl. resignation, SetUp/FEN headers for custom starts) (#8)
- [x] Save game / resume game (JSON: root FEN + UCI moves + resignation; resume replays through the legality gate) (#7)
- [x] Resign / result recording (session-level termination folded into outcome/is_game_over) (#6)
- [x] Undo (ply takeback; plies=2 gives vs-engine pair semantics, pairing policy stays in the caller) (#5)
- [x] Move history + captured-pieces derivation from board state (#4)
- [x] `GameSession` class wrapping `python-chess`: new game, submit move (SAN/UCI → accept/reject), turn tracking, game-over detection (mate/stalemate/draw rules) (#2)
- [x] Pushed initial commit to github.com/ryanskarnulis/chess; first CI run green (lint + test)
- [x] Repo settings: delete-branch-on-merge enabled; auto-merge/branch protection confirmed unavailable on Free-plan private repos → merge-on-green handled via `gh pr checks --watch` + squash-merge
- [x] README.md: project overview, architecture summary, status/roadmap, dev setup
- [x] Restructured to monorepo layout: Python project moved into `backend/` (frontend/ arrives in Phase 1)
- [x] Removed docs/DEVELOPMENT.md — CLAUDE.md is the single source of truth for process
- [x] Created TODO.md (living backlog) and DONE.md (this file)
- [x] CI pipeline: GitHub Actions running ruff lint/format-check + pytest with coverage on every PR and push to main
- [x] Python scaffold: src-layout package `chessapp`, pytest + ruff dev tooling, smoke test green locally
- [x] Created private GitHub repo `ryanskarnulis/chess` (initial push pending `workflow` token scope)
- [x] CLAUDE.md: architecture principles, TDD/agile process, git/PR workflow, commands
- [x] Project brief (BRIEF.md): architecture, stack selection, phasing, risks
